# Runtime Artifacts

This directory is for local runtime outputs only.

- `source-pages/`: cached readable text fetched from source article URLs.
- `pipeline-reports/`: local text reports generated by worker runs.

Do not commit real runtime artifacts, downloaded page text, reports, or secrets.
