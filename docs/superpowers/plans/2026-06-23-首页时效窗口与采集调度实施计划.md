# 首页时效窗口与采集调度 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 `GET /events` 默认返回最近 48 小时内的首页事件流，并在低量时向 7 天窗口兜底，同时保持历史事件详情可访问。

**Architecture:** 不新增数据库表，不修改 Agent 生产链路。只在 `ProductQueryService` 增加首页窗口选择逻辑，FastAPI 和前端继续消费同一个 `GET /events` 契约。

**Tech Stack:** Python 3.13, SQLAlchemy 2, FastAPI, pytest, PostgreSQL/SQLite test fixtures.

---

## File Structure

- Modify: `apps/worker/worker/services/product_query_service.py`
  - Add recent-window selection for `list_published_events`.
  - Keep existing ordering and response shape.
- Modify: `apps/worker/tests/test_product_query_service.py`
  - Add service-level tests with fixed `now`.
- Modify: `apps/worker/tests/test_product_api.py`
  - Add API-level regression for default homepage recency.
- Modify: `docs/07-验收与运行/后端P1测试记录.md`
  - Record commands, fixtures, results, failures, and uncovered scope.
- Modify: `docs/00-项目总览/项目状态.md`
  - Record P1-10 completion after implementation.

## Task 1: ProductQueryService Tests

**Files:**
- Modify: `apps/worker/tests/test_product_query_service.py`

- [ ] **Step 1: Add failing test for 48-hour default window**

Add a test near existing `list_published_events` tests:

```python
from datetime import UTC, datetime, timedelta


def test_list_published_events_defaults_to_recent_48_hour_window():
    session = make_session()
    service = ProductQueryService(session)
    now = datetime(2026, 6, 23, 12, 0, tzinfo=UTC)

    recent = publish_reviewed_dossier(
        session,
        create_reviewed_dossier(
            session,
            candidate_key="recent-ai-event",
            title="最近 AI 事件",
        ),
    )
    recent.published_at = now - timedelta(hours=2)
    within_window = publish_reviewed_dossier(
        session,
        create_reviewed_dossier(
            session,
            candidate_key="within-ai-event",
            title="窗口内 AI 事件",
        ),
    )
    within_window.published_at = now - timedelta(hours=47)
    old = publish_reviewed_dossier(
        session,
        create_reviewed_dossier(
            session,
            candidate_key="old-ai-event",
            title="过期 AI 事件",
        ),
    )
    old.published_at = now - timedelta(hours=72)
    session.commit()

    items = service.list_published_events(limit=20, offset=0, now=now, min_recent_items=0)

    slugs = [item.slug for item in items]
    assert recent.slug in slugs
    assert within_window.slug in slugs
    assert old.slug not in slugs
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
cd apps/worker
.\.venv\Scripts\python.exe -m pytest tests/test_product_query_service.py::test_list_published_events_defaults_to_recent_48_hour_window -v
```

Expected: FAIL because `list_published_events()` does not accept `now` and does not filter by `published_at`.

- [ ] **Step 3: Add failing test for 7-day low-volume backfill**

```python
def test_list_published_events_backfills_to_7_days_when_recent_count_is_low():
    session = make_session()
    service = ProductQueryService(session)
    now = datetime(2026, 6, 23, 12, 0, tzinfo=UTC)

    recent = publish_reviewed_dossier(
        session,
        create_reviewed_dossier(
            session,
            candidate_key="recent-ai-event",
            title="最近 AI 事件",
        ),
    )
    recent.published_at = now - timedelta(hours=3)
    backfill = publish_reviewed_dossier(
        session,
        create_reviewed_dossier(
            session,
            candidate_key="backfill-ai-event",
            title="兜底 AI 事件",
        ),
    )
    backfill.published_at = now - timedelta(days=3)
    too_old = publish_reviewed_dossier(
        session,
        create_reviewed_dossier(
            session,
            candidate_key="too-old-ai-event",
            title="超出兜底窗口 AI 事件",
        ),
    )
    too_old.published_at = now - timedelta(days=9)
    session.commit()

    items = service.list_published_events(limit=20, offset=0, now=now, min_recent_items=8)

    slugs = [item.slug for item in items]
    assert recent.slug in slugs
    assert backfill.slug in slugs
    assert too_old.slug not in slugs
```

- [ ] **Step 4: Add failing test that old detail remains accessible**

```python
def test_get_event_by_slug_still_returns_old_published_event():
    session = make_session()
    service = ProductQueryService(session)
    now = datetime(2026, 6, 23, 12, 0, tzinfo=UTC)

    old = publish_reviewed_dossier(
        session,
        create_reviewed_dossier(
            session,
            candidate_key="old-detail-event",
            title="老详情 AI 事件",
        ),
    )
    old.published_at = now - timedelta(days=9)
    session.commit()

    detail = service.get_event_by_slug(old.slug)

    assert detail is not None
    assert detail.slug == old.slug
```

- [ ] **Step 5: Run full service test file**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_product_query_service.py -v
```

Expected: new tests fail before implementation.

## Task 2: Implement Recent Window

**Files:**
- Modify: `apps/worker/worker/services/product_query_service.py`

- [ ] **Step 1: Add imports**

Add:

```python
from datetime import UTC, datetime, timedelta
from sqlalchemy import func
```

- [ ] **Step 2: Extend `list_published_events` signature**

Change the signature to:

```python
def list_published_events(
    self,
    *,
    limit: int = 20,
    offset: int = 0,
    category: str | None = None,
    recent_hours: int = 48,
    min_recent_items: int = 8,
    backfill_days: int = 7,
    now: datetime | None = None,
) -> list[ProductEventListItem]:
```

- [ ] **Step 3: Add cutoff helpers**

Inside the class add:

```python
    def _current_time(self, now: datetime | None) -> datetime:
        if now is not None:
            return now if now.tzinfo else now.replace(tzinfo=UTC)
        return datetime.now(UTC)

    def _published_base_filters(self, category: str | None) -> list[object]:
        filters: list[object] = [PublishedEvent.status == "published"]
        if category:
            filters.append(PublishedEvent.category == category)
        return filters
```

- [ ] **Step 4: Implement two-stage cutoff**

Replace the query body with:

```python
        current_time = self._current_time(now)
        recent_cutoff = current_time - timedelta(hours=recent_hours)
        backfill_cutoff = current_time - timedelta(days=backfill_days)
        base_filters = self._published_base_filters(category)

        recent_count = self.session.scalar(
            select(func.count())
            .select_from(PublishedEvent)
            .where(*base_filters, PublishedEvent.published_at >= recent_cutoff)
        ) or 0

        cutoff = backfill_cutoff if recent_count < min_recent_items else recent_cutoff
        statement = (
            select(PublishedEvent)
            .where(*base_filters, PublishedEvent.published_at >= cutoff)
            .order_by(
                PublishedEvent.homepage_rank.is_(None),
                PublishedEvent.homepage_rank.asc(),
                PublishedEvent.ranking_score.desc(),
                PublishedEvent.published_at.desc(),
                PublishedEvent.created_at.desc(),
            )
            .limit(limit)
            .offset(offset)
        )
        return [self._event_list_item(event) for event in self.session.scalars(statement).all()]
```

- [ ] **Step 5: Run service tests**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_product_query_service.py -v
```

Expected: PASS.

- [ ] **Step 6: Commit**

```powershell
git add apps/worker/worker/services/product_query_service.py apps/worker/tests/test_product_query_service.py
git commit -m "feat(worker): add homepage recent event window"
```

## Task 3: FastAPI Regression

**Files:**
- Modify: `apps/worker/tests/test_product_api.py`

- [ ] **Step 1: Add API regression**

Add a test using event timestamps relative to `datetime.now(UTC)`:

```python
from datetime import UTC, datetime, timedelta


def test_events_api_defaults_to_recent_homepage_window():
    session_factory = make_session_factory()
    now = datetime.now(UTC)

    with session_factory() as session:
        recent = create_published_event(session, "api-recent-event")
        recent.published_at = now - timedelta(hours=1)
        old = create_published_event(session, "api-old-event")
        old.published_at = now - timedelta(days=9)
        session.commit()

    client = TestClient(create_app(session_factory=session_factory))

    events_payload = client.get("/events").json()
    slugs = [item["slug"] for item in events_payload["items"]]

    assert recent.slug in slugs
    assert old.slug not in slugs

    detail_response = client.get(f"/events/{old.slug}")
    assert detail_response.status_code == 200
    assert detail_response.json()["slug"] == old.slug
```

- [ ] **Step 2: Run API test**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_product_api.py::test_events_api_defaults_to_recent_homepage_window -v
```

Expected: PASS after Task 2.

- [ ] **Step 3: Run product API suite**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_product_api.py tests/test_product_query_service.py -v
```

Expected: PASS.

- [ ] **Step 4: Commit**

```powershell
git add apps/worker/tests/test_product_api.py
git commit -m "test(worker): cover homepage recency API behavior"
```

## Task 4: Docs and Final Verification

**Files:**
- Modify: `docs/07-验收与运行/后端P1测试记录.md`
- Modify: `docs/00-项目总览/项目状态.md`
- Modify: `docs/00-项目总览/文档索引.md`
- Modify: `docs/README.md`

- [ ] **Step 1: Record test evidence**

Append a section to `后端P1测试记录.md` with:

```markdown
## 2026-06-23 P1-10 首页时效窗口测试记录

- 策略：`GET /events` 默认返回最近 48 小时发布事件；48 小时内少于 8 条时向 7 天窗口兜底。
- 未改变：`GET /events/{slug}` 仍可读取历史事件。

| 命令 | 验证内容 | 真实结果 |
| --- | --- | --- |
| `.\.venv\Scripts\python.exe -m pytest tests/test_product_query_service.py -v` | service 48 小时窗口、7 天兜底、老详情可访问 | 执行后记录实际输出，例如 `N passed in Xs`；未执行则写明原因 |
| `.\.venv\Scripts\python.exe -m pytest tests/test_product_api.py -v` | API 默认首页窗口和详情不受影响 | 执行后记录实际输出，例如 `N passed in Xs`；未执行则写明原因 |
| `.\.venv\Scripts\python.exe -m pytest -v` | worker 全量回归 | 执行后记录实际输出，例如 `N passed in Xs`；未执行则写明原因 |
```

- [ ] **Step 2: Run final verification**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_product_query_service.py -v
.\.venv\Scripts\python.exe -m pytest tests/test_product_api.py -v
.\.venv\Scripts\python.exe -m pytest -v
git diff --check
```

Expected: all commands exit 0.

- [ ] **Step 3: Commit docs**

```powershell
git add docs/07-验收与运行/后端P1测试记录.md docs/00-项目总览/项目状态.md docs/00-项目总览/文档索引.md docs/README.md
git commit -m "docs(worker): record homepage recency window acceptance"
```
