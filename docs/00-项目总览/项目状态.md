# AI World Radar 项目状态

更新时间：2026-06-22 13:18:18 +08:00

## 1. 当前阶段

当前项目处于新版 PRD v2.0、Agent 系统设计 v1.0、技术架构与数据模型设计 v1.0 完成后的 P1-4 真实 LLM Agent 节点替换完成阶段。

当前结论：

> 产品形态保持不变，P1 仍以“事件档案生产与发布”为核心；P1-1 后端数据底座已经完成，P1-2 已完成 LangGraph 工作流、三 Agent 确定性 stub、工程 tool 适配层和新版脚本入口，P1-3 已完成 HN / GitHub 采集接入新版 `source_signals`，P1-4 已完成真实 LLM Agent 节点替换。本机 PostgreSQL `ai_world_radar` 已按新版 P1 migration 重建，并通过 `stub` pipeline smoke。2026-06-22 已补齐高热度 HN 来源 real provider publish smoke，真实 provider 输出 `status=succeeded`、`published_count=1`，reviewer 判断 `publish/low` 且明确不把社区讨论写成官方事实。当前分支为 `codex/p1-data-foundation`，worktree 为 `C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。默认模式仍为 `AGENT_MODE=stub`，真实 LLM 模式需显式使用 `AGENT_MODE=llm` 或 `--agent-mode llm`；仍不得恢复旧 `EvidenceCard / EventCluster / Brief` 主链路。

当前主线对话职责是维护项目方向、阶段推进、文档一致性和跨对话交接信息。

## 2. 项目信息

- 项目名称：AI World Radar
- 本地路径：`D:\AI World Radar`
- 本次 P1 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`
- 本次 P1 分支：`codex/p1-data-foundation`
- GitHub 仓库：`https://github.com/porter2222/ai-world-radar`
- 当前主分支：`main`
- 提交者：`porter <2037388336@qq.com>`

## 3. 已完成事项

- 已创建本地项目目录：`D:\AI World Radar`
- 已初始化 Git 仓库。
- 已创建 GitHub 远程仓库：`porter2222/ai-world-radar`
- 已完成产品宪章讨论，并落地 Markdown 与 HTML 阅读版。
- 已完成 PRD v2.0，明确 P1 从“晨报优先”调整为“事件档案生产与发布优先”。
- 已完成首页事件卡片展示字段调研，并吸收到 PRD v2.0。
- 已完成信息源策略讨论和文档落地。
- 已完成 GitHub 采集策略、AI 事件重要性与热议度、杂志出版社标准编辑流程等调研资料。
- 已完成后端 P1 第一轮工程骨架实现，并合并到 `main`。
- 已建立 `apps/worker` Python Worker 工程结构。
- 已完成 SQLAlchemy / Alembic、HN Algolia collector、原文缓存、Agent stubs、pipeline 串联、运行报告和 pytest 测试。
- 已完成后端 P1 测试记录，历史稳定测试为 `18 passed`。
- 已完成新版 Agent 系统设计 v1.0 Markdown 文档。
- 已完成新版 Agent 系统设计 v1.0 HTML 阅读版，包含左侧导航。
- 已完成新版技术架构与数据模型设计 v1.0 Markdown 文档。
- 已完成新版技术架构与数据模型设计 v1.0 HTML 阅读版，包含左侧导航。
- 已完成新版 P1 implementation plan：`P1实施路线图与第一阶段后端数据底座计划.md`。
- 已完成新版 P1 implementation plan HTML 阅读版，包含左侧导航。
- 已完成 P1-1 后端数据底座重构，分支为 `codex/p1-data-foundation`。
- 已完成 `pydantic==2.13.4` 直接依赖、Pydantic schema、新版 SQLAlchemy ORM、Alembic migration、SignalService、RunLogService、EventService。
- 已落地新版核心链路：`SourceSignal -> EventCandidate -> EventDossier -> ReviewResult -> PublishedEvent`。
- 已隔离旧 `EvidenceCard / EventCluster / ContentArtifact / QualityGateResult / Brief / BriefItem` 主链路测试，旧 HN pipeline 和旧脚本入口不再作为 P1-1 验收入口。
- 已完成 P1-1 最终验收记录，最终 worker 全量测试为 `32 passed in 1.20s`。
- 已更新文档入口、文档索引和项目状态。
- 已新增 P1-2 implementation plan：`P1-2 LangGraph工作流与三Agent最小闭环计划.md`。
- 已新增 P1-2 implementation plan HTML 阅读版，包含左侧导航。
- 已完成 P1-2 LangGraph 工作流与三 Agent 最小闭环，分支为 `codex/p1-data-foundation`。
- 已完成 `langgraph==1.2.4` 直接依赖、`EventPipelineState` workflow state、三类确定性 Agent stub、`EventPipelineTools` 工程适配层、`run_event_pipeline` LangGraph 同步工作流和新版脚本入口 `scripts/run_event_pipeline.py`。
- 已按 review 要求物理删除旧 `scripts/run_hn_pipeline.py`、旧 `worker/pipelines/hn_event_pipeline.py`、旧 `worker/pipelines` 包、旧 6 个 Agent stub、旧 `worker/legacy/README.md` 和旧 legacy guard 测试。
- 已新增 cleanup 回归测试 `tests/test_legacy_runtime_cleanup.py`，确保旧 runtime 文件不存在，且 `worker/` runtime 源码不再引用旧事件模型契约名。
- 已完成 P1-2 最终验收记录，cleanup 后 worker 全量测试最终复验为 `41 passed in 4.09s`。
- 已完成 P1-2 全新 SQLite smoke，输出 `status=succeeded`、`signals_count=1`、`candidates_count=1`、`dossiers_count=1`、`published_count=1`。
- 已新增 P1-3 implementation plan：`P1-3 HN与GitHub采集接入新版链路计划.md`。
- 已新增 P1-3 implementation plan HTML 阅读版，包含左侧导航和任务拆解。
- 已完成 P1-3 HN / GitHub 采集接入新版链路，分支为 `codex/p1-data-foundation`。
- 已新增 HN source adapter，将 `HNStory` 稳定映射为 `SourceSignalCreate`，`source_key=hn_algolia`，`source_hash=hn_algolia:<objectID>`。
- 已新增 GitHub releases collector 和 source adapter，使用 GitHub 官方 releases REST API，`source_key=github_releases`，`source_hash=github_releases:<owner>/<repo>:<release id>`。
- 已新增 `scripts/collect_source_signals.py`，只写 `sources` / `source_signals`，不运行事件生产 workflow。
- 已更新 `scripts/run_event_pipeline.py`，支持用 `--source-key` 和 `--limit` 消费已入库 source signals。
- 已完成 P1-3 最终验收记录，worker 全量测试为 `50 passed in 10.44s`。
- 已完成 P1-3 全新 SQLite live smoke，采集阶段写入 `sources=2`、`source_signals=2`，pipeline 阶段生成 `published_events=1`。
- 已新增 P1-4 implementation plan：`P1-4 真实LLM Agent节点替换计划.md`。
- 已新增 P1-4 implementation plan HTML 阅读版，包含左侧导航、任务拆解和验收标准。
- 已完成 P1-4 真实 LLM Agent 节点替换，分支为 `codex/p1-data-foundation`。
- 已新增 `LLMJsonAgent` 结构化 JSON 调用基座，支持 JSON 提取、Pydantic 校验、repair retry 和失败异常。
- 已新增 `OnDutyEditorLLMAgent`、`ResearchWriterLLMAgent`、`ReviewPublisherLLMAgent`，分别输出 `EventCandidateDraft`、`EventDossierDraft`、`ReviewResultDraft`。
- 已新增 `EventAgentSet` 和 `create_event_agents`，支持 `stub|llm` 模式；`scripts/run_event_pipeline.py` 已支持 `--agent-mode stub|llm`。
- 已完成 `agent_runs` LLM metadata 记录：`model_provider`、`model_name`、`prompt_version`、`retry_count`、`trace_json.llm_raw_text`。
- 已完成 LLM 输出持续非法时的 failed AgentRun 和 failed PipelineRun 记录。
- 已新增 `scripts/smoke_llm_event_pipeline.py`，支持 fake LLM smoke 和真实 provider smoke。
- 已完成 P1-4 最终验收记录，最新 worker 全量测试为 `69 passed in 16.63s`。
- 已完成 P1-4 fake LLM smoke，输出 `status=succeeded`、`agent_mode=llm`、`published_count=1`、`agent_runs_count=3`。
- 已执行 P1-4 OpenAI / OpenAI-compatible provider smoke：`smoke_llm_client.py --call` 输出 `provider=openai`、`model=gpt-5.5`、`pong`；完整 pipeline 已跑到三类 LLM Agent，但因内置示例来源被 reviewer 判为 `manual_review`，未自动发布。
- 已按用户要求删除并重建本机 PostgreSQL `ai_world_radar`，执行新版 Alembic revision `202606120001`，当前 DataGrip 刷新后应看到新版表：`sources`、`source_signals`、`event_candidates`、`event_candidate_signals`、`event_dossiers`、`review_results`、`published_events`、`pipeline_runs`、`agent_runs`、`admin_actions`。
- 已在重建后的本机 PostgreSQL `ai_world_radar` 上执行 `stub` pipeline smoke，输出 `status=succeeded`、`signals_count=1`、`candidates_count=1`、`dossiers_count=1`、`published_count=1`；核心表计数验证为 `sources=1`、`source_signals=1`、`pipeline_runs=1`、`agent_runs=3`、`published_events=1`。
- 已于 2026-06-16 复跑 worker 全量测试，结果为 `69 passed in 12.53s`。
- 已于 2026-06-22 将 writer/reviewer prompt 调整为热度优先、表达边界优先：热度源可支撑“社区正在讨论”，但不能支撑“官方已确认事实”。
- 已于 2026-06-22 将 `scripts/smoke_llm_event_pipeline.py` 的默认 smoke 信号改为高热度 HN 社区源：`source_key=hn_algolia`、`points=512`、`comments=186`、`hn_heat_score=88`。
- 已于 2026-06-22 复跑 worker 全量测试，结果为 `71 passed in 12.88s`。
- 已于 2026-06-22 执行高热度 HN 来源 real provider publish smoke，输出 `status=succeeded`、`published_count=1`、`agent_runs_count=3`、`failed_agent_runs_count=0`；入库 review 为 `decision=publish`、`risk_level=low`，并在 `checked_items` 中确认该来源只支撑“社区正在热议”，不代表 OpenAI 官方发布。

## 4. 当前文档

### 4.1 总览文档

- `docs\README.md`：文档入口。
- `docs\00-项目总览\项目状态.md`：当前项目阶段、关键结论和跨对话交接信息。
- `docs\00-项目总览\文档索引.md`：文档结构、阅读顺序和用途。

### 4.2 产品定义

- `docs\01-产品定义\产品宪章.md`：产品使命、目标用户、边界和原则。
- `docs\01-产品定义\产品宪章.html`：产品宪章 HTML 阅读版。
- `docs\01-产品定义\产品需求文档.md`：PRD v2.0，P1 事件档案生产与发布优先版。
- `docs\01-产品定义\产品需求文档.html`：PRD v2.0 HTML 阅读版。

### 4.3 信息源与调研资料

- `docs\02-信息源策略\信息源策略.md`：P1 冷启动信息源策略。
- `docs\02-信息源策略\信息源策略.html`：信息源策略 HTML 阅读版。
- `docs\03-调研资料\GitHub采集策略调研.md`
- `docs\03-调研资料\GitHub采集策略仓库清单.md`
- `docs\03-调研资料\GitHub爬虫精准采集策略补充调研.md`
- `docs\03-调研资料\AI事件重要性与热议度采集策略报告.md`
- `docs\03-调研资料\QFT晨报项目参考价值调研.md`
- `docs\03-调研资料\首页事件卡片展示字段调研.md`
- `docs\03-调研资料\杂志出版社标准编辑流程调研.md`

### 4.4 系统设计

- `docs\04-系统设计\Agent系统设计.md`：新版 Agent 系统设计 v1.0，定义编辑部式 Multi-Agent 终局蓝图、P1 落地边界、Agent/Skill/Tool 分层、LangGraph 工作流和新数据模型方向。
- `docs\04-系统设计\Agent系统设计.html`：新版 Agent 系统设计 v1.0 HTML 阅读版。
- `docs\04-系统设计\技术架构与数据模型设计.md`：新版技术架构与数据模型设计 v1.0，定义 P1 `Next.js + Python Worker + PostgreSQL` 单机架构、Pydantic/SQLAlchemy 分层、新核心表、运行链路和阶段化工程路线。
- `docs\04-系统设计\技术架构与数据模型设计.html`：新版技术架构与数据模型设计 v1.0 HTML 阅读版。

### 4.5 实现计划、任务书与运行

- `docs\05-实现计划\P1实施路线图与第一阶段后端数据底座计划.md`：新版 P1 implementation plan，定义 P1 总路线图与第一阶段后端数据底座重构任务。
- `docs\05-实现计划\P1实施路线图与第一阶段后端数据底座计划.html`：新版 P1 implementation plan HTML 阅读版。
- `docs\05-实现计划\P1-2 LangGraph工作流与三Agent最小闭环计划.md`：P1-2 implementation plan，定义 LangGraph 工作流、三 Agent 确定性 stub、工程 tool 适配层、新版脚本入口、旧可执行代码物理清理和验收记录要求。
- `docs\05-实现计划\P1-2 LangGraph工作流与三Agent最小闭环计划.html`：P1-2 implementation plan HTML 阅读版。
- `docs\05-实现计划\P1-3 HN与GitHub采集接入新版链路计划.md`：P1-3 implementation plan，定义 HN / GitHub 真实采集如何写入新版 `source_signals`，以及如何复用事件生产 workflow。
- `docs\05-实现计划\P1-3 HN与GitHub采集接入新版链路计划.html`：P1-3 implementation plan HTML 阅读版。
- `docs\05-实现计划\P1-4 真实LLM Agent节点替换计划.md`：P1-4 implementation plan 与完成记录，定义真实 LLM Agent 节点替换的任务顺序、测试策略、验收标准和最终输出。
- `docs\05-实现计划\P1-4 真实LLM Agent节点替换计划.html`：P1-4 implementation plan HTML 阅读版，已同步最终验收摘要。
- `docs\05-实现计划\后端P1实现计划与功能切片拆解.md`：旧版后端 P1 第一切片实现计划，保留为历史参考。
- `docs\06-代理任务书\后端P1开发代理任务书.md`：旧版后端 P1 工程骨架代理任务书，已执行完成，可作为历史参考。
- `docs\07-验收与运行\后端P1测试记录.md`：后端 P1 第一轮工程骨架、P1-1 数据底座、P1-2 LangGraph 最小闭环、P1-3 HN / GitHub 采集接入、P1-4 真实 LLM Agent 节点替换的测试记录、运行记录、失败修复和未覆盖范围。
- `docs\07-验收与运行\本地开发调试指南.md`：本地调试、临时脚本和单个函数 / Agent 节点测试指南。

## 5. 核心产品结论

AI World Radar 的一句话定位是：

> AI World Radar 是一个面向中文用户的全球 AI 圈事件雷达，它从外网和中文公开信息源中发现 AI 圈热点与重要事件，并将碎片化信息整理成中文用户能快速理解、可追踪、可修正的事件档案。

P1 不以新闻链接、文章摘要或日报为核心，而是把全球 AI 圈的碎片化信号整理成可理解、可追踪、可修正的事件档案。

晨报不取消，但后置到 P1.5，基于已发布事件生成。

## 6. 最新 Agent 架构结论

新版 Agent 系统设计 v1.0 的核心口径是：

> AI World Radar 是基于 LangGraph 的编辑部式 Multi-Agent 情报生产系统。

终局架构：

```text
LangGraph 工作流编排
+ 编辑部式主 Agent
+ 能力型子 Agent / Skill
+ 工程 Tool 白名单
+ PostgreSQL 状态与审计记录
+ 后台监控与人工修正
```

P1 实现：

```text
单机 Python Worker
+ PostgreSQL
+ 本地文件缓存
+ 同步 pipeline
+ 最小 Agent 闭环
+ 最小运行记录
```

三类主 Agent：

- 值班编辑 Agent：负责信号筛选、事件归并、排序和选题角度。
- 研究写作 Agent：负责资料整理、首页事件卡、详情正文和修订。
- 审稿发布 Agent：负责质量门禁、风险检查、修订意见和发布建议。

核心边界：

- Agent 负责语义判断和内容生产。
- 工程系统负责采集、入库、状态流转、发布、审计和兜底。
- Agent 不能直接发布，不能直接修改核心数据库状态。
- P1 不做完整真实性核验，不做复杂社媒大爬虫，不做追问助手。

## 7. 数据模型重构方向

数据库可以按新版生产线推倒重来。

P1-1 已落地的新核心生产链路为：

```text
SourceSignal
-> EventCandidate
-> EventDossier
-> ReviewResult
-> PublishedEvent
```

P1-1 已实现的新核心表：

- `sources`
- `source_signals`
- `event_candidates`
- `event_candidate_signals`
- `event_dossiers`
- `review_results`
- `published_events`
- `pipeline_runs`
- `agent_runs`
- `admin_actions`

P1 可以暂不单独实现 `tool_calls`，先放在 `agent_runs.trace_json` 中，后续再拆表。

## 8. 当前未解决问题

- 真实 provider SDK 调用已在本机 OpenAI / OpenAI-compatible 配置下通过；高热度 HN 来源 real provider publish smoke 已生成热议型 `PublishedEvent`。尚未在本机 PostgreSQL `ai_world_radar` 上复跑真实 provider 高热发布 smoke。
- 真实 provider 输出质量、成本、延迟和 token 消耗尚未评估。
- 受控 tool-calling 尚未实现。
- 前端首页、事件详情页和后台管理页尚未开发。
- 管理后台的具体交互、权限保护和 Agent 监控视图尚未设计。
- 部署方案尚未确定。
- 本机 PostgreSQL `ai_world_radar` 已完成新版 migration 和 `stub` pipeline smoke；真实 provider 高热发布 smoke 本次使用独立 SQLite smoke 库，未写入该 PostgreSQL 库。
- P1-1 到 P1-4 自动化测试尚未覆盖 `EventService` 的 `revise/manual_review/reject` 独立分支、slug 冲突分支、LangGraph 修订循环深度分支、HN / GitHub live API 限流和网络异常分支。

## 9. 下一步建议

下一阶段建议执行：

> P1-5 发布质量与工程准备：补齐 `revise/manual_review/reject` 分支覆盖、slug 冲突分支、LangGraph 修订循环深度分支，以及真实 provider 成本、延迟和 token 消耗记录；如需要让 DataGrip 直接看到真实 provider 数据，可在本机 PostgreSQL `ai_world_radar` 上复跑一次高热度 HN real provider smoke。默认 `AGENT_MODE=stub` 不变，真实 LLM 调用继续显式开启。

P1-4 已完成以下执行点：

- 保持 `SourceSignal -> EventCandidate -> EventDossier -> ReviewResult -> PublishedEvent` 主链路不变。
- 已实现 LLM JSON Agent 基座和 repair 机制。
- 已替换值班编辑 Agent、研究写作 Agent、审稿发布 Agent。
- 已增加 Agent factory、workflow 注入和 `--agent-mode stub|llm` 脚本入口。
- Provider / gateway 配置继续放在 `.env`，不要把密钥写入文档或代码。
- 每个 Agent 节点均有 fake LLM 回归测试；真实 OpenAI / OpenAI-compatible provider 已可调用，高热度来源自动发布 smoke 和表达边界验收已补齐。
- P1-4 仍不恢复旧 `EvidenceCard / EventCluster / Brief` 主链路；晨报继续后置到 P1.5。

## 10. 跨对话协作规则

后续开启新对话时，应先读取：

1. `docs\README.md`
2. `docs\00-项目总览\项目状态.md`
3. `docs\00-项目总览\文档索引.md`
4. `docs\01-产品定义\产品宪章.md`
5. `docs\01-产品定义\产品需求文档.md`
6. `docs\02-信息源策略\信息源策略.md`
7. `docs\04-系统设计\Agent系统设计.md`
8. `docs\04-系统设计\技术架构与数据模型设计.md`
9. `docs\05-实现计划\P1实施路线图与第一阶段后端数据底座计划.md`
10. `docs\05-实现计划\P1-2 LangGraph工作流与三Agent最小闭环计划.md`
11. `docs\05-实现计划\P1-3 HN与GitHub采集接入新版链路计划.md`
12. `docs\05-实现计划\P1-4 真实LLM Agent节点替换计划.md`
13. `docs\07-验收与运行\后端P1测试记录.md`
14. `git status`
15. 最近 3 次 `git log`

每个对话结束前，尽量完成：

1. 更新对应文档。
2. 更新 `docs\00-项目总览\项目状态.md`。
3. 提交 commit。
4. 如有需要，推送到远程仓库。
5. 输出交接摘要。

## 11. 文档命名规则

项目文档文件名统一使用中文。

大文档应同时提供：

- Markdown 源文档。
- HTML 阅读版。

示例：

- `docs\01-产品定义\产品宪章.md`
- `docs\01-产品定义\产品宪章.html`
- `docs\01-产品定义\产品需求文档.md`
- `docs\01-产品定义\产品需求文档.html`
- `docs\04-系统设计\Agent系统设计.md`
- `docs\04-系统设计\Agent系统设计.html`

## 12. 新对话启动模板

可以在新对话中使用以下提示：

```text
你是 AI World Radar 项目的全职开发助手。
项目路径：D:\AI World Radar

请先只读以下文件，理解当前项目状态，不要立刻写代码：
1. docs\README.md
2. docs\00-项目总览\项目状态.md
3. docs\00-项目总览\文档索引.md
4. docs\01-产品定义\产品宪章.md
5. docs\01-产品定义\产品需求文档.md
6. docs\02-信息源策略\信息源策略.md
7. docs\04-系统设计\Agent系统设计.md
8. docs\04-系统设计\技术架构与数据模型设计.md
9. docs\05-实现计划\P1实施路线图与第一阶段后端数据底座计划.md
10. docs\05-实现计划\P1-2 LangGraph工作流与三Agent最小闭环计划.md
11. docs\05-实现计划\P1-3 HN与GitHub采集接入新版链路计划.md
12. docs\05-实现计划\P1-4 真实LLM Agent节点替换计划.md
13. docs\07-验收与运行\后端P1测试记录.md
14. git status
15. 最近 3 次 git log

读完后先告诉我：
- 当前项目处于什么阶段
- 已经完成了什么
- 本次对话最适合继续做什么
- 你建议的下一步

本次目标建议是：在 P1-1 后端数据底座、P1-2 LangGraph 最小闭环、P1-3 HN / GitHub 采集接入、P1-4 真实 LLM Agent 节点替换、本机 OpenAI / OpenAI-compatible SDK smoke、以及 2026-06-22 高热度 HN 来源 real provider publish smoke 均已通过的基础上，进入 P1-5 发布质量与工程准备。优先补 `revise/manual_review/reject` 分支覆盖、成本延迟审计和 PostgreSQL 真实 provider smoke 复验；不要恢复旧 `EvidenceCard / EventCluster / Brief` 主链路；应继续沿用新版 `SourceSignal -> EventCandidate -> EventDossier -> ReviewResult -> PublishedEvent`。
```
