# P1-8 官方与开发者平台公开源扩展计划 Implementation Plan

> **For agentic workers:** 本计划必须按 TDD 执行。每个代码 task 先写失败测试，再实现，再运行测试通过，最后更新文档并单独提交。

**Goal:** 在 P1-7 `official_feeds` 基础上扩展更多低成本公开平台源，让 OpenAI、GitHub、Hugging Face、Google、AWS、PyTorch、Ollama 等平台的公开 feed 能稳定进入 `source_signals`。

**Architecture:** 本阶段继续沿用新版主链路 `SourceSignal -> EventCandidate -> EventDossier -> ReviewResult -> PublishedEvent`。新增或调整平台源只负责采集和映射，不直接调用 Agent、不直接发布、不新增数据库表。

**Tech Stack:** Python 3.13, httpx 0.28.1, BeautifulSoup 4.13.4, SQLAlchemy 2.0.41, pytest 8.4.0, PostgreSQL / SQLite smoke。

---

## 1. 当前阶段

P1-7 已完成 `github_repo_trends`、`official_feeds` / `official_news`，但官方 RSS live smoke 只真实覆盖了 `nvidia_news`。用户已经确认继续扩展更多平台，但当前阶段仍不做账号池、强反爬和复杂社媒大爬虫。

本阶段目标是扩展“公开、无需登录、低反爬风险”的平台源，优先选择 RSS / Atom / XML feed。HTML 列表页只保留为兜底或后续任务。

## 2. 阶段边界

### 负责

- 扩展 `OFFICIAL_SOURCE_PROFILES` 中的公开平台 feed。
- 优先把已有 HTML profile 中已发现 RSS 的源切换为 RSS。
- 为新增 profile 补测试，确保 source key、mode、entry_url 稳定。
- 使用 fixture mode 验证多 profile 组合采集仍只写 `sources` / `source_signals`。
- 运行 SQLite smoke；如网络允许，运行 PostgreSQL live source smoke。
- 更新验收记录、项目状态、文档索引、实现计划 README 和 HTML 阅读版。

### 不负责

- 不接 X / Reddit / YouTube / Facebook / TikTok。
- 不做账号池、代理池、验证码绕过、浏览器自动化反爬。
- 不做大规模网页深爬。
- 不做论文源、arXiv、Semantic Scholar。
- 不新增数据库表。
- 不改变默认 `AGENT_MODE=stub`。

## 3. 候选平台探测结果

2026-06-23 本机网络探测结果：

| profile key | URL | 结果 | 处理 |
| --- | --- | --- | --- |
| `openai_news` | `https://openai.com/news/rss.xml` | `200 text/xml` | 从 HTML 改为 RSS |
| `github_changelog` | `https://github.blog/changelog/feed/` | `200 application/rss+xml` | 从 HTML 改为 RSS |
| `huggingface_blog` | `https://huggingface.co/blog/feed.xml` | `200 application/rss+xml` | 新增 RSS |
| `google_ai_blog` | `https://blog.google/technology/ai/rss/` | `200 application/xml` | 新增 RSS |
| `aws_machine_learning_blog` | `https://aws.amazon.com/blogs/machine-learning/feed/` | `200 application/rss+xml` | 新增 RSS |
| `pytorch_blog` | `https://pytorch.org/blog/feed.xml` | `200 application/rss+xml` | 新增 RSS |
| `ollama_blog` | `https://ollama.com/blog/rss.xml` | `200 text/xml` | 新增 RSS |
| `microsoft_ai_blog` | `https://blogs.microsoft.com/ai/feed/` | `410 Gone` | 暂不接入，需另找官方 feed |
| `anthropic_news_rss` | `https://www.anthropic.com/news/rss.xml` | `404 Not Found` | 暂不切换，保留 HTML profile |
| `deepmind_blog_rss` | `https://deepmind.google/discover/blog/rss.xml` | `404 Not Found` | 暂不切换，保留 HTML profile |
| `langchain_blog_rss` | `https://blog.langchain.com/rss/` | `403 Forbidden` | 暂不接入 |
| `meta_ai_blog` | `https://ai.meta.com/blog/rss/` | 连接意外关闭 | 暂不接入 |
| `mistral_news` | `https://mistral.ai/news/rss.xml` | 连接意外关闭 | 暂不接入 |

## 4. 任务拆解

### Task 0: Plan, baseline, and candidate probing

**Files:**

- Create: `docs/05-实现计划/P1-8 官方与开发者平台公开源扩展计划.md`
- Create: `docs/05-实现计划/P1-8 官方与开发者平台公开源扩展计划.html`
- Modify: `docs/README.md`
- Modify: `docs/05-实现计划/README.md`
- Modify: `docs/00-项目总览/项目状态.md`
- Modify: `docs/00-项目总览/文档索引.md`
- Modify: `docs/07-验收与运行/后端P1测试记录.md`

- [x] **Step 1: Run baseline**

```powershell
cd "C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation\apps\worker"
.\.venv\Scripts\python.exe -m pytest -v
```

Actual:

```text
108 passed in 31.45s
```

- [x] **Step 2: Probe candidate feeds**

Actual result is recorded in section 3.

- [ ] **Step 3: Commit plan**

```powershell
git add docs/README.md docs/05-实现计划/README.md docs/05-实现计划/P1-8* docs/00-项目总览/项目状态.md docs/00-项目总览/文档索引.md docs/07-验收与运行/后端P1测试记录.md
git commit -m "docs(worker): add P1-8 public source expansion plan"
```

### Task 1: Add expanded official feed profiles

**Files:**

- Modify: `apps/worker/scripts/collect_source_signals.py`
- Modify: `apps/worker/tests/test_collect_source_signals_script.py`
- Modify: `docs/07-验收与运行/后端P1测试记录.md`
- Modify: `docs/05-实现计划/P1-8 官方与开发者平台公开源扩展计划.md`
- Modify: `docs/05-实现计划/P1-8 官方与开发者平台公开源扩展计划.html`

- [x] **Step 1: Write failing tests**

Add tests proving:

- `openai_news` and `github_changelog` use RSS URLs.
- New profiles exist: `huggingface_blog`, `google_ai_blog`, `aws_machine_learning_blog`, `pytorch_blog`, `ollama_blog`.
- All new profiles use `mode="rss"`.
- Fixture mode can collect multiple official profiles without creating `pipeline_runs` / `published_events`.

- [x] **Step 2: Run RED**

Expected:

```text
new profile assertions fail before implementation
```

Actual:

```text
tests/test_collect_source_signals_script.py: collected 9 items
2 failed, 7 passed in 12.91s
failure 1: openai_news was still mode="html"
failure 2: Unknown official profile: huggingface_blog
```

- [x] **Step 3: Implement profiles**

Add or update built-in profiles:

```text
openai_news                 mode=rss entry_url=https://openai.com/news/rss.xml
github_changelog            mode=rss entry_url=https://github.blog/changelog/feed/
huggingface_blog            mode=rss entry_url=https://huggingface.co/blog/feed.xml
google_ai_blog              mode=rss entry_url=https://blog.google/technology/ai/rss/
aws_machine_learning_blog   mode=rss entry_url=https://aws.amazon.com/blogs/machine-learning/feed/
pytorch_blog                mode=rss entry_url=https://pytorch.org/blog/feed.xml
ollama_blog                 mode=rss entry_url=https://ollama.com/blog/rss.xml
```

- [x] **Step 4: Run GREEN**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_collect_source_signals_script.py -v
```

Actual:

```text
9 passed in 13.73s
```

- [x] **Step 5: Run targeted regression**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_collect_source_signals_script.py tests/test_official_news_collector.py -v
```

Actual:

```text
12 passed in 16.09s
```

### Task 2: SQLite smoke and optional PostgreSQL live source smoke

- [x] **Step 1: Run fresh SQLite fixture smoke**

Use multiple official profiles in fixture mode and confirm only source layer is written.

Actual:

```text
collect_source_signals.py fixture smoke:
status=succeeded
source_keys=["huggingface_blog","ollama_blog","openai_news"]
sources_count=3
signals_count=3

SQLite counts:
sources=3
source_signals=3
pipeline_runs=0
published_events=0
```

- [x] **Step 2: Run live source smoke**

Run a small live official source collection against `ai_world_radar` with 2-3 newly added RSS profiles. Do not log secrets.

执行偏差：当前 worktree 未提供本机 PostgreSQL `DATABASE_URL`，为避免把数据库密码写入命令、文档或 git，本轮先使用 fresh SQLite + 真实网络 RSS 做 live source smoke，验证真实公开 feed 可以入库且仍不触发 pipeline。PostgreSQL live smoke 留到用户提供临时环境变量或本地 `.env` 后执行。

Actual:

```text
collect_source_signals.py live SQLite smoke:
status=succeeded
source_keys=["huggingface_blog","ollama_blog","openai_news"]
sources_count=3
signals_count=3

SQLite counts:
sources=3
source_signals=3
pipeline_runs=0
published_events=0

Latest titles:
huggingface_blog: PP-OCRv6 on Hugging Face: 50-Language OCR from 1.5M to 34.5M Parameters
ollama_blog: Ollama's highest performance on Apple Silicon yet with MLX
openai_news: Daybreak: Tools for securing every organization in the world
```

- [x] **Step 3: Final tests, docs and commit**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest -v
```

Actual:

```text
110 passed in 30.44s
```

Update test record, project status and plan completion state.

## 5. 验收标准

- 新增 profile 可通过 `--official-profile` 调用。
- `openai_news`、`github_changelog` 优先使用 RSS，不再依赖 HTML 列表解析。
- fixture mode 多 profile 采集仍只写 `sources` / `source_signals`。
- 不创建 `pipeline_runs`、`event_candidates`、`event_dossiers`、`review_results` 或 `published_events`。
- 测试记录写清楚真实命令、真实输出、失败修复和未覆盖范围。

## 6. 最终验收摘要

- RED：`tests/test_collect_source_signals_script.py` 先得到 `2 failed, 7 passed in 12.91s`，证明旧 profile 未覆盖 P1-8 平台源。
- GREEN：`tests/test_collect_source_signals_script.py` 通过，结果为 `9 passed in 13.73s`。
- 组合回归：`tests/test_collect_source_signals_script.py tests/test_official_news_collector.py` 通过，结果为 `12 passed in 16.09s`。
- fresh SQLite fixture smoke：`source_keys=["huggingface_blog","ollama_blog","openai_news"]`，`sources_count=3`，`signals_count=3`。
- 真实网络 + SQLite live source smoke：同三个 profile 均真实采集成功，入库 3 条来源信号。
- 最终 worker 全量回归：`110 passed in 30.44s`。
- PostgreSQL live source smoke：本轮未执行，原因是 worktree 没有 `.env` 或可脱敏读取的 `DATABASE_URL`；为避免泄露数据库密码，改用真实网络 + fresh SQLite live smoke 验证真实 feed 入库。

## 7. 风险与后续

- 官方 feed URL 可能变更，后续需要定期 source 运行观察。
- RSS 内容质量不等，后续仍需要 pipeline / Agent 做去重和筛选。
- 本阶段不解决调度、重试、告警和限流预算；这些属于后续 source operation 任务。
