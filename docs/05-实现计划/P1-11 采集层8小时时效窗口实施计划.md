# P1-11 采集层 8 小时时效窗口实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 `collect_source_signals.py --source-group daily_all` 默认只把最近 8 小时内发生或本轮探测到的信号写入 `source_signals`，并在 summary 中记录过旧、缺时间和未来时间跳过数量。

**Architecture:** 不新增数据库表，不改 FastAPI 和前端。窗口判断放在采集脚本的入库前公共层：各来源 collector 继续产出规范化对象，mapper 生成 `SourceSignalCreate` 后先经过统一窗口过滤，再调用 `SignalService.upsert_signal()`；GitHub repo trends 作为“本轮探测到的趋势”，由采集脚本传入 detected time 作为 `published_at`。

**Tech Stack:** Python 3.13, SQLAlchemy 2, Pydantic 2, pytest, PostgreSQL/SQLite smoke, current worker scripts.

---

## File Structure

- Modify: `apps/worker/scripts/collect_source_signals.py`
  - Add `--lookback-hours`, `--now`, window calculation, skip counters, source-level filtered upsert helpers.
  - Keep `--continue-on-source-error` behavior if already present in the worktree.
- Modify: `apps/worker/worker/collectors/hn_algolia.py`
  - Add hour-based API window support with injectable `now`.
- Modify: `apps/worker/worker/sources/github_trends_source.py`
  - Add `detected_at` parameter and use it as `SourceSignal.published_at`.
- Modify: `apps/worker/worker/sources/official_news_source.py`
  - Keep stable short `source_item_id` for long official entry IDs.
- Modify: `apps/worker/worker/collectors/official_news.py`
  - Keep month-only date parse returning `None`, so the unified window filter can skip missing time.
- Modify: `apps/worker/tests/test_collect_source_signals_script.py`
  - Add script/service tests for 8-hour filtering, summary counters and GitHub trends detected time.
- Modify: `apps/worker/tests/test_official_news_collector.py`
  - Keep tests for long official entry ID and month-only date parse.
- Modify: `docs/07-验收与运行/后端P1测试记录.md`
  - Record RED/GREEN commands, full regression and real PostgreSQL smoke.
- Modify: `docs/00-项目总览/项目状态.md`
  - Record P1-11 completion, test status and remaining risks.
- Modify: `docs/00-项目总览/文档索引.md`, `docs/README.md`, `docs/05-实现计划/README.md`
  - Add this implementation plan to official project docs.

## Current Dirty Worktree Guardrail

The current worktree already has uncommitted changes from live real-flow debugging. Before implementation, inspect them with:

```powershell
cd "C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation"
git status --short
git diff -- apps/worker/scripts/collect_source_signals.py apps/worker/worker/sources/official_news_source.py apps/worker/worker/collectors/official_news.py
git diff -- apps/worker/scripts/run_event_pipeline.py apps/worker/tests/test_run_event_pipeline_script.py
```

Expected current interpretation:

- Keep official source long `source_item_id` hashing. It fixes a real P1-11 source ingestion blocker.
- Keep official month-only date parsing returning `None`. P1-11 will skip missing time at the window layer.
- Keep `--continue-on-source-error` only if tests pass; it is useful for production-style collection but not the core P1-11 requirement.
- Do not stage selector batching or `--select-all-candidates` in P1-11 commits unless the user explicitly approves it. That belongs to pipeline throughput, not collection recency.

## Task 1: RED Tests for Window Filtering and Summary

**Files:**
- Modify: `apps/worker/tests/test_collect_source_signals_script.py`

- [ ] **Step 1: Add in-process collection helpers**

Append these helpers near existing `query_signals()`:

```python
def collect_in_memory(args):
    """在进程内运行采集逻辑，便于 monkeypatch 生效。
    输入：脚本参数 SimpleNamespace。
    输出：提交后的 SQLite Session、engine 和 source_keys；调用方负责关闭 session 和 dispose engine。
    """
    engine = create_engine("sqlite+pysqlite:///:memory:")
    Base.metadata.create_all(engine)
    session_factory = sessionmaker(bind=engine, autoflush=False, expire_on_commit=False)
    session = session_factory()
    source_keys = collect_selected_sources(SignalService(session), args)
    session.commit()
    return session, engine, source_keys


def build_p1_11_args(*, source, now, lookback_hours=8, **overrides):
    """构造 P1-11 采集测试参数。
    输入：source 列表、当前时间、窗口小时数和覆盖字段。
    输出：可传给 collect_selected_sources 的 SimpleNamespace。
    """
    from scripts.collect_source_signals import CollectionStats, build_collection_window

    defaults = {
        "source": source,
        "hn_limit": 5,
        "github_repo": [],
        "github_limit": 3,
        "github_token_env": "GITHUB_TOKEN",
        "github_trend_query": [],
        "github_trend_limit": 5,
        "github_trend_min_stars": 100,
        "github_trend_token_env": "GITHUB_TOKEN",
        "snapshot_bucket": "2026062408",
        "official_profile": [],
        "official_limit": 5,
        "fixture_mode": False,
        "continue_on_source_error": False,
        "source_failures": [],
        "collection_window": build_collection_window(now=now, lookback_hours=lookback_hours),
        "collection_stats": CollectionStats(),
    }
    defaults.update(overrides)
    return SimpleNamespace(**defaults)


def query_session_signals(session):
    """查询进程内采集测试写入的来源信号。
    输入：SQLAlchemy Session。
    输出：按 source_hash 排序的信号摘要列表。
    """
    rows = session.execute(
        select(SourceSignal, Source.source_key)
        .join(Source, Source.id == SourceSignal.source_id)
        .order_by(SourceSignal.source_hash)
    ).all()
    return [
        {
            "source_key": source_key,
            "source_item_id": signal.source_item_id,
            "source_hash": signal.source_hash,
            "published_at": signal.published_at,
            "metadata": signal.metadata_json,
        }
        for signal, source_key in rows
    ]
```

- [ ] **Step 2: Add RED test for GitHub release 8-hour filtering**

Append:

```python
def test_collect_source_signals_filters_github_releases_to_lookback_window(tmp_path, monkeypatch):
    """验证 GitHub Releases 只写入采集窗口内的 release。
    输入：一个 2 小时内 release 和一个 12 小时前 release，窗口为 8 小时。
    输出：只写入新 release，summary 记录 1 条 stale skip。
    """
    from datetime import UTC, datetime, timedelta
    from worker.collectors.github_releases import GitHubRelease

    now = datetime(2026, 6, 24, 8, 0, tzinfo=UTC)

    def fake_fetch_github_releases(owner, repo, limit, token):
        return [
            GitHubRelease(
                release_id="recent-release",
                owner=owner,
                repo=repo,
                tag_name="v2.0.0",
                name="Recent release",
                html_url="https://github.com/openai/openai-python/releases/tag/v2.0.0",
                published_at=now - timedelta(hours=2),
                body="Recent release body.",
                assets_count=0,
                is_prerelease=False,
                is_draft=False,
            ),
            GitHubRelease(
                release_id="old-release",
                owner=owner,
                repo=repo,
                tag_name="v1.0.0",
                name="Old release",
                html_url="https://github.com/openai/openai-python/releases/tag/v1.0.0",
                published_at=now - timedelta(hours=12),
                body="Old release body.",
                assets_count=0,
                is_prerelease=False,
                is_draft=False,
            ),
        ]

    monkeypatch.setattr("scripts.collect_source_signals.fetch_github_releases", fake_fetch_github_releases)
    args = build_p1_11_args(
        source=["github"],
        now=now,
        github_repo=["openai/openai-python"],
        github_limit=2,
    )
    session, engine, source_keys = collect_in_memory(args)
    try:
        signals = query_session_signals(session)
        assert source_keys == {"github_releases"}
        assert [signal["source_item_id"] for signal in signals] == ["openai/openai-python#recent-release"]
        assert args.collection_stats.as_dict() == {"stale": 1, "missing_published_at": 0, "future": 0}
    finally:
        session.close()
        engine.dispose()
```

- [ ] **Step 3: Add RED test for official feed stale and missing time**

Append:

```python
def test_collect_source_signals_filters_official_entries_with_stale_or_missing_time(tmp_path, monkeypatch):
    """验证官方源只写入窗口内且有发布时间的条目。
    输入：一个 1 小时内条目、一个 20 小时前条目、一个无发布时间条目。
    输出：只写入新条目，summary 分别记录 stale 和 missing_published_at。
    """
    from datetime import UTC, datetime, timedelta

    now = datetime(2026, 6, 24, 8, 0, tzinfo=UTC)

    def fake_fetch_official_news(profile, limit):
        return [
            OfficialNewsEntry(
                profile=profile,
                entry_id="recent-official",
                title="Recent official AI update",
                url="https://openai.com/news/recent-official",
                published_at=now - timedelta(hours=1),
                summary="Recent official summary.",
            ),
            OfficialNewsEntry(
                profile=profile,
                entry_id="old-official",
                title="Old official AI update",
                url="https://openai.com/news/old-official",
                published_at=now - timedelta(hours=20),
                summary="Old official summary.",
            ),
            OfficialNewsEntry(
                profile=profile,
                entry_id="missing-time-official",
                title="Missing time official AI update",
                url="https://openai.com/news/missing-time-official",
                published_at=None,
                summary="Missing time summary.",
            ),
        ]

    monkeypatch.setattr("scripts.collect_source_signals.fetch_official_news", fake_fetch_official_news)
    args = build_p1_11_args(
        source=["official_feeds"],
        now=now,
        official_profile=["openai_news"],
        official_limit=3,
    )
    session, engine, source_keys = collect_in_memory(args)
    try:
        signals = query_session_signals(session)
        assert source_keys == {"openai_news"}
        assert [signal["source_item_id"] for signal in signals] == ["recent-official"]
        assert args.collection_stats.as_dict() == {"stale": 1, "missing_published_at": 1, "future": 0}
    finally:
        session.close()
        engine.dispose()
```

- [ ] **Step 4: Add RED test for HN 8-hour filtering**

Append:

```python
def test_collect_source_signals_filters_hn_stories_to_lookback_window(tmp_path, monkeypatch):
    """验证 HN story 经过统一 8 小时窗口过滤。
    输入：一个 3 小时内 story 和一个 13 小时前 story。
    输出：只写入新 story，summary 记录 stale skip。
    """
    from datetime import UTC, datetime, timedelta
    from worker.collectors.hn_algolia import HNStory

    now = datetime(2026, 6, 24, 8, 0, tzinfo=UTC)

    def fake_fetch_hn_stories(hours, limit, queries, now):
        return [
            HNStory(
                hn_id="recent-hn",
                title="Recent HN AI story",
                original_url="https://example.com/recent-hn",
                author="tester",
                points=100,
                num_comments=20,
                created_at=now - timedelta(hours=3),
                created_at_i=int((now - timedelta(hours=3)).timestamp()),
                story_text=None,
                hn_heat_score=140,
                matched_query="OpenAI",
            ),
            HNStory(
                hn_id="old-hn",
                title="Old HN AI story",
                original_url="https://example.com/old-hn",
                author="tester",
                points=90,
                num_comments=10,
                created_at=now - timedelta(hours=13),
                created_at_i=int((now - timedelta(hours=13)).timestamp()),
                story_text=None,
                hn_heat_score=110,
                matched_query="OpenAI",
            ),
        ]

    monkeypatch.setattr("scripts.collect_source_signals.fetch_hn_stories", fake_fetch_hn_stories)
    args = build_p1_11_args(source=["hn"], now=now, hn_limit=2)
    session, engine, source_keys = collect_in_memory(args)
    try:
        signals = query_session_signals(session)
        assert source_keys == {"hn_algolia"}
        assert [signal["source_item_id"] for signal in signals] == ["recent-hn"]
        assert args.collection_stats.as_dict() == {"stale": 1, "missing_published_at": 0, "future": 0}
    finally:
        session.close()
        engine.dispose()
```

- [ ] **Step 5: Add RED test for GitHub trends detected time**

Append:

```python
def test_collect_source_signals_sets_github_trend_published_at_to_detected_time(tmp_path, monkeypatch):
    """验证 GitHub repo trend 使用本轮 detected time，而不是旧 pushed_at。
    输入：一个 pushed_at 很旧但本轮被搜索命中的 repo。
    输出：SourceSignal.published_at 等于 --now，metadata 仍保留旧 pushed_at。
    """
    from datetime import UTC, datetime
    from worker.collectors.github_repo_trends import GitHubRepositoryTrend

    now = datetime(2026, 6, 24, 8, 0, tzinfo=UTC)
    old_pushed_at = datetime(2025, 7, 9, 12, 0, tzinfo=UTC)

    def fake_fetch_github_repository_trends(query, limit, min_stars, token):
        return [
            GitHubRepositoryTrend(
                repo_id="repo-1",
                owner="example",
                repo="fast-llm",
                full_name="example/fast-llm",
                html_url="https://github.com/example/fast-llm",
                description="Fast LLM repo.",
                stargazers_count=1250,
                forks_count=80,
                open_issues_count=7,
                language="Python",
                topics=["llm"],
                is_archived=False,
                is_fork=False,
                pushed_at=old_pushed_at,
                updated_at=old_pushed_at,
                created_at=old_pushed_at,
                query=query,
            )
        ]

    monkeypatch.setattr(
        "scripts.collect_source_signals.fetch_github_repository_trends",
        fake_fetch_github_repository_trends,
    )
    args = build_p1_11_args(
        source=["github_trends"],
        now=now,
        github_trend_query=["topic:llm stars:>100"],
        github_trend_limit=1,
        snapshot_bucket="2026062408",
    )
    session, engine, source_keys = collect_in_memory(args)
    try:
        signals = query_session_signals(session)
        assert source_keys == {"github_repo_trends"}
        assert signals[0]["published_at"] == now
        assert signals[0]["metadata"]["pushed_at"] == "2025-07-09T12:00:00+00:00"
    finally:
        session.close()
        engine.dispose()
```

- [ ] **Step 6: Run RED tests**

Run:

```powershell
cd apps\worker
.\.venv\Scripts\python.exe -m pytest tests/test_collect_source_signals_script.py -v
```

Expected before implementation:

- At least the new tests fail because `CollectionStats` / `build_collection_window` do not exist, `fetch_hn_stories()` does not accept `hours`, and GitHub trends still uses `repo.pushed_at or repo.updated_at`.

## Task 2: Implement Collection Window Core

**Files:**
- Modify: `apps/worker/scripts/collect_source_signals.py`

- [ ] **Step 1: Add imports**

Change:

```python
from datetime import UTC, datetime
```

to:

```python
from dataclasses import dataclass, field
from datetime import UTC, datetime, timedelta
```

- [ ] **Step 2: Add stats dataclass and time helpers**

Add after defaults:

```python
@dataclass
class CollectionWindow:
    """采集窗口。
    输入：窗口开始、结束和允许未来偏移。
    输出：供信号入库前判断是否写入。
    """

    start: datetime
    end: datetime
    allowed_future_skew: timedelta = timedelta(minutes=5)


@dataclass
class CollectionStats:
    """采集跳过统计。
    输入：无。
    输出：记录 stale、missing_published_at、future 三类跳过数量。
    """

    stale: int = 0
    missing_published_at: int = 0
    future: int = 0

    def as_dict(self) -> dict[str, int]:
        """转换为 stdout JSON 字段。
        输入：当前统计对象。
        输出：稳定 key 顺序的 dict。
        """
        return {
            "stale": self.stale,
            "missing_published_at": self.missing_published_at,
            "future": self.future,
        }
```

Add helper functions:

```python
def parse_utc_datetime(value: str | None) -> datetime:
    """解析命令行传入的 UTC 时间。
    输入：ISO datetime 字符串；为空时使用当前 UTC。
    输出：timezone-aware UTC datetime。
    """
    if not value:
        return datetime.now(UTC)
    parsed = datetime.fromisoformat(value.replace("Z", "+00:00"))
    if parsed.tzinfo is None:
        return parsed.replace(tzinfo=UTC)
    return parsed.astimezone(UTC)


def build_collection_window(*, now: datetime, lookback_hours: int) -> CollectionWindow:
    """构造本轮采集窗口。
    输入：当前时间和窗口小时数。
    输出：CollectionWindow，start 为 now-lookback_hours，end 为 now。
    """
    return CollectionWindow(start=now - timedelta(hours=lookback_hours), end=now)


def normalize_datetime(value: datetime | None) -> datetime | None:
    """统一 datetime 时区。
    输入：可选 datetime。
    输出：timezone-aware UTC datetime；空值返回 None。
    """
    if value is None:
        return None
    if value.tzinfo is None:
        return value.replace(tzinfo=UTC)
    return value.astimezone(UTC)


def should_write_signal(payload: SourceSignalCreate, *, window: CollectionWindow, stats: CollectionStats) -> bool:
    """判断信号是否应写入 source_signals。
    输入：SourceSignalCreate、采集窗口和统计对象。
    输出：可写入返回 True；否则递增对应跳过计数并返回 False。
    """
    published_at = normalize_datetime(payload.published_at)
    if published_at is None:
        stats.missing_published_at += 1
        return False
    if published_at < window.start:
        stats.stale += 1
        return False
    if published_at > window.end + window.allowed_future_skew:
        stats.future += 1
        return False
    return True


def upsert_signal_if_in_window(
    service: SignalService,
    payload: SourceSignalCreate,
    *,
    window: CollectionWindow,
    stats: CollectionStats,
) -> bool:
    """按采集窗口有条件写入信号。
    输入：SignalService、SourceSignalCreate、窗口和统计对象。
    输出：写入返回 True；被窗口过滤返回 False。
    """
    if not should_write_signal(payload, window=window, stats=stats):
        return False
    service.upsert_signal(payload)
    return True
```

- [ ] **Step 3: Add CLI args**

In `parse_args()`, add:

```python
parser.add_argument("--lookback-hours", type=int, default=8, help="采集层默认时效窗口小时数。")
parser.add_argument("--now", default=None, help="测试专用：覆盖当前 UTC 时间，ISO 8601 格式。")
```

- [ ] **Step 4: Build window and stats in `main()`**

After `args = parse_args()`:

```python
current_time = parse_utc_datetime(args.now)
args.collection_window = build_collection_window(now=current_time, lookback_hours=args.lookback_hours)
args.collection_stats = CollectionStats()
```

- [ ] **Step 5: Add summary fields**

After `summary = build_summary(...)`:

```python
summary["window"] = {
    "lookback_hours": args.lookback_hours,
    "start": args.collection_window.start.isoformat(),
    "end": args.collection_window.end.isoformat(),
}
summary["skipped_signals"] = args.collection_stats.as_dict()
```

- [ ] **Step 6: Pass window/stats through collection functions**

Update calls:

```python
collect_hn_signals(
    session_service,
    hours=args.lookback_hours,
    limit=args.hn_limit,
    fixture_mode=args.fixture_mode,
    window=args.collection_window,
    stats=args.collection_stats,
)
```

```python
collect_github_signals(..., window=args.collection_window, stats=args.collection_stats)
collect_github_repo_trend_signals(..., detected_at=args.collection_window.end)
collect_official_feed_signals(..., window=args.collection_window, stats=args.collection_stats)
```

- [ ] **Step 7: Update each collector wrapper**

Use this pattern for HN:

```python
def collect_hn_signals(
    service: SignalService,
    *,
    hours: int,
    limit: int,
    fixture_mode: bool,
    window: CollectionWindow,
    stats: CollectionStats,
) -> None:
    """采集 HN story 并按采集窗口写入 SourceSignal。
    输入：SignalService、小时窗口、数量限制、fixture 开关、采集窗口和统计对象。
    输出：无返回值；只有窗口内 story 会写入 source_signals。
    """
    service.upsert_source(build_hn_source())
    stories = load_fixture_hn_stories(limit=limit) if fixture_mode else fetch_hn_stories(hours=hours, limit=limit, now=window.end)
    for story in stories:
        upsert_signal_if_in_window(service, hn_story_to_signal(story), window=window, stats=stats)
```

Use this pattern for GitHub Releases:

```python
for release in releases:
    upsert_signal_if_in_window(service, github_release_to_signal(release), window=window, stats=stats)
```

Use this pattern for official feeds:

```python
written_count = 0
for entry in entries:
    if upsert_signal_if_in_window(service, official_news_entry_to_signal(entry), window=window, stats=stats):
        written_count += 1
if written_count:
    mark_source_status(service, source_key=profile.source_key, status="succeeded")
    source_keys.add(profile.source_key)
```

Use this pattern for GitHub trends:

```python
github_repo_trend_to_signal(
    repo,
    snapshot_bucket=snapshot_bucket,
    previous_stargazers_count=previous_stargazers_count,
    detected_at=detected_at,
)
```

- [ ] **Step 8: Run Task 1 tests**

Run:

```powershell
cd apps\worker
.\.venv\Scripts\python.exe -m pytest tests/test_collect_source_signals_script.py -v
```

Expected after Task 2 and Task 3 together: new tests pass. At this exact point, GitHub trends and HN may still fail until Task 3 is complete.

## Task 3: Source-Specific Time Semantics

**Files:**
- Modify: `apps/worker/worker/collectors/hn_algolia.py`
- Modify: `apps/worker/worker/sources/github_trends_source.py`
- Modify: `apps/worker/worker/sources/official_news_source.py`
- Modify: `apps/worker/worker/collectors/official_news.py`
- Modify: `apps/worker/tests/test_official_news_collector.py`

- [ ] **Step 1: Update HN collector signature**

Change `fetch_hn_stories()` to:

```python
def fetch_hn_stories(
    hours: int = 8,
    limit: int = 100,
    queries: list[str] | None = None,
    now: datetime | None = None,
) -> list[HNStory]:
    """请求 HN Algolia API 并返回 P1 候选 story。
    输入：时间窗口小时数、最大数量、可选 query profile 和可选当前时间。
    输出：最近窗口内按热度排序、已去重的 HN story 列表。
    """
    queries = queries or DEFAULT_QUERY_PROFILE
    current = now or datetime.now(UTC)
    window_start = int((current - timedelta(hours=hours)).timestamp())
    ...
```

Keep request params:

```python
"numericFilters": f"created_at_i>{window_start}",
```

- [ ] **Step 2: Update GitHub trends mapper**

Change signature:

```python
def github_repo_trend_to_signal(
    repo: GitHubRepositoryTrend,
    *,
    snapshot_bucket: str,
    previous_stargazers_count: int | None = None,
    detected_at: datetime | None = None,
) -> SourceSignalCreate:
```

Inside function:

```python
trend_detected_at = detected_at or datetime.now(UTC)
```

Set:

```python
published_at=trend_detected_at,
```

Keep metadata:

```python
"detected_at": trend_detected_at.isoformat(),
"pushed_at": repo.pushed_at.isoformat() if repo.pushed_at else None,
"created_at": repo.created_at.isoformat() if repo.created_at else None,
"updated_at": repo.updated_at.isoformat() if repo.updated_at else None,
```

Add imports:

```python
from datetime import UTC, datetime
```

- [ ] **Step 3: Keep official source stable item id**

Ensure `official_news_source.py` contains:

```python
def _source_item_id(profile: OfficialSourceProfile, entry_id: str) -> str:
    """生成符合 SourceSignal 长度限制的官方源 item id。
    输入：官方源 profile 和 collector 提供的原始 entry id。
    输出：短 entry id 原样返回；超长 entry id 返回带 profile key 的稳定短 hash。
    """
    if len(entry_id) <= 128:
        return entry_id
    return f"official:{profile.source_key}:{_stable_entry_hash(entry_id)}"
```

and:

```python
source_item_id=_source_item_id(profile, entry.entry_id),
```

- [ ] **Step 4: Keep month-only dates as missing time**

Ensure `_parse_datetime()` in `official_news.py` catches invalid ISO fallback:

```python
try:
    parsed = parsedate_to_datetime(raw_value)
except (TypeError, ValueError):
    try:
        parsed = datetime.fromisoformat(raw_value.replace("Z", "+00:00"))
    except ValueError:
        return None
```

- [ ] **Step 5: Run focused tests**

Run:

```powershell
cd apps\worker
.\.venv\Scripts\python.exe -m pytest tests/test_official_news_collector.py tests/test_collect_source_signals_script.py -v
```

Expected: all official collector tests and P1-11 collection tests pass.

- [ ] **Step 6: Commit Task 1-3 implementation**

Stage only relevant files:

```powershell
git add -- apps/worker/scripts/collect_source_signals.py apps/worker/worker/collectors/hn_algolia.py apps/worker/worker/sources/github_trends_source.py apps/worker/worker/sources/official_news_source.py apps/worker/worker/collectors/official_news.py apps/worker/tests/test_collect_source_signals_script.py apps/worker/tests/test_official_news_collector.py
git diff --cached --check
git commit --author="porter <2037388336@qq.com>" -m "feat(worker): add collection recency window"
```

Do not stage `apps/worker/scripts/run_event_pipeline.py` or `apps/worker/tests/test_run_event_pipeline_script.py` in this commit unless the user approves selector batching as part of a separate task.

## Task 4: Regression and Real PostgreSQL Smoke

**Files:**
- Modify: `docs/07-验收与运行/后端P1测试记录.md`

- [ ] **Step 1: Run targeted regression**

Run:

```powershell
cd apps\worker
.\.venv\Scripts\python.exe -m pytest tests/test_collect_source_signals_script.py -v
.\.venv\Scripts\python.exe -m pytest tests/test_official_news_collector.py -v
.\.venv\Scripts\python.exe -m pytest tests/test_run_event_pipeline_script.py -v
```

Expected:

- Collection script tests pass.
- Official collector tests pass.
- Run event pipeline tests either pass, or failures are only from pre-existing selector batching changes; if failures occur, do not hide them. Record exact failures and either fix them in a separate commit or leave them unstaged if unrelated to P1-11.

- [ ] **Step 2: Run worker full regression**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest -v
```

Expected: full worker suite passes. If unrelated pre-existing dirty changes cause failure, identify the file and reason before proceeding.

- [ ] **Step 3: Run formatting check**

Run:

```powershell
git diff --check
```

Expected: exit code 0.

- [ ] **Step 4: Run real PostgreSQL collection smoke**

Use process-only env loading. Do not print or commit `.env`, API keys or full `DATABASE_URL`.

Command shape:

```powershell
cd apps\worker
$env:PYTHONUTF8='1'
.\.venv\Scripts\python.exe scripts\collect_source_signals.py `
  --source-group daily_all `
  --lookback-hours 8 `
  --continue-on-source-error `
  --hn-limit 5 `
  --github-limit 3 `
  --github-trend-limit 5 `
  --official-limit 5
```

Expected stdout summary includes:

```json
{
  "status": "succeeded",
  "window": {
    "lookback_hours": 8
  },
  "skipped_signals": {
    "stale": 0,
    "missing_published_at": 0,
    "future": 0
  }
}
```

The exact skip counts may be nonzero in real production; record the real values.

- [ ] **Step 5: Query PostgreSQL for stale writes**

Run a query using redacted connection handling:

```sql
select s.source_key, ss.source_item_id, ss.published_at, ss.metadata
from source_signals ss
join sources s on s.id = ss.source_id
where ss.collected_at >= :smoke_started_at
order by ss.collected_at desc;
```

Acceptance:

- For ordinary sources, no row has `published_at < smoke_started_at - interval '8 hours'`.
- For `github_repo_trends`, `published_at` is near smoke run time; old `metadata->>'pushed_at'` is allowed.
- If a real source fails, it appears in `failed_sources`; this is acceptable only when `--continue-on-source-error` was used and other sources still ran.

- [ ] **Step 6: Record test evidence**

Append to `docs/07-验收与运行/后端P1测试记录.md`:

```markdown
## 2026-06-24 P1-11 采集层 8 小时时效窗口测试记录

### 测试内容

- 采集脚本默认 8 小时窗口。
- GitHub Releases 过旧 release 不入库。
- HN 过旧 story 不入库。
- 官方源过旧或缺发布时间 entry 不入库。
- GitHub repo trends 使用本轮 detected time。
- summary 返回窗口和跳过统计。

### 测试数据

- SQLite fixture / monkeypatch 数据：记录具体标题、时间和跳过原因。
- PostgreSQL real smoke：记录 source group、lookback_hours、run started_at、summary 和核心查询结果。

### 命令与真实结果

| 命令 | 验证内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- |
| `.\.venv\Scripts\python.exe -m pytest tests/test_collect_source_signals_script.py -v` | 采集窗口脚本测试 | 写入真实输出，例如 `N passed in Xs` | 通过/失败 |
| `.\.venv\Scripts\python.exe -m pytest tests/test_official_news_collector.py -v` | 官方源长 ID 和日期解析 | 写入真实输出 | 通过/失败 |
| `.\.venv\Scripts\python.exe -m pytest tests/test_run_event_pipeline_script.py -v` | pipeline 回归 | 写入真实输出 | 通过/失败 |
| `.\.venv\Scripts\python.exe -m pytest -v` | worker 全量回归 | 写入真实输出 | 通过/失败 |
| `collect_source_signals.py --source-group daily_all --lookback-hours 8` | 真实 PostgreSQL 采集 | 写入真实 summary 摘要，不含 secret | 通过/失败 |

### 未覆盖范围

- 未新增调度器或队列。
- 未新增表或 migration。
- 未覆盖 X / Reddit / YouTube。
- 未统计 only-new signal count；当前 `signals_count` 仍保持现有兼容语义。
```

## Task 5: Project Status and Final Commit

**Files:**
- Modify: `docs/00-项目总览/项目状态.md`
- Modify: `docs/00-项目总览/文档索引.md`
- Modify: `docs/README.md`
- Modify: `docs/05-实现计划/README.md`

- [ ] **Step 1: Update project status**

In `docs/00-项目总览/项目状态.md`, update current stage paragraph to include:

```markdown
2026-06-24 已完成 P1-11 采集层 8 小时时效窗口：`collect_source_signals.py` 默认使用 `--lookback-hours 8`，普通来源信号必须有窗口内 `published_at` 才写入；GitHub repo trends 使用本轮 detected time 作为 `SourceSignal.published_at`，旧 repo 时间只保留在 metadata；采集 summary 返回 `window` 和 `skipped_signals`，便于验收。
```

Also record the real test outputs from Task 4.

- [ ] **Step 2: Ensure docs index lists P1-11 plan**

`docs/00-项目总览/文档索引.md` should include:

```markdown
- `docs/05-实现计划/P1-11 采集层8小时时效窗口设计.md`
- `docs/05-实现计划/P1-11 采集层8小时时效窗口实施计划.md`
```

- [ ] **Step 3: Ensure README recommended reading lists P1-11 docs**

`docs/README.md` should include both P1-11 design and implementation plan in the recommended reading sequence.

- [ ] **Step 4: Run final checks**

Run:

```powershell
$old_path_pattern = ('docs' + '[\\/]' + 'superpowers|superpowers' + '[\\/]' + '(specs|plans)')
rg -n $old_path_pattern docs --glob "!05-实现计划/P1-11 采集层8小时时效窗口实施计划.md"
git diff --check
git status --short
```

Expected:

- No old process-directory references outside this implementation plan.
- `git diff --check` exits 0.
- Only intended P1-11 code/docs changes remain unstaged or staged.

- [ ] **Step 5: Commit docs**

Stage only docs:

```powershell
git add -- docs/README.md docs/00-项目总览/文档索引.md docs/00-项目总览/项目状态.md docs/05-实现计划/README.md docs/07-验收与运行/后端P1测试记录.md
git diff --cached --check
git commit --author="porter <2037388336@qq.com>" -m "docs(worker): record collection recency acceptance"
```

## Task 6: Optional Push

Only after all tests pass and commits are clean:

```powershell
git status --short --branch
git push origin codex/p1-data-foundation
```

Expected:

- Push succeeds, or network failure is reported with exact error.
- Do not commit `.env`, API keys, database passwords, runtime DB files, cache files or local build artifacts.

## Self-Review

- Spec coverage: covers 8-hour default window, ordinary source stale/missing/future skips, GitHub trends detected time, summary counters, real PostgreSQL smoke, no schema migration and no frontend/API changes.
- Incomplete-section scan: no incomplete sections are intentionally left.
- Scope control: selector batching / `--select-all-candidates` is explicitly excluded from P1-11 commits unless separately approved.
- Type consistency: plan uses existing `SourceSignalCreate`, `SignalService`, `GitHubRepositoryTrend`, `HNStory`, `OfficialNewsEntry`, and script paths verified from current worktree.
