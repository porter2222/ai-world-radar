# 后端 P1 测试记录

更新时间：2026-06-12 19:30:45 +08:00

## 1. 测试环境

- 项目路径：`D:\AI World Radar`
- Worker 路径：`D:\AI World Radar\apps\worker`
- Python：`3.13.12`
- 虚拟环境：`apps/worker/.venv`
- 依赖文件：`apps/worker/requirements.txt`
- 默认数据库配置来源：`.env.example`

## 2. 执行命令与结果

| 测试项 | 命令 | 测试数据 | 验证内容 | 结果 |
|---|---|---|---|---|
| 依赖安装 | `python -m venv .venv; .\.venv\Scripts\python -m pip install -r requirements.txt` | `apps/worker/requirements.txt` | Worker 依赖可安装 | 通过 |
| 单元测试 | `.\.venv\Scripts\python -m pytest` | fixture HN JSON、fixture HTML、SQLite memory DB | HN 解析、热度分、去重、原文缓存、Agent stubs、DB models、pipeline 幂等、报告、脚本入口 | 通过：18 passed |
| Alembic 临时库迁移 | `DATABASE_URL=sqlite:///<temp>/ai-world-radar-alembic-test.sqlite; alembic upgrade head` | 临时 SQLite 文件 | migration 文件可执行、核心表可创建 | 通过 |
| 默认 PostgreSQL 迁移 | `.\.venv\Scripts\alembic -c alembic.ini upgrade head` | `.env.example` 默认 `postgres/postgres@localhost:5432/ai_world_radar` | 本机 PostgreSQL 连接与迁移 | 阻塞：5432 可达，但默认 `postgres` 密码认证失败 |
| 独立 PostgreSQL 迁移 | `DATABASE_URL=<本地真实 PostgreSQL 连接串>; python -m alembic upgrade head` | 本机独立库 `ai_world_radar` | 复用本机 PostgreSQL 连接信息，创建独立库并执行 P1 migration | 通过 |
| 本地 pipeline smoke | `DATABASE_URL=sqlite:///<temp>/ai-world-radar-pipeline-smoke.sqlite; alembic upgrade head; python scripts\run_hn_pipeline.py --days 7 --limit 3` | 真实 HN Algolia API + 临时 SQLite DB | 脚本入口、HN 请求、原文抓取容错、Agent stubs、写库、txt 报告 | 通过 |
| PostgreSQL pipeline smoke | `DATABASE_URL=<本地真实 PostgreSQL 连接串>; python scripts\run_hn_pipeline.py --days 7 --limit 3 --force` | 真实 HN Algolia API + 本机 PostgreSQL `ai_world_radar` | migration 后真实 PostgreSQL 入库、报告生成、核心表行数验证 | 通过 |
| 首跑 Brief 回归 smoke | `DATABASE_URL=sqlite:///<temp>/ai-world-radar-smoke-fixed.sqlite; alembic upgrade head; python scripts\run_hn_pipeline.py --days 7 --limit 1 --force` | 真实 HN Algolia API + 全新临时 SQLite DB | 生产式 `autoflush=False` 首跑 PublishedEvent 对 Brief 查询可见，Brief/BriefItem 和 PipelineRun 计数正确 | 通过 |
| PostgreSQL 首跑 Brief 回归 smoke | `DATABASE_URL=<本地 PostgreSQL 临时库连接串>; alembic upgrade head; python scripts\run_hn_pipeline.py --days 7 --limit 1 --force` | 真实 HN Algolia API + 全新 PostgreSQL DB `ai_world_radar_brief_smoke_20260609` | 在 PostgreSQL 方言下验证首跑 Brief/BriefItem 生成和 PipelineRun 计数 | 通过 |

## 3. 单元测试覆盖

| 测试文件 | 覆盖内容 |
|---|---|
| `tests/test_hn_algolia_collector.py` | HN Algolia hit 解析、固定 query profile、按热度排序 |
| `tests/test_hn_scoring.py` | `hn_heat_score = points + num_comments * 2` |
| `tests/test_dedupe.py` | 按 HN item id 和 URL 去重，并保留更高热度条目 |
| `tests/test_page_cache.py` | HTML 可读文本提取、缓存路径、正文 hash |
| `tests/test_agent_stubs.py` | Evidence / Cluster / Ranking / Detail / Brief / QualityGate stub 结构化输出 |
| `tests/test_db_models.py` | P1 核心表存在，不建立 `raw_items` / `candidate_events` |
| `tests/test_hn_event_pipeline.py` | 临时 DB 内完整 pipeline 写入、PublishedEvent 幂等、生产式 `autoflush=False` 首跑 Brief/BriefItem 回归 |
| `tests/test_pipeline_report_writer.py` | txt 运行报告内容 |
| `tests/test_run_hn_pipeline_script.py` | 本地脚本参数解析和直接运行入口 |

## 4. Pipeline Smoke 摘要

- Pipeline Run：`run_6a291558cb654b5d9c2d3246ec47fe1b`
- 状态：`success`
- Fetched stories：`3`
- Evidence cards：`3`
- Event clusters：`3`
- Published events：`3`
- Brief items：`2`
- 运行报告路径：`runtime/pipeline-reports/2026-06-09-030010-hn-event-pipeline.txt`

说明：该 smoke 使用临时 SQLite 数据库验证代码路径。

## 4.1 PostgreSQL Pipeline Smoke 摘要

- 数据库：本机 PostgreSQL 独立库 `ai_world_radar`
- Pipeline Run：`run_9b0f92eb49ae4d7cbb95d663dce4995f`
- 状态：`success`
- Fetched stories：`3`
- Evidence cards：`3`
- Event clusters：`3`
- Published events：`3`
- Brief items：`2`
- 运行报告路径：`runtime/pipeline-reports/2026-06-09-034119-hn-event-pipeline.txt`
- 入库验证：`sources=1`、`pipeline_runs=1`、`evidence_cards=3`、`event_clusters=3`、`event_cluster_cards=3`、`content_artifacts=8`、`quality_gate_results=3`、`published_events=3`、`briefs=1`、`brief_items=2`

## 4.2 首跑 Brief 回归 Smoke 摘要

- 数据库：全新临时 SQLite 数据库
- Pipeline Run：`run_536db87d8abc46b69cbe9dea609ec8d5`
- 状态：`success`
- Fetched stories：`1`
- Evidence cards：`1`
- Event clusters：`1`
- Published events：`1`
- Brief items：`1`
- 运行报告路径：`runtime/pipeline-reports/2026-06-09-061429-hn-event-pipeline.txt`
- 入库验证：`pipeline_runs=1`、`published_events=1`、`briefs=1`、`brief_items=1`
- PipelineRun 计数验证：`published_event_count=1`

## 4.3 PostgreSQL 首跑 Brief 回归 Smoke 摘要

- 数据库：全新 PostgreSQL 数据库 `ai_world_radar_brief_smoke_20260609`
- Pipeline Run：`run_bd402ed2c49641cd959474f1e1ea6ffd`
- 状态：`success`
- Fetched stories：`1`
- Evidence cards：`1`
- Event clusters：`1`
- Published events：`1`
- Brief items：`1`
- 运行报告路径：`runtime/pipeline-reports/2026-06-09-062355-hn-event-pipeline.txt`
- 入库验证：`pipeline_runs=1`、`published_events=1`、`briefs=1`、`brief_items=1`
- PipelineRun 计数验证：`published_event_count=1`

## 5. Network 测试情况

本轮没有新增独立的 `pytest -m network` 测试用例。

已执行一次真实网络 smoke：

- HN Algolia API：已请求。
- HN story `original_url`：已由 pipeline 尝试抓取并写入 runtime 缓存。

## 6. 未覆盖范围

- 未调用真实 LLM，后端第一轮按任务书使用确定性 Agent stub。
- 未做真实 Agent prompt、repair prompt、tool-calling 流程。
- 未做 FastAPI、前端页面、部署、定时任务或队列。
- 未做 X / Reddit / YouTube / Facebook 采集。

## 7. 后续验收建议

1. 使用真实 `DATABASE_URL` 重新执行 `cd apps/worker && .\.venv\Scripts\python scripts\run_hn_pipeline.py --days 7 --limit 100`，验证更大批量入库。
2. 用任务书 SQL 示例查询 `evidence_cards`、`event_clusters`、`published_events`、`briefs`、`brief_items` 和最新 `pipeline_runs`。
3. 重复运行一次 pipeline，确认 `published_events` 不因同一 `event_cluster` 重复新增。

## 2026-06-12 P1-1 后端数据底座重构测试记录

### 当前执行阶段

- 阶段：P1-1 后端数据底座与 Worker 重构。
- 当前完成 task：Task 1 依赖与导入边界。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### Task 1 代码改动

- 新增 `apps/worker/tests/test_dependency_imports.py`，验证 worker 环境可以导入并使用 Pydantic。
- 修改 `apps/worker/pyproject.toml`，将 `pydantic==2.13.4` 写入直接依赖。

### 测试数据

- Pydantic smoke model：`SmokeModel`。
- 字段：`name: str`。
- 输入值：`ai-world-radar`。
- 断言：`SmokeModel(name="ai-world-radar").name == "ai-world-radar"`。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `Test-Path .\.venv\Scripts\python.exe` | 检查 worktree 内 worker 虚拟环境是否存在 | `False` | 环境缺失，先创建虚拟环境 |
| 2 | `python -m venv .venv; .\.venv\Scripts\python.exe -m pip install --upgrade pip; .\.venv\Scripts\python.exe -m pip install -e ".[dev]"` | 创建并安装 worker 开发环境 | `Successfully installed ... ai-world-radar-worker-0.1.0 ... pydantic-2.13.4 ... pytest-8.4.0` | 通过 |
| 3 | `.\.venv\Scripts\python.exe -m pytest -v` | P1-1 重构前 worker 全量基线测试 | `21 passed in 2.66s` | 通过；旧架构测试当前全绿 |
| 4 | `.\.venv\Scripts\python.exe -m pytest tests/test_dependency_imports.py -v` | Task 1 新增依赖导入测试，修改 pyproject 前运行 | `1 passed in 0.28s` | 本机依赖已存在，未出现缺少 pydantic 的 RED |
| 5 | `.\.venv\Scripts\python.exe -m pip install -e ".[dev]"; .\.venv\Scripts\python.exe -m pytest tests/test_dependency_imports.py -v` | 显式写入 `pydantic==2.13.4` 后重新安装并验证 | `Requirement already satisfied: pydantic==2.13.4 ... 1 passed in 0.20s` | 通过 |

### 失败与偏差记录

- Task 1 计划中的预期 RED 是 `ModuleNotFoundError: No module named 'pydantic'`。
- 实际执行中，本机环境在安装现有 worker 依赖时已通过 `openai` 的依赖链安装 `pydantic-2.13.4`，所以新增测试在修改 `pyproject.toml` 前直接通过。
- 处理方式：按计划文档要求记录“本机依赖已存在”，同时仍将 `pydantic==2.13.4` 写入 `pyproject.toml`，保证其他机器可以复现 P1-1 schema 依赖。

### 未覆盖范围

- Task 1 只验证 Pydantic 依赖导入，不验证新版 schema 字段契约。
- 尚未建立新版 SQLAlchemy ORM 表结构。
- 尚未替换 Alembic migration。
- 尚未实现 SignalService、RunLogService、EventService。
- 尚未隔离旧 HN pipeline 测试。

### 是否可以进入下一 task

可以进入 Task 2：Pydantic Schema 契约。

### Task 2 代码改动

- 新增 `apps/worker/worker/schemas/common.py`，定义 Worker schema 基类、状态枚举和评分字段 helper。
- 新增 `apps/worker/worker/schemas/source.py`，定义 `SourceCreate`、`SourceSignalCreate`。
- 新增 `apps/worker/worker/schemas/event.py`，定义 `EventCandidateDraft`、`EventDossierDraft`、`ReviewResultDraft`、`PublishEventCommand`。
- 新增 `apps/worker/worker/schemas/run.py`，定义 `PipelineRunCreate`、`AgentRunRecord`。
- 新增 `apps/worker/worker/schemas/__init__.py`，导出 P1-1 schema 契约。
- 新增 `apps/worker/tests/test_schema_contracts.py`，覆盖 schema 字段、枚举和分数范围。

### Task 2 测试数据

- Source signal：`source_key=hn_algolia`、`source_item_id=123`、`source_hash=hn_algolia:123`、标题 `OpenAI releases a new model`、热度 `points=245`、`comments=88`。
- Event candidate：`candidate_key=openai-new-model`、标题 `OpenAI 新模型引发开发者讨论`、评分 `heat_score=82`、`importance_score=90`、`audience_value_score=76`、`ranking_score=85`。
- Event dossier：卡片标题、卡片摘要、详情标题、详情正文、`why_it_matters`、`follow_up_points` 和 HN source ref。
- Review result：`decision=publish`、`risk_level=low`。
- Publish command：`candidate_id=cand_1`、`dossier_id=dos_1`、`publish_mode=auto`。
- Run schema：`PipelineRunCreate(run_key=manual-20260612-001)` 和 `AgentRunRecord(agent_role=editor)`。

### Task 2 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_schema_contracts.py -v` | schema 文件实现前运行契约测试 | `ModuleNotFoundError: No module named 'worker.schemas'`，`1 error during collection` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_schema_contracts.py -v` | schema 文件实现后验证字段契约、枚举和分数范围 | `8 passed in 0.28s` | 通过 |

### Task 2 失败与修复记录

- 失败原因：`worker.schemas` 包尚不存在，测试收集阶段无法导入 `worker.schemas.event`。
- 修复方式：新增 `worker/schemas` 包，按计划拆分 `common/source/event/run` 四类 schema，并通过 `__init__.py` 统一导出。

### Task 2 未覆盖范围

- 只验证 Pydantic 输入契约，不验证 ORM 落库。
- 未验证 Alembic migration。
- 未实现 service 层。
- 未接入真实 Agent 或 LangGraph。

### Task 2 是否可以进入下一 task

可以进入 Task 3：SQLAlchemy 新模型。

### Task 3 代码改动

- 新增 `apps/worker/worker/db/base.py`，定义新版 `Base`、`TimestampMixin` 和字符串主键生成函数 `new_id`。
- 新增 `apps/worker/worker/models/core.py`，定义新版 P1-1 10 张核心表 ORM：
  - `Source`
  - `SourceSignal`
  - `EventCandidate`
  - `EventCandidateSignal`
  - `EventDossier`
  - `ReviewResult`
  - `PublishedEvent`
  - `PipelineRun`
  - `AgentRun`
  - `AdminAction`
- 新增 `apps/worker/worker/models/__init__.py`，导出新版模型。
- 重写 `apps/worker/worker/db/models.py` 为兼容 re-export：`from worker.models import *`。
- 重写 `apps/worker/tests/test_db_models.py`，断言新版核心表存在、旧主链路表不存在。
- 新增 `apps/worker/tests/test_v1_models.py`，断言 10 个新版 ORM 类的 `__tablename__`。

### Task 3 测试数据

- 新版核心表集合：`sources`、`source_signals`、`event_candidates`、`event_candidate_signals`、`event_dossiers`、`review_results`、`published_events`、`pipeline_runs`、`agent_runs`、`admin_actions`。
- 旧主链路表集合：`evidence_cards`、`event_clusters`、`event_cluster_cards`、`content_artifacts`、`quality_gate_results`、`briefs`、`brief_items`、`raw_items`。
- 测试数据库：内存 SQLite `sqlite+pysqlite:///:memory:`。

### Task 3 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_db_models.py tests/test_v1_models.py -v` | 新模型实现前运行新版 ORM 契约测试 | `ModuleNotFoundError: No module named 'worker.models'`，`2 errors during collection` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_db_models.py tests/test_v1_models.py -v` | 新模型实现后验证 metadata、建表和类表名 | `3 passed in 0.46s` | 通过 |

### Task 3 失败与修复记录

- 失败原因：旧 worker 只有 `worker.db.models`，尚未建立新版 `worker.models` 包。
- 修复方式：新增 `worker/db/base.py`、`worker/models/core.py`、`worker/models/__init__.py`，并将 `worker/db/models.py` 改为新版模型 re-export。
- 旧表处理：Task 3 已从新版 metadata 中移除旧 `EvidenceCard / EventCluster / ContentArtifact / Brief` 等表定义；旧代码如仍引用旧模型，将在 Task 7 进行隔离或删除。

### Task 3 未覆盖范围

- 未验证 Alembic migration 文件是否与新版 metadata 一致。
- 未验证 PostgreSQL 真实迁移。
- 未实现 service 层写入逻辑。
- 未修复旧 HN pipeline 对旧模型的导入，旧链路隔离属于 Task 7。

### Task 3 是否可以进入下一 task

可以进入 Task 4：Alembic migration metadata 与迁移。

### Task 4 代码改动

- 修改 `apps/worker/worker/db/migrations/env.py`，直接使用 `worker.models.Base.metadata`，并允许 pytest 直接导入 `env.target_metadata`。
- 删除旧 migration：`apps/worker/worker/db/migrations/versions/202606090001_create_p1_core_tables.py`。
- 新增 migration：`apps/worker/worker/db/migrations/versions/202606120001_create_event_dossier_core_tables.py`。
- 新增 `apps/worker/tests/test_migration_metadata.py`，验证 Alembic target metadata 指向新版 Base。
- 调整 `apps/worker/worker/models/core.py`：保留 `AgentRun.dossier_id` 字段，但不设置数据库级外键，避免与 `EventDossier.generated_by_agent_run_id` 形成循环 FK。

### Task 4 测试数据

- Alembic target metadata：`worker.db.migrations.env.target_metadata`。
- 新版 Base：`worker.models.Base.metadata`。
- 临时 SQLite 数据库：`sqlite:///<Temp>/ai-world-radar-p1-1-migration-*.sqlite`。
- 目标 revision：`202606120001`。

### Task 4 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_migration_metadata.py -v` | migration metadata 测试，env.py 未改前 | `AttributeError: module 'alembic.context' has no attribute 'config'`，`1 error during collection` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_migration_metadata.py -v; .\.venv\Scripts\python.exe -m alembic upgrade head; .\.venv\Scripts\python.exe -m alembic current` | 初版新版 migration 检查 | metadata 测试 `1 passed`；`alembic upgrade head` 失败：`No support for ALTER of constraints in SQLite dialect` | 发现循环 FK 与 SQLite ALTER 约束问题 |
| 3 | `.\.venv\Scripts\python.exe -m pytest tests/test_migration_metadata.py -v; .\.venv\Scripts\python.exe -m alembic upgrade head; .\.venv\Scripts\python.exe -m alembic current` | 去掉 `agent_runs.dossier_id` 数据库级 FK 后重跑 migration | `1 passed in 0.80s`；`Running upgrade -> 202606120001`；`202606120001 (head)` | 通过 |
| 4 | `.\.venv\Scripts\python.exe -m pytest tests/test_db_models.py tests/test_v1_models.py tests/test_migration_metadata.py -v` | 模型和 migration metadata 联合回归 | `4 passed in 0.62s` | 通过 |

### Task 4 失败与修复记录

- 第一次失败：`env.py` 在 pytest 直接导入时立即访问 `alembic.context.config`，测试环境没有该属性。
- 修复方式：使用 `config = getattr(context, "config", None)`，让 `target_metadata` 可被测试导入；只有 Alembic 正常执行时才进入 offline/online migration。
- 第二次失败：新版 migration 初版为 `agent_runs.dossier_id -> event_dossiers.id` 追加外键，SQLite 不支持 `ALTER TABLE ADD CONSTRAINT`。
- 修复方式：保留 `agent_runs.dossier_id` 字符串字段用于运行记录关联，但取消数据库级外键；`EventDossier.generated_by_agent_run_id -> agent_runs.id` 仍保留外键。这样避免循环依赖，也让 SQLite 临时库 migration 可重复验证。

### Task 4 未覆盖范围

- 本 task 使用临时 SQLite 验证 migration，没有对本地 PostgreSQL 运行新版 migration。
- 未验证 downgrade。
- 未实现 service 层写入。
- 旧 HN pipeline 导入旧模型的问题仍留到 Task 7 隔离。

### Task 4 是否可以进入下一 task

可以进入 Task 5：SignalService 与 RunLogService。

### Task 5 代码改动

- 新增 `apps/worker/worker/services/signal_service.py`。
  - `SignalService.upsert_source` 按 `source_key` 幂等写入来源。
  - `SignalService.upsert_signal` 按 `(source_id, source_hash)` 幂等写入信号。
- 新增 `apps/worker/worker/services/run_log_service.py`。
  - `RunLogService.start_pipeline_run` 创建 pipeline run。
  - `RunLogService.record_agent_run` 创建 agent run。
  - `RunLogService.finish_pipeline_run` 写入结束状态、摘要和 `ended_at`。
- 新增 `apps/worker/worker/services/__init__.py`。
- 新增 `apps/worker/tests/test_signal_service.py`。
- 新增 `apps/worker/tests/test_run_log_service.py`。

### Task 5 测试数据

- Source：`source_key=hn_algolia`、`name=Hacker News Algolia`、`source_type=community`、`fetch_method=api`。
- SourceSignal 第一次写入：`source_item_id=123`、`original_title=OpenAI model discussion`、`source_hash=hn_algolia:123`、`heat_metrics.points=245`。
- SourceSignal 第二次写入：同一 `source_hash`，标题改为 `OpenAI model discussion updated`，`heat_metrics.points=300`。
- PipelineRun：`run_key=manual-20260612-001`、`trigger_type=manual`、`source_scope={"sources": ["hn_algolia"]}`。
- AgentRun：`agent_name=值班编辑 Agent`、`agent_role=editor`、`status=succeeded`、`input_summary=1 signal`。

### Task 5 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_signal_service.py tests/test_run_log_service.py -v` | 服务实现前运行测试 | `ModuleNotFoundError: No module named 'worker.services'`，`2 errors during collection` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_signal_service.py tests/test_run_log_service.py -v` | 实现 SignalService 与 RunLogService 后验证 | `2 passed in 0.64s` | 通过 |

### Task 5 失败与修复记录

- 失败原因：`worker.services` 包尚不存在。
- 修复方式：新增 `SignalService`、`RunLogService` 和 `services/__init__.py`。
- 事务边界：所有服务方法只调用 `session.flush()`，不调用 `commit()`；事务所有权留给外层 pipeline / workflow。

### Task 5 未覆盖范围

- 未实现 EventService。
- 未验证 PostgreSQL 写入。
- 未接入真实 Agent 或 LangGraph。
- 未覆盖 Source 不存在时的异常测试；当前实现会抛出 `ValueError`。

### Task 5 是否可以进入下一 task

可以进入 Task 6：EventService 与发布幂等。

### Task 6 代码改动

- 新增 `apps/worker/worker/services/event_service.py`。
  - `create_candidate_with_signals`：按 `candidate_key` 幂等创建/更新候选事件，并关联 SourceSignal。
  - `save_dossier`：按 candidate 保存事件档案版本，`version=max+1`。
  - `save_review_result`：保存审稿结果，并按 decision 更新 dossier 状态。
  - `publish_dossier`：只允许已有 `decision=publish` 的 dossier 发布，并以 `candidate_id` 作为 P1 发布幂等边界。
- 修改 `apps/worker/worker/services/__init__.py`，导出 `EventService`。
- 新增 `apps/worker/tests/test_event_service.py`。

### Task 6 测试数据

- SourceSignal：复用 Task 5 的 `hn_algolia:123`。
- EventCandidateDraft：
  - `candidate_key=openai-new-model`
  - `title=OpenAI 新模型引发开发者讨论`
  - `category=模型与产品`
  - `heat_score=82`
  - `importance_score=90`
  - `audience_value_score=76`
  - `ranking_score=85`
- EventDossierDraft：
  - `card_title=OpenAI 新模型引发开发者讨论`
  - `detail_title=OpenAI 新模型为什么引发开发者关注`
  - `detail_body=这是一段面向中文用户的事件解释正文，说明背景、变化和可能影响。`
  - `source_refs=[{"title": "HN discussion", "url": "https://news.ycombinator.com/item?id=123"}]`
- ReviewResultDraft：`decision=publish`、`risk_level=low`。
- PublishEventCommand：`publish_mode=auto`。

### Task 6 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_event_service.py -v` | EventService 实现前运行完整事件服务流测试 | `ModuleNotFoundError: No module named 'worker.services.event_service'`，`1 error during collection` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_event_service.py -v` | 实现 EventService 后验证候选、dossier、review、publish 与幂等 | `1 passed in 0.73s` | 通过 |

### Task 6 失败与修复记录

- 失败原因：`worker.services.event_service` 尚不存在。
- 修复方式：新增 EventService，并在 `services/__init__.py` 导出。
- 幂等规则：`publish_dossier` 先按 `candidate_id` 查询已存在 PublishedEvent；同一 candidate 重复发布返回同一行。
- 状态流转：保存 dossier 后 candidate 进入 `reviewing`；publish 审稿后 dossier 进入 `approved`；发布后 candidate 进入 `published`，dossier 进入 `published_snapshot`。

### Task 6 未覆盖范围

- 未覆盖 `revise`、`manual_review`、`reject` 三种 review decision 的独立测试。
- 未覆盖 slug 冲突分支。
- 未验证 PostgreSQL 写入。
- 未接入真实 Agent 或 LangGraph。

### Task 6 是否可以进入下一 task

可以进入 Task 7：旧链路隔离与全量测试恢复。

### Task 7 代码改动

- 删除 `apps/worker/tests/test_agent_stubs.py`：旧 deterministic Agent stub 测试仍围绕 `EvidenceCard / EventCluster / Brief` 输出，不再作为新版 P1-1 契约。
- 删除 `apps/worker/tests/test_hn_event_pipeline.py`：该测试依赖旧 `Brief`、`BriefItem`、`EvidenceCard`、`EventCluster` 等 ORM 模型，已与新版核心表冲突。
- 删除 `apps/worker/tests/test_run_hn_pipeline_script.py`：该测试导入 `scripts/run_hn_pipeline.py`，而脚本继续导入旧 `HNEventPipeline` 和旧模型，不再作为 P1-1 入口验收。
- 新增 `apps/worker/tests/test_legacy_boundaries.py`：改为验证 legacy 边界说明存在，并明确旧对象和旧入口不再是 P1-1 实现契约。
- 新增 `apps/worker/worker/legacy/README.md`：记录新版链路 `SourceSignal -> EventCandidate -> EventDossier -> ReviewResult -> PublishedEvent`，并标记旧 `worker/pipelines/hn_event_pipeline.py` 与 `scripts/run_hn_pipeline.py` 不是 P1-1 entrypoints。

### Task 7 测试数据

- Legacy README 路径：`apps/worker/worker/legacy/README.md`。
- 新版链路文本：`SourceSignal -> EventCandidate -> EventDossier -> ReviewResult -> PublishedEvent`。
- 旧对象名：`EvidenceCard`、`EventCluster`、`ContentArtifact`、`QualityGateResult`、`Brief`、`BriefItem`。
- 旧入口路径：`worker/pipelines/hn_event_pipeline.py`、`scripts/run_hn_pipeline.py`。
- 保留测试数据：HN collector fixture、dedupe HN story、page cache HTML fixture、LLM client fake transport、内存 SQLite service fixture。

### Task 7 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest -v` | Task 7 修改前运行 worker 全量测试，确认旧链路失败面 | `collected 31 items / 2 errors`；`tests/test_hn_event_pipeline.py` 与 `tests/test_run_hn_pipeline_script.py` 均因 `ImportError: cannot import name 'Brief' from 'worker.db.models'` 失败 | RED 成立，失败来自旧链路导入已移除模型 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_legacy_boundaries.py -v` | 新 legacy 边界测试，创建 README 前运行 | `2 failed`；`FileNotFoundError: ... worker\legacy\README.md` | RED 成立，失败原因正确 |
| 3 | `.\.venv\Scripts\python.exe -m pytest tests/test_legacy_boundaries.py -v` | 创建 legacy README 后验证旧链路边界说明 | `2 passed in 0.05s` | 通过 |
| 4 | `.\.venv\Scripts\python.exe -m pytest -v` | 旧测试隔离后 worker 全量回归 | `32 passed in 1.07s` | 通过 |

### Task 7 失败与修复记录

- 首次全量失败原因：旧 `test_hn_event_pipeline.py` 直接导入 `Brief`、`BriefItem`、`EvidenceCard`、`EventCluster`，旧 `test_run_hn_pipeline_script.py` 通过 `scripts/run_hn_pipeline.py` 间接导入同一批旧模型；这些模型已在 Task 3 从新版 metadata 中移除。
- 修复方式：按计划要求删除或替换旧架构测试，不恢复旧表；新增 legacy 边界测试，确保旧 HN pipeline 和脚本入口被文档标记为非 P1-1 入口。
- 保留范围：`tests/test_hn_algolia_collector.py`、`tests/test_hn_scoring.py`、`tests/test_dedupe.py`、`tests/test_page_cache.py`、`tests/test_pipeline_report_writer.py`、`tests/test_llm_client.py` 均保留并在全量测试中通过。

### Task 7 未覆盖范围

- 未把旧 `worker/pipelines/hn_event_pipeline.py` 改造成新版 `SourceSignal -> PublishedEvent` pipeline；该工作属于后续 P1-2/P1-3。
- 未恢复旧 `scripts/run_hn_pipeline.py` 的可运行性；该脚本已标记为非 P1-1 入口。
- 未测试真实 PostgreSQL 写入和真实网络 smoke；Task 7 只恢复 worker 自动化测试边界。

### Task 7 是否可以进入下一 task

可以进入 Task 8：验收记录与人工可见证据。

## 2026-06-12 P1-1 最终验收汇总

### 本次执行阶段与 task

- 阶段：P1-1 后端数据底座与 Worker 重构。
- 分支：`codex/p1-data-foundation`。
- worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。
- 已完成 task：
  - Task 1：依赖与导入边界。
  - Task 2：Pydantic Schema 契约。
  - Task 3：SQLAlchemy 新模型。
  - Task 4：Alembic migration metadata 与迁移。
  - Task 5：SignalService 与 RunLogService。
  - Task 6：EventService 与发布幂等。
  - Task 7：旧链路隔离与全量测试恢复。
  - Task 8：验收记录与人工可见证据。

### 新增、删除、重写的主要文件

- 新增：`apps/worker/tests/test_dependency_imports.py`、`apps/worker/tests/test_schema_contracts.py`、`apps/worker/tests/test_v1_models.py`、`apps/worker/tests/test_migration_metadata.py`、`apps/worker/tests/test_signal_service.py`、`apps/worker/tests/test_run_log_service.py`、`apps/worker/tests/test_event_service.py`、`apps/worker/tests/test_legacy_boundaries.py`。
- 新增：`apps/worker/worker/db/base.py`、`apps/worker/worker/models/__init__.py`、`apps/worker/worker/models/core.py`、`apps/worker/worker/schemas/*`、`apps/worker/worker/services/*`、`apps/worker/worker/legacy/README.md`。
- 新增：`apps/worker/worker/db/migrations/versions/202606120001_create_event_dossier_core_tables.py`。
- 重写：`apps/worker/worker/db/models.py` 改为新版模型 re-export。
- 修改：`apps/worker/pyproject.toml` 写入 `pydantic==2.13.4`。
- 删除：旧 migration `apps/worker/worker/db/migrations/versions/202606090001_create_p1_core_tables.py`。
- 删除：旧测试 `apps/worker/tests/test_agent_stubs.py`、`apps/worker/tests/test_hn_event_pipeline.py`、`apps/worker/tests/test_run_hn_pipeline_script.py`。
- 修改文档：`docs/05-实现计划/P1实施路线图与第一阶段后端数据底座计划.md`、对应 HTML 阅读版、`docs/07-验收与运行/后端P1测试记录.md`、`docs/00-项目总览/项目状态.md`。

### 最终测试数据

- Pydantic smoke：`SmokeModel(name="ai-world-radar")`。
- SourceSignal：`source_key=hn_algolia`、`source_item_id=123`、`source_hash=hn_algolia:123`、`original_title=OpenAI model discussion`。
- EventCandidate：`candidate_key=openai-new-model`、`title=OpenAI 新模型引发开发者讨论`、`heat_score=82`、`importance_score=90`、`audience_value_score=76`、`ranking_score=85`。
- EventDossier：`card_title=OpenAI 新模型引发开发者讨论`、`detail_title=OpenAI 新模型为什么引发开发者关注`、`source_refs=[{"title": "HN discussion", "url": "https://news.ycombinator.com/item?id=123"}]`。
- ReviewResult：`decision=publish`、`risk_level=low`。
- PipelineRun：`run_key=manual-20260612-001`、`trigger_type=manual`。
- Legacy 边界：`worker/legacy/README.md` 中的新链路和旧对象名声明。

### 最终命令与真实结果

| 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- |
| `.\.venv\Scripts\python.exe -m pytest tests/test_dependency_imports.py -v` | Pydantic 依赖导入 | `1 passed in 0.16s` | 通过 |
| `.\.venv\Scripts\python.exe -m pytest tests/test_schema_contracts.py -v` | schema 字段、枚举和分数范围 | `8 passed in 0.28s` | 通过 |
| `.\.venv\Scripts\python.exe -m pytest tests/test_db_models.py tests/test_v1_models.py tests/test_migration_metadata.py -v` | ORM 表结构、旧表移除、migration metadata | `4 passed in 0.82s` | 通过 |
| `.\.venv\Scripts\python.exe -m pytest tests/test_signal_service.py tests/test_run_log_service.py tests/test_event_service.py -v` | 服务层写入、状态流转和发布幂等 | `3 passed in 0.81s` | 通过 |
| `.\.venv\Scripts\python.exe -m pytest tests/test_legacy_boundaries.py -v` | 旧链路隔离说明 | `2 passed in 0.06s` | 通过 |
| `.\.venv\Scripts\python.exe -m pytest -v` | worker 全量测试 | `32 passed in 1.20s` | 通过 |
| `git diff --check` | diff 空白和格式检查 | 无错误输出；仅有 Windows 工作区 LF/CRLF 提示 | 通过 |
| `rg <三类临时标记关键词> docs/07-验收与运行/后端P1测试记录.md` | 验收文档临时标记扫描 | 无匹配，命令退出码为 1 | 通过 |

### 本阶段失败测试与修复方式

- Task 1：预期缺少 `pydantic` 的 RED 没有出现；本机因 `openai` 依赖链已安装 `pydantic-2.13.4`，处理方式是记录偏差并仍写入直接依赖。
- Task 2：`worker.schemas` 不存在导致 `ModuleNotFoundError`；新增 `worker/schemas` 包后修复。
- Task 3：`worker.models` 不存在导致 `ModuleNotFoundError`；新增新版 ORM 包并将 `worker/db/models.py` 改为 re-export 后修复。
- Task 4：`env.py` 直接访问 `alembic.context.config` 导致测试导入失败；改为可测试导入。初版 migration 因循环 FK 在 SQLite 下触发 `No support for ALTER of constraints in SQLite dialect`；取消 `agent_runs.dossier_id` 数据库级 FK 后修复。
- Task 5：`worker.services` 不存在导致导入失败；新增 `SignalService` 和 `RunLogService` 后修复。
- Task 6：`worker.services.event_service` 不存在导致导入失败；新增 `EventService` 后修复。
- Task 7：旧 HN pipeline 测试导入已移除的 `Brief` 等旧模型；删除旧架构测试，新增 legacy 边界测试，不恢复旧表。

### 未覆盖范围

- 未接入真实 LangGraph workflow。
- 未接入真实 LLM Agent、真实 prompt、repair prompt 或 tool-calling。
- 未开发 FastAPI、Next.js 前台、后台管理 UI、Redis、队列、对象存储或向量库。
- 未把旧 HN pipeline 改造成新版采集到发布链路；真实 HN/GitHub 冷启动采集属于后续 P1-3。
- 未执行本地 PostgreSQL 新版 migration smoke；本轮自动化主要使用内存 SQLite 与临时 SQLite。
- 未覆盖 `EventService` 的 `revise`、`manual_review`、`reject` 独立分支和 slug 冲突分支。

### 是否可以进入 P1-2

可以进入 P1-2。理由：P1-1 要求的依赖、schema、ORM、migration、服务层、发布幂等、旧链路隔离和人工可读测试记录已经完成，最终 worker 全量测试为 `32 passed in 1.20s`。P1-2 应在此数据底座上引入 LangGraph 工作流和三类 Agent 最小 stub 闭环。

## 2026-06-13 P1-2 Task 1 LangGraph 依赖与导入边界测试记录

### 当前执行阶段

- 阶段：P1-2 LangGraph 工作流与三 Agent 最小闭环。
- 当前完成 task：Task 1 LangGraph 依赖与导入边界。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### Task 1 代码改动

- 修改 `apps/worker/tests/test_dependency_imports.py`，新增 `test_langgraph_is_available`，验证 worker 环境可以导入 `langgraph.graph.StateGraph` 并创建最小 `StateGraph(dict)`。
- 修改 `apps/worker/pyproject.toml`，将 `langgraph==1.2.4` 写入 worker 直接依赖。
- 修改 `apps/worker/requirements.txt`，补充 `langgraph==1.2.4` 和 P1-1 已使用但 requirements 缺失的 `pydantic==2.13.4`。
- 更新 `docs/05-实现计划/P1-2 LangGraph工作流与三Agent最小闭环计划.md` 和对应 HTML 阅读版，记录 Task 1 完成状态与真实 RED/GREEN 输出摘要。

### 测试数据

- LangGraph smoke graph：`StateGraph(dict)`。
- 断言：`graph is not None`。
- 测试环境：`apps/worker/.venv`，Python `3.13.12`，pytest `8.4.0`。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_dependency_imports.py -v` | 写入 LangGraph 导入测试后、添加依赖前运行 | `collected 2 items`；`test_pydantic_is_available PASSED`；`test_langgraph_is_available FAILED`；失败为 `ModuleNotFoundError: No module named 'langgraph'`；最终 `1 failed, 1 passed in 0.35s` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pip install -e ".[dev]"` | 安装写入 `pyproject.toml` 后的 worker 开发环境 | pip 下载并安装 `langgraph-1.2.4` 及其依赖；输出包含 `Successfully installed ... langgraph-1.2.4 ... ai-world-radar-worker-0.1.0` | 通过 |
| 3 | `.\.venv\Scripts\python.exe -m pytest tests/test_dependency_imports.py -v` | 安装依赖后重新验证导入测试 | `collected 2 items`；两个测试均 `PASSED`；最终 `2 passed in 0.64s` | GREEN 通过 |

### 失败与修复记录

- 失败原因：P1-1 worker 环境中没有 `langgraph` 直接依赖，新增导入测试后抛出 `ModuleNotFoundError: No module named 'langgraph'`。
- 修复方式：将 `langgraph==1.2.4` 写入 `apps/worker/pyproject.toml` 和 `apps/worker/requirements.txt`，重新执行 `pip install -e ".[dev]"`。
- 依赖记录补充：`requirements.txt` 同步补入 `pydantic==2.13.4`，避免使用 requirements 安装时缺少 P1-1 schema 直接依赖。

### 未覆盖范围

- Task 1 只验证 LangGraph 依赖导入，不验证 workflow state schema。
- 尚未实现三 Agent stub、工程 tool 适配层、LangGraph event pipeline 或新版脚本入口。
- 尚未做 PostgreSQL smoke。

### 是否可以进入下一 task

可以进入 Task 2：Workflow State Schema。

## 2026-06-13 P1-2 Task 2 Workflow State Schema 测试记录

### 当前执行阶段

- 阶段：P1-2 LangGraph 工作流与三 Agent 最小闭环。
- 当前完成 task：Task 2 Workflow State Schema。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### Task 2 代码改动

- 新增 `apps/worker/tests/test_workflow_state.py`，验证 workflow state 字段、修订次数上限和未知字段拒绝。
- 新增 `apps/worker/worker/schemas/workflow.py`，定义 `EventPipelineStatus` 和 `EventPipelineState`。
- 修改 `apps/worker/worker/schemas/__init__.py`，导出 `EventPipelineState` 与 `EventPipelineStatus`。
- 更新 `docs/05-实现计划/P1-2 LangGraph工作流与三Agent最小闭环计划.md` 和对应 HTML 阅读版，记录 Task 2 完成状态与真实 RED/GREEN 输出摘要。

### 测试数据

- `run_id=run_1`
- `signal_ids=["sig_1"]`
- `candidate_ids=["cand_1"]`
- `dossier_id=dos_1`
- `current_node=review_event_dossier`
- `revision_count=2`
- `status=running`
- 负向数据：`revision_count=3`，以及未知字段 `unexpected=True`。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_workflow_state.py -v` | 新增测试后、实现 schema 前运行 | `collected 0 items / 1 error`；失败为 `ModuleNotFoundError: No module named 'worker.schemas.workflow'` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_workflow_state.py -v` | 新增 `EventPipelineState` 并导出后验证 | `collected 2 items`；两个测试均 `PASSED`；最终 `2 passed in 0.10s` | GREEN 通过 |

### 失败与修复记录

- 失败原因：P1-2 workflow state schema 尚不存在，测试导入 `worker.schemas.workflow` 失败。
- 修复方式：新增 `worker/schemas/workflow.py`，使用 P1-1 的 `WorkerSchema` 基类，定义 `EventPipelineState`，并通过 `Field(ge=0, le=2)` 限制修订次数。
- 额外约束：继承 `WorkerSchema` 后继续禁止未知字段，防止 LangGraph 节点随意塞入未约定状态。

### 未覆盖范围

- Task 2 只验证 workflow state schema，不验证 LangGraph 节点执行。
- 尚未实现三 Agent stub、工程 tool 适配层、LangGraph event pipeline 或新版脚本入口。
- 尚未验证 `pipeline_runs` 计数回填。

### 是否可以进入下一 task

可以进入 Task 3：三 Agent 确定性 Stub。

## 2026-06-13 P1-2 Task 3 三 Agent 确定性 Stub 测试记录

### 当前执行阶段

- 阶段：P1-2 LangGraph 工作流与三 Agent 最小闭环。
- 当前完成 task：Task 3 三 Agent 确定性 Stub。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### Task 3 代码改动

- 新增 `apps/worker/tests/test_event_pipeline_agent_stubs.py`，验证三 Agent stub 输出 P1-1 schema 兼容 payload。
- 新增 `apps/worker/worker/agents/event_pipeline_agents.py`，定义：
  - `OnDutyEditorAgentStub`
  - `ResearchWriterAgentStub`
  - `ReviewPublisherAgentStub`
- 修改 `apps/worker/worker/agents/__init__.py`，导出 P1-2 三 Agent stub。
- 更新 `docs/05-实现计划/P1-2 LangGraph工作流与三Agent最小闭环计划.md` 和对应 HTML 阅读版，记录 Task 3 完成状态与真实 RED/GREEN 输出摘要。

### 测试数据

- 来源信号 dict：
  - `id=sig_1`
  - `source_key=demo`
  - `original_title=OpenAI releases a new developer tool`
  - `original_url=https://example.com/openai-tool`
  - `raw_summary=Developers discuss the new tool and pricing.`
  - `heat_metrics={"points": 120, "comments": 45}`
- 值班编辑输出断言：`candidate_key=demo-openai-releases-a-new-developer-tool`，`ranking_score > 0`。
- 研究写作输出断言：`source_refs[0].url=https://example.com/openai-tool`，`why_it_matters` 包含 `中文用户`。
- 审稿发布输出断言：完整 dossier 的 `decision=publish`，`risk_level=low`。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_event_pipeline_agent_stubs.py -v` | 新增测试后、实现三 Agent stub 前运行 | `collected 0 items / 1 error`；失败为 `ModuleNotFoundError: No module named 'worker.agents.event_pipeline_agents'` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_event_pipeline_agent_stubs.py -v` | 新增三 Agent stub 后验证 schema 输出 | `collected 3 items`；三个测试均 `PASSED`；最终 `3 passed in 0.10s` | GREEN 通过 |

### 失败与修复记录

- 失败原因：P1-2 三 Agent stub 模块尚不存在。
- 修复方式：新增 `event_pipeline_agents.py`，实现三个确定性 stub；所有 stub 只返回 Pydantic schema，不直接写库。
- 代码注释：新增类和函数均使用中文 docstring，说明做什么、输入什么、输出什么。

### 未覆盖范围

- Task 3 只验证 Agent stub 的结构化输出，不验证数据库写入。
- 尚未实现工程 tool 适配层、LangGraph event pipeline 或新版脚本入口。
- 尚未覆盖审稿 `revise` 和 `manual_review` 分支的独立测试。

### 是否可以进入下一 task

可以进入 Task 4：工程 Tool 适配层。

## 2026-06-13 P1-2 Task 4 工程 Tool 适配层测试记录

### 当前执行阶段

- 阶段：P1-2 LangGraph 工作流与三 Agent 最小闭环。
- 当前完成 task：Task 4 工程 Tool 适配层。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### Task 4 代码改动

- 新增 `apps/worker/tests/test_event_pipeline_tools.py`，使用 `autoflush=False` 内存 SQLite 验证 tool 层完整写入流程和 run 计数。
- 新增 `apps/worker/worker/tools/__init__.py`，导出 `EventPipelineTools`。
- 新增 `apps/worker/worker/tools/event_pipeline_tools.py`，封装 P1-1 的 `EventService`、`RunLogService` 和 P1-2 三 Agent stub。
- 更新 `docs/05-实现计划/P1-2 LangGraph工作流与三Agent最小闭环计划.md` 和对应 HTML 阅读版，记录 Task 4 完成状态与真实 RED/GREEN 输出摘要。

### 测试数据

- 数据库：内存 SQLite `sqlite+pysqlite:///:memory:`。
- Session：`sessionmaker(..., autoflush=False, expire_on_commit=False)`。
- Source：`source_key=demo`、`name=Demo`、`source_type=fixture`、`fetch_method=manual`。
- SourceSignal：
  - `source_item_id=demo-1`
  - `original_title=OpenAI releases a new developer tool`
  - `original_url=https://example.com/openai-tool`
  - `raw_summary=Developers discuss the new tool.`
  - `source_hash=demo:1`
  - `heat_metrics={"points": 120, "comments": 45}`
- PipelineRun：`run_key=manual-p1-2-tools`、`source_scope={"source": "demo"}`。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_event_pipeline_tools.py -v` | 新增测试后、实现 tool 层前运行 | `collected 0 items / 1 error`；失败为 `ModuleNotFoundError: No module named 'worker.tools'` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_event_pipeline_tools.py -v` | 新增 `EventPipelineTools` 后验证服务层写入和计数回填 | `collected 1 item`；测试 `PASSED`；最终 `1 passed in 0.63s` | GREEN 通过 |

### 失败与修复记录

- 失败原因：P1-2 工程 tool 适配层尚不存在。
- 修复方式：新增 `worker/tools` 包和 `EventPipelineTools`，通过 P1-1 服务层完成候选事件、事件档案、审稿结果和发布快照写入。
- 计数处理：tool 层在本轮 run 上回填 `signals_count / candidates_count / dossiers_count / published_count / failed_count`，并通过最终数据库查询验证。

### 未覆盖范围

- Task 4 只验证 tool 层直接调用，不验证 LangGraph 节点编排。
- 尚未实现新版脚本入口。
- 尚未覆盖同一数据库多次 run 的计数隔离回归。

### 是否可以进入下一 task

可以进入 Task 5：LangGraph Event Pipeline。

## 2026-06-13 P1-2 Task 5 LangGraph Event Pipeline 测试记录

### 当前执行阶段

- 阶段：P1-2 LangGraph 工作流与三 Agent 最小闭环。
- 当前完成 task：Task 5 LangGraph Event Pipeline。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### Task 5 代码改动

- 新增 `apps/worker/tests/test_event_pipeline_workflow.py`，验证 LangGraph 工作流首跑即可从预置 SourceSignal 生成 PublishedEvent，并记录 PipelineRun / AgentRun。
- 新增 `apps/worker/worker/workflows/__init__.py`，导出 `run_event_pipeline`。
- 新增 `apps/worker/worker/workflows/event_pipeline.py`，实现 P1-2 LangGraph 同步工作流：
  - `collect_signals`
  - `normalize_signals`
  - `editorial_triage`
  - `merge_and_rank_events`
  - `build_research_package`
  - `draft_event_dossier`
  - `review_event_dossier`
  - `revise_if_needed`
  - `publish_or_manual_review`
  - `record_run`
- 更新 `docs/05-实现计划/P1-2 LangGraph工作流与三Agent最小闭环计划.md` 和对应 HTML 阅读版，记录 Task 5 完成状态与真实 RED/GREEN 输出摘要。

### 测试数据

- 数据库：内存 SQLite `sqlite+pysqlite:///:memory:`。
- Session：`sessionmaker(..., autoflush=False, expire_on_commit=False)`。
- Source：`source_key=demo`、`name=Demo`、`source_type=fixture`、`fetch_method=manual`。
- SourceSignal：
  - `source_item_id=demo-1`
  - `original_title=OpenAI releases a new developer tool`
  - `original_url=https://example.com/openai-tool`
  - `raw_summary=Developers discuss the new tool.`
  - `source_hash=demo:1`
  - `heat_metrics={"points": 120, "comments": 45}`
- Workflow 输入：
  - `signal_ids=[signal.id]`
  - `run_key=manual-p1-2-workflow`
  - `source_scope={"source": "demo"}`

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_event_pipeline_workflow.py -v` | 新增测试后、实现 workflow 前运行 | `collected 0 items / 1 error`；失败为 `ModuleNotFoundError: No module named 'worker.workflows'` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_event_pipeline_workflow.py -v` | 新增 LangGraph workflow 后验证首跑发布和计数 | `collected 1 item`；测试 `PASSED`；最终 `1 passed in 1.14s` | GREEN 通过 |

### 失败与修复记录

- 失败原因：P1-2 workflow 包尚不存在。
- 修复方式：新增 `worker/workflows/event_pipeline.py`，通过 LangGraph `StateGraph(dict)` 串起 P1-2 节点，并使用 `EventPipelineTools` 调用 P1-1 服务层完成数据库写入。
- 可观测性：workflow 在 editor、writer、reviewer 三个语义节点分别写入 `agent_runs`，测试断言本轮有 3 条 AgentRun。
- 计数处理：`record_run` 节点通过 tool 层回填 `pipeline_runs` 计数，测试断言 `published_count` 等于最终 `published_events` 表行数。

### 未覆盖范围

- Task 5 只覆盖默认发布路径，未单独覆盖 `revise -> draft_event_dossier` 修订循环和 `manual_review` 路径。
- 尚未实现新版脚本入口。
- 尚未执行 PostgreSQL smoke。

### 是否可以进入下一 task

可以进入 Task 6：新版脚本入口与本地 Smoke。

## 2026-06-13 P1-2 Task 6 新版脚本入口与本地 Smoke 测试记录

### 当前执行阶段

- 阶段：P1-2 LangGraph 工作流与三 Agent 最小闭环。
- 当前完成 task：Task 6 新版脚本入口与本地 Smoke。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### Task 6 代码改动

- 新增 `apps/worker/tests/test_run_event_pipeline_script.py`，通过 subprocess 验证新版脚本入口可用。
- 新增 `apps/worker/scripts/run_event_pipeline.py`，支持：
  - `--database-url`
  - `--run-key`
  - `--create-schema-for-smoke`
  - `--seed-demo-signal`
- 脚本输出 JSON 摘要，包含 `status`、`run_id`、`published_event_id`、`signals_count`、`candidates_count`、`dossiers_count`、`published_count`。
- 更新 `docs/05-实现计划/P1-2 LangGraph工作流与三Agent最小闭环计划.md` 和对应 HTML 阅读版，记录 Task 6 完成状态与真实 RED/GREEN/smoke 输出摘要。

### 测试数据

- pytest 临时 SQLite：`tmp_path / "p1_2_script_smoke.sqlite"`。
- 手动 smoke SQLite：`apps/worker/scratch/p1_2_smoke.sqlite`。
- Demo source：`source_key=demo`、`name=Demo Source`、`source_type=fixture`、`fetch_method=manual`。
- Demo signal：
  - `source_item_id=demo-1`
  - `original_title=OpenAI releases a new developer tool`
  - `original_url=https://example.com/openai-tool`
  - `raw_summary=Developers discuss the new tool and pricing.`
  - `source_hash=demo:openai-developer-tool`
  - `heat_metrics={"points": 120, "comments": 45}`

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_run_event_pipeline_script.py -v` | 新增测试后、实现脚本前运行 | `collected 1 item`；测试 `FAILED`；stderr 包含 `can't open file ... scripts\run_event_pipeline.py`；最终 `1 failed in 0.35s` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_run_event_pipeline_script.py -v` | 新增脚本后验证临时 SQLite 首跑发布 | `collected 1 item`；测试 `PASSED`；最终 `1 passed in 2.28s` | GREEN 通过 |
| 3 | `.\.venv\Scripts\python.exe scripts/run_event_pipeline.py --database-url "sqlite+pysqlite:///scratch/p1_2_smoke.sqlite" --create-schema-for-smoke --seed-demo-signal --run-key "manual-p1-2-smoke"` | 人工可见本地 smoke，验证脚本 stdout JSON | `{"candidates_count": 1, "dossiers_count": 1, "published_count": 1, "published_event_id": "pub_048cfba2a56843c095a71bb7a9d4bf45", "run_id": "run_b5633712541748b9800f54c603534b66", "signals_count": 1, "status": "succeeded"}` | 通过 |

### 失败与修复记录

- 失败原因：P1-2 新版脚本入口尚不存在，subprocess 无法打开 `scripts/run_event_pipeline.py`。
- 修复方式：新增 `run_event_pipeline.py`，使用 P1-2 workflow 和 demo signal 完成本地 smoke。
- 边界说明：脚本当前只支持 `--seed-demo-signal` 的本地 smoke，不接真实 HN / GitHub 采集源；真实采集接入属于 P1-3。

### 未覆盖范围

- Task 6 未测试真实 PostgreSQL 数据库。
- 未测试无 `--seed-demo-signal` 时由真实采集源提供 signal 的路径。
- 未替换旧 `scripts/run_hn_pipeline.py`，该工作属于 Task 7。

### 是否可以进入下一 task

可以进入 Task 7：旧入口 Guard 与全量测试恢复。

## 2026-06-13 P1-2 Task 7 旧入口 Guard 与全量测试恢复记录

### 当前执行阶段

- 阶段：P1-2 LangGraph 工作流与三 Agent 最小闭环。
- 当前完成 task：Task 7 旧入口 Guard 与全量测试恢复。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### Task 7 代码改动

- 新增 `apps/worker/tests/test_legacy_entrypoints.py`，验证旧 `scripts/run_hn_pipeline.py` 不再误跑旧主链路，而是快速失败并提示改用 `scripts/run_event_pipeline.py`。
- 重写 `apps/worker/scripts/run_hn_pipeline.py` 为 fail-fast legacy guard，不再导入旧 `worker.pipelines.hn_event_pipeline`。
- 修改 `apps/worker/worker/legacy/README.md`，说明旧 HN pipeline 和旧脚本不是 P1-1/P1-2 入口，并指向新版 `scripts/run_event_pipeline.py`。
- 更新 `docs/05-实现计划/P1-2 LangGraph工作流与三Agent最小闭环计划.md` 和对应 HTML 阅读版，记录 Task 7 完成状态、失败原因和全量测试结果。

### 测试数据

- 旧入口命令：`scripts/run_hn_pipeline.py --days 7 --limit 1`。
- 期望输出关键词：`legacy`、`run_event_pipeline.py`。
- legacy README 精确边界短语：`not P1-1 entrypoints`。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_legacy_entrypoints.py -v` | 新增旧入口 guard 测试后、改脚本前运行 | `collected 1 item`；测试 `FAILED`；旧脚本 stderr 为 `ImportError: cannot import name 'Brief' from 'worker.db.models'`；没有 `legacy` 或 `run_event_pipeline.py` 提示；最终 `1 failed in 1.43s` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_legacy_boundaries.py tests/test_legacy_entrypoints.py -v` | 增加 fail-fast guard 并更新 README 后验证 legacy 边界 | `collected 3 items`；三个测试均 `PASSED`；最终 `3 passed in 0.22s` | 局部通过 |
| 3 | `.\.venv\Scripts\python.exe -m pytest -v` | 第一次 Task 7 全量测试 | `collected 42 items`；`tests/test_legacy_boundaries.py::test_legacy_readme_marks_old_contracts_as_reference_only FAILED`；最终 `1 failed, 41 passed in 4.72s` | 发现文档短语回归 |
| 4 | `.\.venv\Scripts\python.exe -m pytest -v` | 修复 legacy README 精确短语后全量回归 | `collected 42 items`；全部测试 `PASSED`；最终 `42 passed in 4.37s` | 通过 |

### 失败与修复记录

- 初始失败根因：旧 `run_hn_pipeline.py` 在脚本顶部导入旧 `HNEventPipeline`，旧 pipeline 又导入已移除的 `Brief` 等旧模型，导致用户看到的是 ImportError 而不是可理解的 P1-2 边界提示。
- 修复方式：将旧脚本改成极小 fail-fast guard，直接向 stderr 输出 `legacy entrypoint` 和 `Use scripts/run_event_pipeline.py instead.`，返回码为 2。
- 全量测试二次失败根因：更新 legacy README 时把旧测试依赖的精确短语 `not P1-1 entrypoints` 改成了 `not P1-1 or P1-2 entrypoints`。
- 二次修复方式：保留原短语 `not P1-1 entrypoints`，并另加一句说明 P1-2 也不是 active event dossier workflow entrypoints。

### 未覆盖范围

- Task 7 不恢复旧 HN pipeline 的可运行性。
- 未把旧 HN collector 接入新版 `SourceSignal -> PublishedEvent` 链路；真实采集接入属于 P1-3。
- 未测试旧脚本参数解析，因为旧脚本已不再解析旧参数。

### 是否可以进入下一 task

可以进入 Task 8：最终验收文档与阶段状态。

## 2026-06-13 P1-2 Task 8 最终验收文档与阶段状态记录

### 当前执行阶段

- 阶段：P1-2 LangGraph 工作流与三 Agent 最小闭环。
- 当前完成 task：Task 8 最终验收文档与阶段状态。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### P1-2 已完成的代码模块

- 依赖：`apps/worker/pyproject.toml`、`apps/worker/requirements.txt` 补齐 `langgraph==1.2.4` 和直接依赖。
- 测试：新增或更新 `test_dependency_imports.py`、`test_workflow_state.py`、`test_event_pipeline_agent_stubs.py`、`test_event_pipeline_tools.py`、`test_event_pipeline_workflow.py`、`test_run_event_pipeline_script.py`、`test_legacy_entrypoints.py`。
- Schema：新增 `worker/schemas/workflow.py`，导出 `EventPipelineState`。
- Agent stub：新增 `worker/agents/event_pipeline_agents.py`，包含值班编辑、研究写作、审稿发布三类确定性 stub。
- Tool 适配层：新增 `worker/tools/event_pipeline_tools.py`，通过 P1-1 服务层写入候选、档案、审稿结果和发布快照，并回填 run 计数。
- Workflow：新增 `worker/workflows/event_pipeline.py`，用 LangGraph 串起 P1-2 同步事件生产流程。
- 脚本入口：新增 `scripts/run_event_pipeline.py`；旧 `scripts/run_hn_pipeline.py` 曾在 Task 7 改为 fail-fast legacy guard，后续已在“旧可执行代码物理清理”中删除。
- 文档：更新 P1-2 计划、HTML 阅读版、测试记录、项目状态、文档索引和实现计划 README。

### 测试数据

- worker 全量 pytest 使用各测试文件内的内存 SQLite / pytest 临时 SQLite fixture；生产一致性关键场景使用 `autoflush=False`。
- 最终 smoke 使用全新本地 SQLite 文件：`apps/worker/scratch/p1_2_final_smoke.sqlite`，命令结束后已删除，不提交。
- 最终 smoke seed 的 demo source：
  - `source_key=demo`
  - `name=Demo Source`
  - `source_type=fixture`
  - `fetch_method=manual`
- 最终 smoke seed 的 demo signal：
  - `source_item_id=demo-1`
  - `original_title=OpenAI releases a new developer tool`
  - `original_url=https://example.com/openai-tool`
  - `raw_summary=Developers discuss the new tool and pricing.`
  - `source_hash=demo:openai-developer-tool`
  - `heat_metrics={"points": 120, "comments": 45}`

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest -v` | P1-2 最终 worker 全量回归 | `collected 42 items`；42 个测试均 `PASSED`；最终 `42 passed in 4.24s` | 通过 |
| 2 | `.\.venv\Scripts\python.exe scripts/run_event_pipeline.py --database-url "sqlite+pysqlite:///scratch/p1_2_final_smoke.sqlite" --create-schema-for-smoke --seed-demo-signal --run-key "manual-p1-2-final-smoke"` | 全新临时库首跑 smoke，验证新版脚本可以生成候选、档案和发布事件 | `{"candidates_count": 1, "dossiers_count": 1, "published_count": 1, "published_event_id": "pub_d93dd4d96833423d8a0fa2fbc9cbdf7b", "run_id": "run_640d994bd08b47de8853396a4388a32b", "signals_count": 1, "status": "succeeded"}` | 通过 |
| 3 | `git diff --check` | 提交前空白与冲突标记检查 | 无输出，退出码为 0 | 通过 |
| 4 | `git status --short --branch` | 清理临时 smoke DB 后检查工作树 | `## codex/p1-data-foundation`；进入文档更新前无业务代码改动 | 通过 |

### P1-2 失败与修复总览

- Task 1：新增导入测试后缺少 `langgraph`，真实失败为 `ModuleNotFoundError: No module named 'langgraph'`；修复为写入 `langgraph==1.2.4` 并安装依赖。
- Task 2：缺少 `worker.schemas.workflow`；修复为新增 `EventPipelineState` 并导出。
- Task 3：缺少 `worker.agents.event_pipeline_agents`；修复为新增三 Agent deterministic stub。
- Task 4：缺少 `worker.tools`；修复为新增 `EventPipelineTools` 和 `worker/tools` 包。
- Task 5：缺少 `worker.workflows`；修复为新增 LangGraph event pipeline。
- Task 6：缺少 `scripts/run_event_pipeline.py`；修复为新增新版脚本入口并完成 SQLite smoke。
- Task 7：旧 `run_hn_pipeline.py` 抛出旧模型 `Brief` ImportError；当时修复为 fail-fast legacy guard。随后 legacy README 精确短语回归导致全量测试 `1 failed, 41 passed`，已保留 `not P1-1 entrypoints` 并补充 P1-2 边界说明，最终全量回归通过；该过渡 guard 后续已在 cleanup 中物理删除。

### 未覆盖范围

- 未做 PostgreSQL 真实临时库 smoke；本阶段最终 smoke 使用 SQLite 验证工作流闭环。
- 未接真实 HN / GitHub 采集源；真实采集写入 `source_signals` 属于 P1-3。
- 未接真实 LLM、prompt、repair prompt 或 tool-calling；本阶段只使用确定性 Agent stub。
- 未开发 FastAPI、Next.js 前端、后台管理 UI、Redis、队列、对象存储或向量数据库。
- 未深入覆盖 `revise` 修订循环、`manual_review` 人审分支和拒稿分支；当前主回归覆盖默认发布路径。
- 未恢复旧 `EvidenceCard / EventCluster / Brief / BriefItem` 链路，旧链路继续作为 legacy 参考。

### 是否可以进入 P1-3

可以进入 P1-3。理由：P1-2 已完成 LangGraph 依赖、workflow state、三 Agent stub、工程 tool 适配层、LangGraph event pipeline、新版脚本入口和最终验收记录；Task 7 的过渡 legacy guard 后续已被物理删除。最终 worker 全量测试为 `42 passed in 4.24s`，全新 SQLite smoke 首跑返回 `status=succeeded` 且 `published_count=1`。P1-3 应聚焦真实 HN / GitHub 采集接入，把外部信号稳定写入新版 `source_signals`，不要恢复旧 `Brief` 主链路。

## 2026-06-13 P1-2 旧可执行代码物理清理补充记录

### 当前执行阶段

- 阶段：P1-2 后验清理，目标是让 review 面看到干净的新工作区。
- 当前完成 task：旧 HN pipeline / 旧 Agent stub / 旧脚本入口物理删除。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 本次删除和改写的模块

- 删除旧脚本入口：`apps/worker/scripts/run_hn_pipeline.py`。
- 删除旧 pipeline 包：`apps/worker/worker/pipelines/__init__.py`、`apps/worker/worker/pipelines/hn_event_pipeline.py`。
- 删除旧 Agent stub：
  - `brief_writer_agent.py`
  - `detail_writer_agent.py`
  - `event_cluster_agent.py`
  - `evidence_agent.py`
  - `quality_gate_agent.py`
  - `ranking_agent.py`
- 删除旧 legacy 说明和测试：`worker/legacy/README.md`、`tests/test_legacy_boundaries.py`、`tests/test_legacy_entrypoints.py`。
- 新增 `tests/test_legacy_runtime_cleanup.py`，验证旧 runtime 文件不存在，并扫描 `worker/` 源码不再引用旧事件模型契约名。
- 改写 `worker/reports/pipeline_report_writer.py` 和 `tests/test_pipeline_report_writer.py`，报告统计从旧 `EvidenceCard / EventCluster / BriefItem` 字段改为新版 `signals / candidates / dossiers / published / failed / agent_runs` 字段。
- 修改 `worker/collectors/page_cache.py` 注释，把旧 `EvidenceCard` 指向改为 `SourceSignal`。

### 测试数据

- RED/GREEN 目标测试：本地 worker 源码路径、旧文件清单、新版 fake `PipelineReport`。
- 全量 pytest：各测试文件内的内存 SQLite / pytest 临时 SQLite fixture；生产一致性关键场景继续使用 `autoflush=False`。
- cleanup smoke：全新本地 SQLite 文件 `apps/worker/scratch/p1_2_cleanup_smoke.sqlite`，命令结束后已删除，不提交。
- cleanup smoke seed 数据仍为 demo source + demo signal：
  - `source_key=demo`
  - `source_item_id=demo-1`
  - `original_title=OpenAI releases a new developer tool`
  - `source_hash=demo:openai-developer-tool`

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_legacy_runtime_cleanup.py tests/test_pipeline_report_writer.py -v` | 写入 cleanup 测试后、删除旧代码前运行 | `collected 3 items`；3 个测试均 `FAILED`；失败原因包括旧路径仍存在、worker runtime 仍命中旧契约名、`PipelineReport.__init__()` 不支持新版 `run_key` 字段 | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_legacy_runtime_cleanup.py tests/test_pipeline_report_writer.py -v` | 删除旧代码并改写报告字段后运行 | `collected 3 items`；3 个测试均 `PASSED`；最终 `3 passed in 0.15s` | GREEN 通过 |
| 3 | `.\.venv\Scripts\python.exe -m pytest -v` | cleanup 后 worker 全量回归 | `collected 41 items`；全部测试 `PASSED`；最终复验 `41 passed in 4.09s` | 通过 |
| 4 | `.\.venv\Scripts\python.exe scripts/run_event_pipeline.py --database-url "sqlite+pysqlite:///scratch/p1_2_cleanup_smoke.sqlite" --create-schema-for-smoke --seed-demo-signal --run-key "manual-p1-2-cleanup-smoke"` | cleanup 后新版脚本 smoke | `{"candidates_count": 1, "dossiers_count": 1, "published_count": 1, "published_event_id": "pub_40bd439058ae4eefae2de25313b771ab", "run_id": "run_80554841d60c4de49d1dde0083862de7", "signals_count": 1, "status": "succeeded"}` | 通过 |
| 5 | `rg -n "EvidenceCard|EventCluster|ContentArtifact|QualityGateResult|BriefItem|Brief\b|HNEventPipeline" apps\worker\worker apps\worker\scripts -g "*.py"` | 扫描 runtime 和 scripts 是否仍引用旧契约名 | 无匹配，退出码为 1 | 通过 |

### 失败与修复记录

- 初始 RED 失败 1：旧 `run_hn_pipeline.py`、旧 `worker/pipelines/hn_event_pipeline.py` 和 6 个旧 Agent stub 仍存在。
- 修复方式：直接删除旧脚本、旧 pipeline 包、旧 Agent stub、旧 legacy README 和旧 legacy 测试，不再保留 fail-fast guard 文件。
- 初始 RED 失败 2：`worker/` runtime 源码仍出现旧契约名，主要来自旧文件和 `page_cache.py` 注释。
- 修复方式：删除旧文件，并把 `page_cache.py` 注释改为 `SourceSignal` 口径。
- 初始 RED 失败 3：`PipelineReport` 仍使用旧统计字段。
- 修复方式：把报告结构改成新版事件 pipeline 统计字段，并更新测试断言。

### 未覆盖范围

- 本次 cleanup 未删除历史调研文档和旧计划文档；这些文档仍作为历史背景保留，不作为新版实现契约。
- 本次 cleanup 未删除 HN Algolia collector、page cache、LLM client 等可复用基础能力；它们不再引用旧事件模型契约。
- 本次 cleanup 未做 PostgreSQL smoke，仍使用 SQLite 临时库验证新版脚本首跑。

### 是否可以进入 P1-3

可以进入 P1-3。当前 worker 代码区已经不再保留旧 HN pipeline、旧 Agent stub 和旧 HN 脚本入口；review 时应以 `SourceSignal -> EventCandidate -> EventDossier -> ReviewResult -> PublishedEvent`、`scripts/run_event_pipeline.py` 和 `worker/workflows/event_pipeline.py` 为唯一后端主链路入口。

## 2026-06-14 P1-3 Task 1 HN SourceSignal Adapter 测试记录

### 当前执行阶段

- 阶段：P1-3 HN / GitHub 采集接入新版链路。
- 当前完成 task：Task 1 HN SourceSignal adapter。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 本次新增和修改的模块

- 新增 `apps/worker/tests/test_hn_source_signal_adapter.py`，用测试固定 HN Algolia source 配置、HNStory 到 SourceSignalCreate 的字段映射，以及无外链 HN story 的 HN item URL fallback。
- 新增 `apps/worker/worker/sources/__init__.py`，导出 source adapter 函数。
- 新增 `apps/worker/worker/sources/hn_source.py`：
  - `build_hn_source`：构造 `hn_algolia` 的 `SourceCreate`。
  - `hn_story_to_signal`：将 HNStory 映射为 `SourceSignalCreate`。
  - `_build_hn_item_url`、`_canonicalize_story_url`、`_build_raw_summary`：分别负责 HN item URL、canonical URL 和原始摘要。
- 更新 `docs/05-实现计划/P1-3 HN与GitHub采集接入新版链路计划.md` 和对应 HTML 阅读版，记录 Task 1 的真实 RED/GREEN 输出。

### 测试数据

- HNStory fixture：
  - `hn_id=1001`
  - `title=OpenAI launches a new coding agent`
  - `original_url=https://example.com/openai-coding-agent/?ref=hn#comments`
  - `author=alice`
  - `points=41`
  - `num_comments=12`
  - `created_at=2026-06-08T10:00:00+00:00`
  - `story_text=HN discussion starter`
  - `hn_heat_score=65`
  - `matched_query=OpenAI`
- fallback 场景：同一 HNStory 的 `original_url=None`，预期回退到 `https://news.ycombinator.com/item?id=1001`。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest -v` | Task 1 开始前 worker 全量基线 | `collected 41 items`；全部测试 `PASSED`；最终 `41 passed in 5.32s` | 基线通过 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_hn_source_signal_adapter.py -v` | 写入 HN adapter 测试后、实现前运行 | `collected 0 items / 1 error`；失败原因为 `ModuleNotFoundError: No module named 'worker.sources'` | RED 成立 |
| 3 | `.\.venv\Scripts\python.exe -m pytest tests/test_hn_source_signal_adapter.py -v` | 实现 `worker.sources.hn_source` 后运行 | `collected 3 items`；3 个测试均 `PASSED`；最终 `3 passed in 0.13s` | GREEN 通过 |

### 失败与修复记录

- RED 失败原因：测试导入 `worker.sources.hn_source`，但当时还不存在 `worker.sources` 包。
- 修复方式：新增 `worker/sources` 包和 HN adapter，实现 `build_hn_source` 与 `hn_story_to_signal`，并保持函数级中文 docstring。
- 计划偏差：原计划示例预期 GREEN 为 `2 passed`；实际新增了第 3 个 fallback 测试，验证无外链 HN story 保留可点击 HN item URL，因此 GREEN 为 `3 passed`。

### 未覆盖范围

- 本 task 未调用真实 HN Algolia API；真实请求仍由既有 `worker.collectors.hn_algolia.fetch_hn_stories` 覆盖，P1-3 后续脚本 task 再做采集入口。
- 本 task 未写数据库；幂等入库由后续 `collect_source_signals.py` 和 `SignalService` 集成测试覆盖。
- 本 task 未运行事件生产 workflow；从已入库 source signals 触发 workflow 属于 Task 4。
- 本 task 未做 GitHub releases；GitHub 接入属于 Task 2。

### 是否可以进入下一 task

可以进入 Task 2：GitHub releases collector and adapter。理由：HN source 配置、source_hash、canonical_url、heat_metrics、metadata 和无外链 fallback 口径已通过测试固定。

## 2026-06-14 P1-3 Task 2 GitHub Releases Collector 与 Adapter 测试记录

### 当前执行阶段

- 阶段：P1-3 HN / GitHub 采集接入新版链路。
- 当前完成 task：Task 2 GitHub releases collector and adapter。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 本次新增和修改的模块

- 新增 `apps/worker/tests/fixtures/github_releases_response.json`，提供两条 GitHub releases REST API 风格 fixture。
- 新增 `apps/worker/tests/test_github_releases_collector.py`，覆盖 release JSON 解析、发布时间倒序排序和 SourceSignalCreate 映射。
- 新增 `apps/worker/worker/collectors/github_releases.py`：
  - `GitHubRelease`：GitHub release 的规范化数据对象。
  - `parse_github_release`：将单条 release JSON 转成 `GitHubRelease`。
  - `collect_from_github_releases_payload`：将 releases payload 转成按发布时间倒序排列的 release 列表。
  - `fetch_github_releases`：通过 GitHub REST API 拉取 releases。
- 新增 `apps/worker/worker/sources/github_source.py`：
  - `build_github_releases_source`：构造 `github_releases` 的 `SourceCreate`。
  - `github_release_to_signal`：将 `GitHubRelease` 映射为 `SourceSignalCreate`。
- 更新 `apps/worker/worker/sources/__init__.py`，导出 GitHub source adapter。
- 更新 `docs/05-实现计划/P1-3 HN与GitHub采集接入新版链路计划.md` 和对应 HTML 阅读版，记录 Task 2 的真实 RED/GREEN 输出。

### 测试数据

- GitHub release fixture 1：
  - `id=2001`
  - `owner=openai`
  - `repo=openai-python`
  - `tag_name=v1.2.0`
  - `name=OpenAI Python v1.2.0`
  - `html_url=https://github.com/openai/openai-python/releases/tag/v1.2.0?utm_source=hn#assets`
  - `published_at=2026-06-10T09:00:00Z`
  - `body=Adds Responses API helper improvements and bug fixes.`
  - `assets_count=2`
  - `draft=false`
  - `prerelease=false`
- GitHub release fixture 2：
  - `id=2002`
  - `tag_name=v1.3.0b1`
  - `name=` 空字符串
  - `published_at=2026-06-11T09:00:00Z`
  - `prerelease=true`
  - `assets_count=0`

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_github_releases_collector.py -v` | 写入 GitHub releases 测试后、实现前运行 | `collected 0 items / 1 error`；失败原因为 `ModuleNotFoundError: No module named 'worker.collectors.github_releases'` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_github_releases_collector.py -v` | 实现 collector 和 source adapter 后运行 | `collected 3 items`；3 个测试均 `PASSED`；最终 `3 passed in 0.39s` | GREEN 通过 |
| 3 | `.\.venv\Scripts\python.exe -m pytest tests/test_hn_source_signal_adapter.py tests/test_github_releases_collector.py -v` | 回归 HN/GitHub source adapter 两组测试 | `collected 6 items`；6 个测试均 `PASSED`；最终 `6 passed in 0.21s` | 通过 |

### 失败与修复记录

- RED 失败原因：测试导入 `worker.collectors.github_releases`，但当时还不存在 GitHub releases collector。
- 修复方式：新增 `github_releases.py` 和 `github_source.py`，并在 `worker.sources` 包导出 GitHub source adapter。
- 设计取舍：本 task 只接 GitHub 官方 releases REST API，不实现 GitHub Trending HTML 抓取；Trending 页面解析仍后置，避免 P1-3 引入易碎 selector。

### 未覆盖范围

- 本 task 未调用真实 GitHub API；`fetch_github_releases` 的 live smoke 放到后续采集脚本和阶段 smoke。
- 本 task 未写数据库；幂等入库由后续 `collect_source_signals.py` 集成测试覆盖。
- 本 task 未运行事件生产 workflow；从已入库 source signals 触发 workflow 属于 Task 4。
- 本 task 未覆盖 GitHub API rate limit、token 权限不足、仓库不存在等 live 错误场景；后续 smoke 若遇到真实网络错误会原文记录。

### 是否可以进入下一 task

可以进入 Task 3：Collection script writes SourceSignal only。理由：HN 和 GitHub 两类来源都已经具备 `SourceCreate` 与 `SourceSignalCreate` 映射能力，下一步可以把它们接到只写 `source_signals` 的采集脚本。

## 2026-06-14 P1-3 Task 3 采集脚本仅写 SourceSignal 测试记录

### 当前执行阶段

- 阶段：P1-3 HN / GitHub 采集接入新版链路。
- 当前完成 task：Task 3 Collection script writes SourceSignal only。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 本次新增和修改的模块

- 新增 `apps/worker/scripts/collect_source_signals.py`：
  - `parse_args`：解析 `--source`、`--fixture-mode`、`--hn-limit`、`--github-repo` 等参数。
  - `main`：创建数据库连接、执行采集、提交事务并输出 JSON 摘要。
  - `collect_selected_sources`：按参数调度 HN 和 GitHub 采集。
  - `collect_hn_signals`：写入 HN source 和 source_signals。
  - `collect_github_signals`：写入 GitHub releases source 和 source_signals。
  - `load_fixture_hn_stories` / `load_fixture_github_releases`：fixture 模式读取本地测试数据。
  - `build_summary`：统计本轮写入的 source 和 signal 数。
- 新增 `apps/worker/tests/test_collect_source_signals_script.py`：
  - 验证脚本只写 `sources` 和 `source_signals`。
  - 验证脚本不会写 `pipeline_runs` 或 `published_events`。
  - 验证重复运行同一 fixture 采集命令时幂等。
- 更新 `docs/05-实现计划/P1-3 HN与GitHub采集接入新版链路计划.md` 和对应 HTML 阅读版，记录 Task 3 的真实 RED/GREEN 输出。

### 测试数据

- HN fixture：`apps/worker/tests/fixtures/hn_algolia_response.json`
  - `hn_id=1001`
  - `hn_id=1002`
  - `query=OpenAI`
- GitHub fixture：`apps/worker/tests/fixtures/github_releases_response.json`
  - `id=2001`
  - `id=2002`
  - `owner=openai`
  - `repo=openai-python`
- fixture 模式命令通过 `--source hn --hn-limit 2 --source github --github-repo openai/openai-python --github-limit 2` 组合两类来源。
- 预期数据库计数：`sources=2`、`source_signals=4`、`pipeline_runs=0`、`published_events=0`。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_collect_source_signals_script.py -v` | 写入采集脚本测试后、实现前运行 | `collected 2 items`；2 个测试均 `FAILED`；失败原因为 `can't open file 'scripts/collect_source_signals.py'` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_collect_source_signals_script.py -v` | 实现脚本后运行 | `collected 2 items`；2 个测试均 `PASSED`；最终 `2 passed in 4.00s` | GREEN 通过 |
| 3 | `.\.venv\Scripts\python.exe -m pytest tests/test_hn_source_signal_adapter.py tests/test_github_releases_collector.py tests/test_collect_source_signals_script.py -v` | HN/GitHub source adapter + 采集脚本回归 | `collected 8 items`；8 个测试均 `PASSED`；最终 `8 passed in 6.06s` | 通过 |

### 失败与修复记录

- RED 失败原因：`scripts/collect_source_signals.py` 尚未存在。
- 修复方式：新增采集脚本，并把 HN / GitHub 采集都写成先 upsert source，再 upsert source_signal 的服务层调用。
- 设计边界：脚本只负责采集并入库 `source_signals`，不创建 `pipeline_runs`，不调用 `run_event_pipeline`，避免把采集和生产耦合。

### 未覆盖范围

- 本 task 未跑 live HN / GitHub 网络请求；fixture 模式是本地 smoke 的稳定回归面。
- 本 task 未跑 PostgreSQL；脚本 smoke 使用 SQLite 临时库。
- 本 task 未运行事件生产 workflow；从已入库 source signals 触发 workflow 属于 Task 4。
- 本 task 未验证 GitHub token 权限不足、HN API 波动或 GitHub rate limit 等线上失败场景。

### 是否可以进入下一 task

可以进入 Task 4：Event pipeline consumes collected signals。理由：采集脚本已经能把 HN 和 GitHub fixture 数据稳定写入 `source_signals`，且不会触发 `pipeline_runs` / `published_events`。

## 2026-06-14 P1-3 Task 4 事件 Pipeline 消费已采集信号测试记录

### 当前执行阶段

- 阶段：P1-3 HN / GitHub 采集接入新版链路。
- 当前完成 task：Task 4 Event pipeline consumes collected signals。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 本次新增和修改的模块

- 修改 `apps/worker/tests/test_run_event_pipeline_script.py`：
  - 新增 `seed_hn_signal`，向临时 SQLite 写入 `hn_algolia` source 和一条 SourceSignal。
  - 新增 `test_run_event_pipeline_script_consumes_collected_source_signals`，验证脚本可以用 `--source-key hn_algolia --limit 1` 消费已入库信号。
- 修改 `apps/worker/scripts/run_event_pipeline.py`：
  - 新增 `--source-key` 和 `--limit` 参数。
  - 新增 `load_signal_ids_by_source_key`，按 `Source.source_key` join `SourceSignal` 并按 `created_at desc, id desc` 选择信号。
  - 保留原 `--seed-demo-signal` smoke 入口，避免破坏 P1-2 既有脚本测试。
- 更新 `docs/05-实现计划/P1-3 HN与GitHub采集接入新版链路计划.md` 和对应 HTML 阅读版，记录 Task 4 的真实 RED/GREEN 输出。

### 测试数据

- 预置 source：
  - `source_key=hn_algolia`
  - `name=Hacker News Algolia`
  - `source_type=community`
  - `fetch_method=api`
- 预置 SourceSignal：
  - `source_item_id=1001`
  - `original_title=OpenAI launches a new coding agent`
  - `original_url=https://example.com/openai-coding-agent`
  - `raw_summary=HN discussion about a new coding agent.`
  - `source_hash=hn_algolia:1001`
  - `heat_metrics={"points": 41, "comments": 12, "hn_heat_score": 65}`
- 脚本运行参数：`--source-key hn_algolia --limit 1 --run-key manual-p1-3-collected-source`。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_run_event_pipeline_script.py -v` | 新增 source-key 脚本测试后、实现前运行 | `collected 2 items`；原 demo smoke `PASSED`；新增测试 `FAILED`；失败原因为 `unrecognized arguments: --source-key hn_algolia --limit 1` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_run_event_pipeline_script.py -v` | 实现 source-key 选择后运行 | `collected 2 items`；2 个测试均 `PASSED`；最终 `2 passed in 6.75s` | GREEN 通过 |
| 3 | `.\.venv\Scripts\python.exe -m pytest tests/test_hn_source_signal_adapter.py tests/test_github_releases_collector.py tests/test_collect_source_signals_script.py tests/test_run_event_pipeline_script.py -v` | HN/GitHub adapter、采集脚本、生产脚本组合回归 | `collected 10 items`；10 个测试均 `PASSED`；最终 `10 passed in 10.11s` | 通过 |

### 失败与修复记录

- RED 失败原因：`run_event_pipeline.py` 只支持 `--seed-demo-signal`，不认识 `--source-key` 和 `--limit`。
- 修复方式：新增 source-key 选择逻辑，脚本可从已经入库的 `source_signals` 选择信号 ID，再调用 P1-2 的 `run_event_pipeline` workflow。
- 兼容性：原 `--seed-demo-signal` demo smoke 仍保留，并继续通过。

### 未覆盖范围

- 本 task 未用 `collect_source_signals.py` 先跑真实脚本再跑 pipeline 脚本；完整串联 smoke 放到 Task 5。
- 本 task 未跑 live HN / GitHub 网络采集；仍使用临时 SQLite 中预置的 `hn_algolia` 信号。
- 本 task 未跑 PostgreSQL；脚本测试使用临时 SQLite。

### 是否可以进入下一 task

可以进入 Task 5：P1-3 smoke, docs, and phase handoff。理由：采集脚本已经能写 `source_signals`，生产脚本已经能从已入库 `source_signals` 选择信号并生成 `PublishedEvent`。

## 2026-06-14 P1-3 Task 5 Smoke、文档与阶段交接测试记录

### 当前执行阶段

- 阶段：P1-3 HN / GitHub 采集接入新版链路。
- 当前完成 task：Task 5 P1-3 smoke, docs, and phase handoff。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 本次阶段最终新增和修改的模块

- 新增 `apps/worker/worker/sources/__init__.py`：导出 HN / GitHub source adapter。
- 新增 `apps/worker/worker/sources/hn_source.py`：
  - `build_hn_source` 构造 `hn_algolia` 的 `SourceCreate`。
  - `map_hn_story_to_source_signal` 将 `HNStory` 映射为 `SourceSignalCreate`。
- 新增 `apps/worker/worker/collectors/github_releases.py`：
  - `parse_github_release` 解析 GitHub releases REST API 单条 release。
  - `collect_from_github_releases_payload` 将 release payload 转成按发布时间倒序排列的列表。
  - `fetch_github_releases` 请求 GitHub 官方 releases REST API。
- 新增 `apps/worker/worker/sources/github_source.py`：
  - `build_github_releases_source` 构造 `github_releases` 的 `SourceCreate`。
  - `map_github_release_to_source_signal` 将 release 映射为 `SourceSignalCreate`。
- 新增 `apps/worker/scripts/collect_source_signals.py`：采集 HN / GitHub 并只写 `sources`、`source_signals`，不运行事件生产 workflow。
- 修改 `apps/worker/scripts/run_event_pipeline.py`：新增 `--source-key`、`--limit` 和 `load_signal_ids_by_source_key`，支持消费已入库 source signals。
- 新增测试：
  - `apps/worker/tests/test_hn_source_signal_adapter.py`
  - `apps/worker/tests/test_github_releases_collector.py`
  - `apps/worker/tests/test_collect_source_signals_script.py`
- 修改测试：
  - `apps/worker/tests/test_run_event_pipeline_script.py`
- 新增 fixture：
  - `apps/worker/tests/fixtures/github_releases_response.json`
- 更新文档：
  - `docs/05-实现计划/P1-3 HN与GitHub采集接入新版链路计划.md`
  - `docs/05-实现计划/P1-3 HN与GitHub采集接入新版链路计划.html`
  - `docs/05-实现计划/README.md`
  - `docs/05-实现计划/P1实施路线图与第一阶段后端数据底座计划.md`
  - `docs/05-实现计划/P1实施路线图与第一阶段后端数据底座计划.html`
  - `docs/00-项目总览/项目状态.md`
  - `docs/00-项目总览/文档索引.md`
  - `docs/07-验收与运行/README.md`
  - `docs/README.md`

### 测试数据

- 全量 pytest：使用 `apps/worker/tests` 下所有 50 个测试用例，覆盖依赖导入、Pydantic schema、SQLAlchemy model、migration metadata、服务层、LangGraph workflow、旧 runtime 清理、HN/GitHub source adapter、采集脚本和 pipeline 脚本。
- live smoke 数据库：全新 SQLite 文件 `apps/worker/scratch/p1_3_smoke_final.sqlite`。
- live smoke HN 数据：真实 HN Algolia API，参数为 `--source hn --hn-limit 1`。
- live smoke GitHub 数据：真实 GitHub releases API，仓库为 `openai/openai-python`，参数为 `--source github --github-repo openai/openai-python --github-limit 1`。
- pipeline smoke 数据：从同一 SQLite 库中选择 `source_key=hn_algolia` 的已入库信号，参数为 `--limit 1 --run-key manual-p1-3-final-smoke`。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest -v` | worker 全量回归 | `collected 50 items`；50 个测试均 `PASSED`；最终 `50 passed in 10.44s` | 通过 |
| 2 | `.\.venv\Scripts\python.exe scripts\collect_source_signals.py --database-url "sqlite+pysqlite:///scratch/p1_3_smoke_final.sqlite" --create-schema-for-smoke --source hn --hn-limit 1 --source github --github-repo openai/openai-python --github-limit 1` | 全新 SQLite live 采集 smoke | `{"signals_count": 2, "source_keys": ["github_releases", "hn_algolia"], "sources_count": 2, "status": "succeeded"}` | 通过 |
| 3 | `.\.venv\Scripts\python.exe scripts\run_event_pipeline.py --database-url "sqlite+pysqlite:///scratch/p1_3_smoke_final.sqlite" --source-key hn_algolia --limit 1 --run-key "manual-p1-3-final-smoke"` | 从已采集 HN signal 运行事件生产 pipeline | `{"candidates_count": 1, "dossiers_count": 1, "published_count": 1, "published_event_id": "pub_c3e6a2a5e1854da3b801f467c39d218e", "run_id": "run_74a443ea196c4978a997161dc316665b", "signals_count": 1, "status": "succeeded"}` | 通过 |
| 4 | Python `sqlite3` 计数检查 | 人工可见落库结果 | `{'sources': 2, 'source_signals': 2, 'pipeline_runs': 1, 'agent_runs': 3, 'event_candidates': 1, 'event_dossiers': 1, 'review_results': 1, 'published_events': 1}` | 通过 |

### 失败与修复记录

- Task 1 RED：`worker.sources` 不存在；修复为新增 `worker.sources` 包和 HN source adapter。
- Task 2 RED：`worker.collectors.github_releases` 不存在；修复为新增 GitHub releases collector 和 GitHub source adapter。
- Task 3 RED：`scripts/collect_source_signals.py` 不存在；修复为新增只写 `sources` / `source_signals` 的采集脚本，并保留 fixture mode。
- Task 4 RED：`run_event_pipeline.py` 不支持 `--source-key` / `--limit`；修复为新增 source-key 查询和 limit 选择逻辑。
- Task 5 没有出现新的测试失败；全量 pytest 和 live SQLite smoke 均一次通过。

### 未覆盖范围

- 未运行 PostgreSQL smoke；本阶段 live smoke 使用 SQLite 临时库，PostgreSQL migration / 部署前 smoke 仍需在后续部署准备阶段补。
- 未接真实 LLM Agent；P1-3 继续复用 P1-2 的确定性 Agent stub，真实 LLM 节点替换属于 P1-4。
- 未实现 GitHub Trending HTML 抓取；本阶段选择 GitHub 官方 releases REST API，避免把易碎页面 selector 放入 P1-3 主链路。
- 未做完整事实核验、repair prompt、tool-calling 和人工审稿后台。
- 未覆盖 HN / GitHub live API 限流、token 权限不足、仓库不存在、网络中断等异常分支；回归测试使用 fixture 和 fake transport 保证稳定。
- 未开发 Next.js 首页、事件详情页、后台管理 UI、Redis、队列、对象存储或向量数据库。

### 是否可以进入 P1-4

可以进入 P1-4。理由：P1-3 已完成 HN / GitHub 两类外部来源到新版 `SourceSignalCreate` 的映射，采集脚本可以把真实公开来源写入 `source_signals`，事件 pipeline 可以从已入库 `hn_algolia` 信号生成 `PublishedEvent`。最终 worker 全量测试为 `50 passed in 10.44s`，全新 SQLite live smoke 首跑返回 `status=succeeded` 且 `published_count=1`。P1-4 应聚焦真实 LLM Agent 节点替换，不恢复旧 `EvidenceCard / EventCluster / Brief` 主链路。

## 2026-06-14 P1-4 Task 0 真实 LLM Agent 节点替换计划测试记录

### 当前执行阶段

- 阶段：P1-4 真实 LLM Agent 节点替换。
- 当前完成 task：Task 0 P1-4 计划与入口文档。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 本次新增和修改的文档

- 新增 `docs/05-实现计划/P1-4 真实LLM Agent节点替换计划.md`：定义 P1-4 目标、边界、任务拆解、测试策略和验收标准。
- 新增 `docs/05-实现计划/P1-4 真实LLM Agent节点替换计划.html`：提供 P1-4 HTML 阅读版。
- 修改 `docs/README.md`：把 P1-4 计划加入推荐阅读顺序和下一步建议。
- 修改 `docs/05-实现计划/README.md`：登记 P1-4 计划 Markdown 和 HTML。
- 修改 `docs/00-项目总览/项目状态.md`：把当前阶段更新为 P1-4 计划阶段，并补充下一步任务拆解。
- 修改 `docs/00-项目总览/文档索引.md`：把 P1-4 计划加入实现计划索引和后端 / Agent 代理阅读顺序。
- 修改 `docs/07-验收与运行/后端P1测试记录.md`：追加本 Task 0 记录。

### 测试数据

- 本 task 是计划与文档 task，没有改 worker 运行代码，也没有新增数据库或业务测试数据。
- 文档依据来自当前 worker 代码结构：`LLMClient`、三类 deterministic stub、`EventPipelineTools`、`run_event_pipeline.py`、`agent_runs` 既有字段。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `git status --short --branch` | 确认工作区与分支 | 输出 `## codex/p1-data-foundation`，无未提交文件 | 通过 |
| 2 | `rg --files apps/worker | sort` | 确认 worker 当前文件结构 | 看到 `worker/llm_client.py`、`worker/agents/event_pipeline_agents.py`、`worker/tools/event_pipeline_tools.py`、`worker/workflows/event_pipeline.py` 等 P1-4 相关文件 | 通过 |
| 3 | `rg -n "P1-4|真实 LLM|Agent|LLMClient|provider|gateway|OpenAI|stub|EventPipeline|review|revise" docs/00-项目总览 docs/04-系统设计 docs/05-实现计划 apps/worker -g '!apps/worker/.venv/**'` | 搜索 P1-4 设计与现有实现接口 | 命中项目状态中的 P1-4 建议、Agent 系统设计中的三 Agent 职责、`LLMClient`、`EventPipelineTools` 和 workflow 注入点 | 通过 |
| 4 | `git diff --check` | 文档格式检查 | 退出码 0；PowerShell 输出为 Windows 工作区 LF/CRLF 提示，没有 trailing whitespace 或冲突标记错误 | 通过 |
| 5 | `rg "待[补]充|占[位]|预[计]通过|应[该]通过" docs/05-实现计划/P1-4 真实LLM Agent节点替换计划.md docs/07-验收与运行/后端P1测试记录.md` | 临时标记和非真实结果口径检查 | 退出码 1，无匹配 | 通过 |

### 失败与修复记录

- 本 task 未运行代码测试，未出现测试失败。
- 文档设计取舍：P1-4 不新增数据库表；复用 `agent_runs` 已有 `model_provider`、`model_name`、`prompt_version`、`retry_count` 和 `trace_json` 字段。

### 未覆盖范围

- 未实现 `LLMJsonAgent`。
- 未实现值班编辑、研究写作、审稿发布三个真实 LLM Agent。
- 未增加 `--agent-mode stub|llm`。
- 未运行 fake LLM workflow smoke 或真实 provider smoke。
- 未覆盖 provider/gateway 失败、LLM JSON repair、agent_runs metadata 写入等 P1-4 后续测试。

### 是否可以进入下一 task

可以进入 P1-4 Task 1：LLM JSON Agent Base。理由：P1-4 权威实施计划已经落地，下一步应按计划先写 `tests/test_llm_json_agent.py`，验证 RED 后再实现结构化 JSON 调用基座。

## 2026-06-14 P1-4 Task 1 LLM JSON Agent Base 测试记录

### 当前执行阶段

- 阶段：P1-4 真实 LLM Agent 节点替换。
- 当前完成 task：Task 1 LLM JSON Agent Base。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 本次新增和修改的模块

- 新增 `apps/worker/tests/test_llm_json_agent.py`：
  - `test_llm_json_agent_parses_fenced_json_into_schema`：验证 fenced JSON 可解析为 `EventCandidateDraft`。
  - `test_llm_json_agent_repairs_invalid_json_once`：验证第一次非法输出后会带错误摘要修复，并返回 `retry_count=1`。
  - `test_llm_json_agent_raises_after_max_retries`：验证多次失败后抛出 `LLMAgentOutputError`。
- 新增 `apps/worker/worker/agents/llm_json_agent.py`：
  - `LLMAgentOutputError`：表示 LLM 输出无法解析为目标 schema。
  - `LLMJsonResult`：封装 payload、raw_text、retry_count 和 prompt_version。
  - `LLMJsonAgent`：统一执行 LLM chat、JSON 提取、Pydantic 校验和 repair retry。
  - `_extract_json_text`：支持纯 JSON、fenced JSON 和夹杂说明文本中的 JSON 对象提取。
- 修改 `apps/worker/worker/agents/__init__.py`：导出 `LLMAgentOutputError`、`LLMJsonAgent` 和 `LLMJsonResult`。
- 更新 `docs/05-实现计划/P1-4 真实LLM Agent节点替换计划.md`：记录 Task 1 RED/GREEN 真实输出。
- 更新 `docs/07-验收与运行/后端P1测试记录.md`：追加本 Task 1 测试记录。

### 测试数据

- Fake LLM response 1：fenced JSON，内容为 `EventCandidateDraft` 所需字段，例如 `candidate_key=hn-openai-agent`、`title=OpenAI 新编码 Agent 引发开发者关注`、`ranking_score=79`。
- Fake LLM response 2：第一次返回 `not json`，第二次返回合法候选事件 JSON，用于验证 repair retry。
- Fake LLM response 3：连续返回 `not json`、`still not json`、`bad again`，用于验证超过最大 repair 次数后的失败异常。
- 所有测试均使用 `FakeLLMClient`，不访问真实 provider，不读取 `.env` 中的 API key。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_llm_json_agent.py -v` | 新增测试后、实现前运行 | `collected 0 items / 1 error`；失败原因为 `ModuleNotFoundError: No module named 'worker.agents.llm_json_agent'` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_llm_json_agent.py -v` | 实现 LLM JSON base 后运行 | `collected 3 items`；3 个测试均 `PASSED`；最终 `3 passed in 0.13s` | GREEN 通过 |
| 3 | `.\.venv\Scripts\python.exe -m pytest tests/test_llm_json_agent.py tests/test_event_pipeline_agent_stubs.py -v` | LLM JSON base + 既有 Agent stub 小回归 | `collected 6 items`；6 个测试均 `PASSED`；最终 `6 passed in 0.14s` | 通过 |

### 失败与修复记录

- RED 失败原因：`worker.agents.llm_json_agent` 模块不存在。
- 修复方式：新增 `llm_json_agent.py`，实现 JSON 提取、Pydantic 校验、repair prompt 和最大重试失败异常；同时更新 `worker.agents` 包导出。

### 未覆盖范围

- 本 task 未实现值班编辑、研究写作、审稿发布三个真实 LLM Agent。
- 本 task 未接入 `EventPipelineTools` 或 `run_event_pipeline.py`。
- 本 task 未记录 `agent_runs.model_provider`、`model_name`、`prompt_version`；该能力属于后续 Task 6。
- 本 task 未调用真实 provider；真实 provider smoke 属于 Task 7。

### 是否可以进入下一 task

可以进入 P1-4 Task 2：On-duty Editor LLM Agent。理由：结构化 JSON 调用基座已经可用，后续值班编辑 Agent 可以复用 `LLMJsonAgent.run_json(EventCandidateDraft, ...)` 输出候选事件草案。

## 2026-06-14 P1-4 Task 2 On-duty Editor LLM Agent 测试记录

### 当前执行阶段

- 阶段：P1-4 真实 LLM Agent 节点替换。
- 当前完成 task：Task 2 On-duty Editor LLM Agent。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 本次新增和修改的模块

- 新增 `apps/worker/tests/test_llm_editor_agent.py`：
  - `test_on_duty_editor_llm_agent_returns_candidate_draft`：验证值班编辑 LLM Agent 把标准化 source signal 转为 `EventCandidateDraft`。
  - `test_on_duty_editor_prompt_forbids_database_and_publish_actions`：验证 prompt 明确限制 Agent 不写库、不发布、不删除或隐藏信号。
- 新增 `apps/worker/worker/agents/llm_event_pipeline_agents.py`：
  - `OnDutyEditorLLMAgent`：真实值班编辑 LLM Agent，复用 `LLMJsonAgent.run_json(EventCandidateDraft, ...)`。
  - `_editor_system_prompt`：定义角色、JSON 输出要求和工程边界。
  - `_editor_user_prompt`：把 source signals 序列化为模型输入，并要求输出候选事件 JSON。
- 修改 `apps/worker/worker/agents/__init__.py`：导出 `OnDutyEditorLLMAgent`。
- 更新 `docs/05-实现计划/P1-4 真实LLM Agent节点替换计划.md`：记录 Task 2 RED/GREEN 真实输出。
- 更新 `docs/07-验收与运行/后端P1测试记录.md`：追加本 Task 2 测试记录。

### 测试数据

- Source signal：`source_key=hn_algolia`、`source_item_id=1001`、`original_title=OpenAI releases a new coding agent`、`original_url=https://example.com/openai-coding-agent`、`heat_metrics={"points": 120, "comments": 45}`。
- Fake LLM response：合法 `EventCandidateDraft` JSON，包含 `candidate_key=hn-openai-agent`、中文标题、`event_type=product_update`、`ranking_score=79`。
- 所有测试均使用 `FakeLLMClient`，不访问真实 provider，不读取 `.env` 中的 API key。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_llm_editor_agent.py -v` | 新增测试后、实现前运行 | `collected 0 items / 1 error`；失败原因为 `ModuleNotFoundError: No module named 'worker.agents.llm_event_pipeline_agents'` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_llm_editor_agent.py tests/test_llm_json_agent.py -v` | 实现 OnDutyEditorLLMAgent 后运行 | `collected 5 items`；5 个测试均 `PASSED`；最终 `5 passed in 0.25s` | GREEN 通过 |
| 3 | `.\.venv\Scripts\python.exe -m pytest tests/test_llm_editor_agent.py tests/test_event_pipeline_agent_stubs.py -v` | 值班编辑 LLM Agent + 既有 stub 回归 | `collected 5 items`；5 个测试均 `PASSED`；最终 `5 passed in 0.14s` | 通过 |

### 失败与修复记录

- RED 失败原因：`worker.agents.llm_event_pipeline_agents` 模块不存在。
- 修复方式：新增 `llm_event_pipeline_agents.py` 和 `OnDutyEditorLLMAgent`，并在 `worker.agents` 包导出。
- 设计边界：`OnDutyEditorLLMAgent` 只生成 `EventCandidateDraft`，不写数据库、不直接发布、不删除或隐藏信号。

### 未覆盖范围

- 本 task 未实现研究写作 LLM Agent 和审稿发布 LLM Agent。
- 本 task 未接入 `EventPipelineTools` 默认 factory 或脚本 `--agent-mode`。
- 本 task 未写入 `agent_runs` metadata；该能力属于后续 Task 6。
- 本 task 未调用真实 provider；真实 provider smoke 属于 Task 7。

### 是否可以进入下一 task

可以进入 P1-4 Task 3：Research Writer LLM Agent。理由：值班编辑 LLM Agent 已经可以用 fake LLM 输出 `EventCandidateDraft`，下一步可复用同一 JSON 基座生成 `EventDossierDraft`。

## 2026-06-14 P1-4 Task 3 Research Writer LLM Agent 测试记录

### 当前执行阶段

- 阶段：P1-4 真实 LLM Agent 节点替换。
- 当前完成 task：Task 3 Research Writer LLM Agent。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 本次新增和修改的模块

- 新增 `apps/worker/tests/test_llm_writer_agent.py`：
  - `test_research_writer_llm_agent_returns_event_dossier_draft`：验证研究写作 LLM Agent 输出 `EventDossierDraft`。
  - `test_research_writer_receives_revision_instructions`：验证审稿修订意见和写作边界进入 prompt。
- 修改 `apps/worker/worker/agents/llm_event_pipeline_agents.py`：
  - 新增 `ResearchWriterLLMAgent`：真实研究写作 LLM Agent，复用 `LLMJsonAgent.run_json(EventDossierDraft, ...)`。
  - 新增 `_writer_system_prompt`：定义中文写作、来源支撑、摘要长度和 JSON 输出要求。
  - 新增 `_writer_user_prompt`：把候选事件、source signals 和 revision instructions 序列化为模型输入。
- 修改 `apps/worker/worker/agents/__init__.py`：导出 `ResearchWriterLLMAgent`。
- 更新 `docs/05-实现计划/P1-4 真实LLM Agent节点替换计划.md`：记录 Task 3 RED/GREEN 真实输出。
- 更新 `docs/07-验收与运行/后端P1测试记录.md`：追加本 Task 3 测试记录。

### 测试数据

- Candidate：`candidate_key=hn-openai-agent`、中文标题 `OpenAI 新编码 Agent 引发开发者关注`、`category=模型与产品`、`ranking_score=79`。
- Source signal：`id=sig_1`、`source_key=hn_algolia`、`original_title=OpenAI releases a new coding agent`、`original_url=https://example.com/openai-coding-agent`。
- Fake LLM response：合法 `EventDossierDraft` JSON，包含 `card_title`、`card_summary`、`detail_title`、`detail_summary`、`detail_body`、`why_it_matters`、`follow_up_points`、`source_refs` 和 `status=draft`。
- 修订意见：`补充来源支撑，减少过度推断。`
- 所有测试均使用 `FakeLLMClient`，不访问真实 provider，不读取 `.env` 中的 API key。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_llm_writer_agent.py -v` | 新增测试后、实现前运行 | `collected 0 items / 1 error`；失败原因为 `ImportError: cannot import name 'ResearchWriterLLMAgent' from 'worker.agents.llm_event_pipeline_agents'` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_llm_writer_agent.py tests/test_llm_json_agent.py -v` | 实现 ResearchWriterLLMAgent 后运行 | `collected 5 items`；5 个测试均 `PASSED`；最终 `5 passed in 0.53s` | GREEN 通过 |
| 3 | `.\.venv\Scripts\python.exe -m pytest tests/test_llm_writer_agent.py tests/test_llm_editor_agent.py tests/test_event_pipeline_agent_stubs.py -v` | writer/editor LLM Agent + 既有 stub 回归 | `collected 7 items`；7 个测试均 `PASSED`；最终 `7 passed in 0.18s` | 通过 |

### 失败与修复记录

- RED 失败原因：`ResearchWriterLLMAgent` 尚未从 `worker.agents.llm_event_pipeline_agents` 导出。
- 修复方式：新增 `ResearchWriterLLMAgent`、writer system/user prompt，并在 `worker.agents` 包导出。
- 设计边界：`ResearchWriterLLMAgent` 只生成 `EventDossierDraft`，不保存数据库；后续保存仍由 `EventService.save_dossier` 控制。

### 未覆盖范围

- 本 task 未实现审稿发布 LLM Agent。
- 本 task 未接入 `EventPipelineTools` 默认 factory 或脚本 `--agent-mode`。
- 本 task 未写入 `agent_runs` metadata；该能力属于后续 Task 6。
- 本 task 未调用真实 provider；真实 provider smoke 属于 Task 7。

### 是否可以进入下一 task

可以进入 P1-4 Task 4：Review Publisher LLM Agent。理由：研究写作 LLM Agent 已经可以用 fake LLM 输出 `EventDossierDraft`，下一步可复用同一 JSON 基座生成 `ReviewResultDraft`。

## 2026-06-14 P1-4 Task 4 Review Publisher LLM Agent 测试记录

### 当前执行阶段

- 阶段：P1-4 真实 LLM Agent 节点替换。
- 当前完成 task：Task 4 Review Publisher LLM Agent。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 本次新增和修改的模块

- 新增 `apps/worker/tests/test_llm_reviewer_agent.py`：
  - `test_review_publisher_llm_agent_returns_publish_review`：验证审稿发布 LLM Agent 输出 `ReviewResultDraft`。
  - `test_review_publisher_prompt_lists_allowed_decisions`：验证 prompt 明确限定 `publish`、`revise`、`manual_review`、`reject`，并包含审稿边界。
- 修改 `apps/worker/worker/agents/llm_event_pipeline_agents.py`：
  - 新增 `ReviewPublisherLLMAgent`：真实审稿发布 LLM Agent，复用 `LLMJsonAgent.run_json(ReviewResultDraft, ...)`。
  - 新增 `_reviewer_system_prompt`：定义只输出 JSON、四种允许 decision、不直接发布、不改写正文、不写数据库、检查来源支撑和过度推断等边界。
  - 新增 `_reviewer_user_prompt`：把事件档案草案和 `revision_count` 序列化为模型输入。
- 修改 `apps/worker/worker/agents/__init__.py`：导出 `ReviewPublisherLLMAgent`。
- 更新 `docs/05-实现计划/P1-4 真实LLM Agent节点替换计划.md`：记录 Task 4 RED/GREEN 真实输出，并补充 `__init__.py` 为实际修改文件。
- 更新 `docs/07-验收与运行/后端P1测试记录.md`：追加本 Task 4 测试记录。

### 测试数据

- Event dossier：`candidate_key=hn-openai-agent`、中文卡片标题 `OpenAI 新编码 Agent 引发关注`、`category=模型与产品`、完整 `detail_body`、`why_it_matters` 和一条 `source_refs`。
- Fake LLM response：合法 `ReviewResultDraft` JSON，包含 `decision=publish`、`risk_level=low`、空 `issues`、空 `revision_instructions`，以及 `checked_items.source_supported=true`、`not_overstated=true`、`has_chinese_context=true`。
- Prompt 断言数据：第二个测试使用 `revision_count=1`，验证 user prompt 包含 `revision_count: 1`。
- 所有测试均使用 `FakeLLMClient`，不访问真实 provider，不读取 `.env` 中的 API key。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_llm_reviewer_agent.py -v` | 新增测试后、实现前运行 | `collected 0 items / 1 error`；失败原因为 `ImportError: cannot import name 'ReviewPublisherLLMAgent' from 'worker.agents.llm_event_pipeline_agents'` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_llm_reviewer_agent.py tests/test_llm_json_agent.py -v` | 实现 ReviewPublisherLLMAgent 后运行 | `collected 5 items`；5 个测试均 `PASSED`；最终 `5 passed in 0.13s` | GREEN 通过 |
| 3 | `.\.venv\Scripts\python.exe -m pytest tests/test_llm_reviewer_agent.py tests/test_llm_writer_agent.py tests/test_llm_editor_agent.py tests/test_event_pipeline_agent_stubs.py -v` | reviewer/writer/editor LLM Agent + 既有 stub 回归 | `collected 9 items`；9 个测试均 `PASSED`；最终 `9 passed in 0.18s` | 通过 |

### 失败与修复记录

- RED 失败原因：`ReviewPublisherLLMAgent` 尚未从 `worker.agents.llm_event_pipeline_agents` 导出。
- 修复方式：新增 `ReviewPublisherLLMAgent`、reviewer system/user prompt，并在 `worker.agents` 包导出。
- 设计边界：`ReviewPublisherLLMAgent` 只生成 `ReviewResultDraft`，不直接发布、不改写正文、不写数据库；后续发布仍由 `EventService.publish_dossier` 控制。

### 未覆盖范围

- 本 task 未接入 `EventPipelineTools` 默认 factory 或脚本 `--agent-mode`，该能力属于 Task 5。
- 本 task 未写入 `agent_runs` metadata；该能力属于后续 Task 6。
- 本 task 未验证 `revise`、`manual_review`、`reject` 三类真实 LLM 输出分支，只验证 prompt 枚举边界和 `publish` schema。
- 本 task 未调用真实 provider；真实 provider smoke 属于 Task 7。

### 是否可以进入下一 task

可以进入 P1-4 Task 5：Agent Factory, Workflow Injection, and CLI Mode。理由：三类真实 LLM Agent 已经都能通过 fake LLM 输出既有 Pydantic schema，下一步可以把 stub/llm 模式选择接入 factory、workflow 和脚本入口。

## 2026-06-14 P1-4 Task 5 Agent Factory, Workflow Injection, and CLI Mode 测试记录

### 当前执行阶段

- 阶段：P1-4 真实 LLM Agent 节点替换。
- 当前完成 task：Task 5 Agent Factory, Workflow Injection, and CLI Mode。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 本次新增和修改的模块

- 新增 `apps/worker/tests/test_agent_factory.py`：
  - `test_create_event_agents_defaults_to_stub_mode`：验证默认 agent mode 是 stub，且不需要 API key。
  - `test_create_event_agents_llm_mode_returns_llm_agents_with_shared_client`：验证 llm 模式创建三类真实 LLM Agent，且共享同一个 fake LLM client。
- 新增 `apps/worker/tests/test_event_pipeline_llm_mode.py`：
  - `test_event_pipeline_can_run_with_injected_llm_agents`：验证 workflow 可以用 `agent_mode="llm"` 和 fake LLM client 跑通首跑发布。
- 修改 `apps/worker/tests/test_run_event_pipeline_script.py`：
  - 新增 `test_run_event_pipeline_script_accepts_agent_mode_argument`，验证脚本接受 `--agent-mode llm`。
- 新增 `apps/worker/worker/agents/factory.py`：
  - `EventAgentSet`：封装 editor、writer、reviewer 三类 Agent。
  - `create_event_agents`：按 `stub|llm` 创建三类 Agent；llm 模式可注入共享 `LLMClient` 或 fake client。
- 修改 `apps/worker/worker/agents/__init__.py`：导出 `EventAgentSet` 和 `create_event_agents`。
- 修改 `apps/worker/worker/config.py`：新增 `Settings.agent_mode`，读取 `AGENT_MODE`，默认 `stub`。
- 修改 `apps/worker/worker/tools/event_pipeline_tools.py`：支持注入 `EventAgentSet`，默认仍创建三类 stub。
- 修改 `apps/worker/worker/workflows/event_pipeline.py`：`run_event_pipeline` 支持 `agent_mode`、`llm_client` 和 `tools` 注入。
- 修改 `apps/worker/scripts/run_event_pipeline.py`：新增 `--agent-mode stub|llm`，默认读取 settings，并在 stdout JSON 中输出 `agent_mode`。
- 更新 `docs/05-实现计划/P1-4 真实LLM Agent节点替换计划.md`：记录 Task 5 RED/GREEN 真实输出和执行偏差。
- 更新 `docs/07-验收与运行/后端P1测试记录.md`：追加本 Task 5 测试记录。

### 执行偏差

- 原计划写的是修改 `apps/worker/tests/test_event_pipeline_llm_mode.py`，但当前 worktree 中该文件不存在；实际处理为新增该测试文件。
- 原计划未列出 `apps/worker/worker/agents/__init__.py`，但为了让 factory 能从 `worker.agents` 包统一导出，实际同步修改该文件。

### 测试数据

- Factory 测试：不传 mode 时断言返回 `OnDutyEditorAgentStub`、`ResearchWriterAgentStub`、`ReviewPublisherAgentStub`；`mode=llm` 时注入 `FakeLLMClient(provider=fake, model=fake-model)`。
- LLM mode workflow 测试：内存 SQLite，`autoflush=False`；预置 `hn_algolia` SourceSignal，标题为 `OpenAI releases a new coding agent`，热度为 `points=120`、`comments=45`。
- Fake LLM response 顺序：两份 `EventCandidateDraft` JSON、一份 `EventDossierDraft` JSON、一份 `ReviewResultDraft` JSON。需要两份候选 JSON 的原因是现有 workflow 在 `editorial_triage` 记录 trace 时调用一次 editor，在 `merge_and_rank_events` 创建候选时再次调用 editor。
- 脚本参数测试：`sys.argv=["run_event_pipeline.py", "--agent-mode", "llm"]`。
- 所有新增测试均使用 fake LLM client，不访问真实 provider，不读取 `.env` 中的 API key。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_agent_factory.py tests/test_event_pipeline_llm_mode.py tests/test_run_event_pipeline_script.py -v` | 新增测试后、实现前运行 | `collected 4 items / 1 error`；失败原因为 `ModuleNotFoundError: No module named 'worker.agents.factory'` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_agent_factory.py tests/test_event_pipeline_llm_mode.py tests/test_run_event_pipeline_script.py -v` | 初版实现后运行 | `1 failed, 5 passed in 7.39s`；失败点为 AgentRun 顺序断言依赖不稳定排序 | 发现测试断言问题 |
| 3 | `.\.venv\Scripts\python.exe -m pytest tests/test_agent_factory.py tests/test_event_pipeline_llm_mode.py tests/test_run_event_pipeline_script.py -v` | 修复 AgentRun 顺序断言后运行 | `collected 6 items`；6 个测试均 `PASSED`；最终 `6 passed in 17.60s` | GREEN 通过 |
| 4 | `.\.venv\Scripts\python.exe -m pytest tests/test_event_pipeline_workflow.py tests/test_event_pipeline_tools.py tests/test_llm_editor_agent.py tests/test_llm_writer_agent.py tests/test_llm_reviewer_agent.py tests/test_agent_factory.py tests/test_event_pipeline_llm_mode.py -v` | workflow/tool/三类 LLM Agent 相关回归 | `collected 11 items`；11 个测试均 `PASSED`；最终 `11 passed in 2.01s` | 通过 |

### 失败与修复记录

- RED 失败原因：`worker.agents.factory` 尚不存在。
- 修复方式：新增 `EventAgentSet` 和 `create_event_agents`，并将 `EventPipelineTools`、workflow 和脚本入口接入 `stub|llm` 模式。
- 初版 GREEN 失败原因：测试用 `AgentRun.created_at asc, id asc` 推断流程顺序，但内存 SQLite 中多条记录时间戳可能相同，且 ID 是随机字符串，不提供流程顺序保证。
- 修复方式：测试改为断言 AgentRun 数量为 3，且名称集合精确等于 `on_duty_editor_llm`、`research_writer_llm`、`review_publisher_llm`。
- 设计边界：Task 5 只接入模式选择和注入，不记录 provider/model/prompt_version/retry_count 到 `agent_runs`；metadata 写入属于 Task 6。

### 未覆盖范围

- 本 task 未验证 `agent_runs.model_provider`、`model_name`、`prompt_version`、`retry_count` 写入；该能力属于 Task 6。
- 本 task 未覆盖 LLM 输出持续非法时的失败 AgentRun 记录；该能力属于 Task 6。
- 本 task 未调用真实 provider；真实 provider smoke 属于 Task 7。
- 本 task 未改变现有 workflow 中 editor 被调用两次的结构，只在测试数据中显式提供两份候选 JSON；是否优化为单次调用不属于本 task。

### 是否可以进入下一 task

可以进入 P1-4 Task 6：Agent Run Metadata and Failure Recording。理由：默认 stub 路径保持通过，`llm` 模式已经可以通过 factory、workflow 和脚本入口显式开启，并能用 fake LLM client 跑通首跑发布。下一步可在这个注入结构上补齐 `agent_runs` 的 provider/model/prompt_version/retry_count 和失败记录。

## 2026-06-14 P1-4 Task 6 Agent Run Metadata and Failure Recording 测试记录

### 当前执行阶段

- 阶段：P1-4 真实 LLM Agent 节点替换。
- 当前完成 task：Task 6 Agent Run Metadata and Failure Recording。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 本次新增和修改的模块

- 修改 `apps/worker/tests/test_event_pipeline_llm_mode.py`：
  - 新增 `test_llm_agent_runs_record_provider_model_prompt_and_retry_count`，验证成功 AgentRun 写入 provider、model、prompt_version 和 retry_count。
  - 新增 `test_llm_agent_failure_records_failed_agent_run`，验证 LLM 输出持续失败时写入 failed AgentRun 和 failed PipelineRun。
- 修改 `apps/worker/worker/tools/event_pipeline_tools.py`：
  - `record_agent_result` 新增 `agent` 参数，从 LLM Agent 的 `last_result` 读取 `raw_text`、`retry_count`、`prompt_version`，并写入 `agent_runs`。
  - 新增 `record_agent_failure`，写入 `status=failed`、provider、model、prompt_version、retry_count 和 error_message。
  - 新增 `_successful_agent_metadata`，集中读取成功调用 metadata。
- 修改 `apps/worker/worker/workflows/event_pipeline.py`：
  - editor / writer / reviewer 节点捕获 `LLMAgentOutputError`，先记录 failed AgentRun，再抛给 workflow 入口处理。
  - `run_event_pipeline` 捕获 `LLMAgentOutputError` 后回填 failed PipelineRun，并返回 `status=failed` 的 `EventPipelineState`。
- 更新 `docs/05-实现计划/P1-4 真实LLM Agent节点替换计划.md`：记录 Task 6 RED/GREEN 真实输出和执行偏差。
- 更新 `docs/07-验收与运行/后端P1测试记录.md`：追加本 Task 6 测试记录。

### 执行偏差

- 原计划列出修改 `apps/worker/worker/agents/llm_json_agent.py`，但 `LLMJsonResult.retry_count/raw_text/prompt_version` 已在 Task 1 完成，本 task 未改该文件。
- 为了让 failed AgentRun 后 workflow 不直接中断，实际新增修改 `apps/worker/worker/workflows/event_pipeline.py`。

### 测试数据

- 成功 metadata 测试：内存 SQLite，`autoflush=False`；预置 `hn_algolia` SourceSignal；fake LLM response 顺序为两份 `EventCandidateDraft` JSON、一次非法 writer 输出 `not json`、一次合法 `EventDossierDraft` JSON、一次合法 `ReviewResultDraft` JSON。
- 成功 metadata 断言：`on_duty_editor_llm` 写入 `model_provider=fake`、`model_name=fake-model`、`prompt_version=p1-4-editor-v1`、`retry_count=0`；`research_writer_llm` 写入 `prompt_version=p1-4-writer-v1`、`retry_count=1`，且 `trace_json.llm_prompt_version=p1-4-writer-v1`；`review_publisher_llm` 写入 `prompt_version=p1-4-reviewer-v1`。
- 失败记录测试：fake LLM response 顺序为两份合法 candidate、一份合法 dossier、三次非法 reviewer 输出；断言 workflow 返回 `status=failed`，failed AgentRun 为 `review_publisher_llm`，`retry_count=2`，错误信息包含 `无法解析 LLM 输出为 ReviewResultDraft`，PipelineRun 为 `failed` 且未生成 PublishedEvent。
- 所有新增测试均使用 fake LLM client，不访问真实 provider，不读取 `.env` 中的 API key。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_event_pipeline_llm_mode.py -v` | 新增 Task 6 测试后、实现前运行 | `collected 3 items`；`test_event_pipeline_can_run_with_injected_llm_agents` 通过；metadata 和 failure 两个测试失败；最终 `2 failed, 1 passed in 2.14s` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_event_pipeline_llm_mode.py -v` | 实现 metadata 与失败记录后运行 | `collected 3 items`；3 个测试均 `PASSED`；最终 `3 passed in 1.57s` | GREEN 通过 |
| 3 | `.\.venv\Scripts\python.exe -m pytest tests/test_event_pipeline_llm_mode.py tests/test_run_log_service.py -v` | Task 6 计划指定 GREEN 组合 | `collected 4 items`；4 个测试均 `PASSED`；最终 `4 passed in 3.69s` | 通过 |
| 4 | `.\.venv\Scripts\python.exe -m pytest tests/test_event_pipeline_workflow.py tests/test_event_pipeline_tools.py tests/test_run_event_pipeline_script.py tests/test_agent_factory.py tests/test_event_pipeline_llm_mode.py tests/test_llm_json_agent.py -v` | workflow/tool/script/factory/JSON Agent 相关回归 | `collected 13 items`；13 个测试均 `PASSED`；最终 `13 passed in 7.35s` | 通过 |

### 失败与修复记录

- RED 失败 1：成功 AgentRun 的 `model_provider` 为 `None`，说明 workflow 记录 AgentRun 时没有从 LLM Agent 读取 metadata。
- 修复方式：`record_agent_result` 新增 `agent` 参数，从 `agent.model_provider`、`agent.model_name`、`agent.prompt_version` 和 `agent.last_result.retry_count/raw_text` 写入 `agent_runs`。
- RED 失败 2：reviewer 连续输出非法 JSON 时直接抛出 `LLMAgentOutputError`，没有 failed AgentRun 和 failed PipelineRun。
- 修复方式：workflow 节点捕获 `LLMAgentOutputError` 后调用 `record_agent_failure`；`run_event_pipeline` 捕获后调用 `finish_run_with_counts(status="failed")` 并返回 failed state。

### 未覆盖范围

- 本 task 未调用真实 provider；真实 provider smoke 属于 Task 7。
- 本 task 未实现单独的 `smoke_llm_event_pipeline.py`；该脚本属于 Task 7。
- 本 task 未改变脚本在 workflow 返回 `status=failed` 时的进程退出策略；当前测试聚焦 workflow 和数据库记录。
- 本 task 未做完整事实核验或人工审稿后台。

### 是否可以进入下一 task

可以进入 P1-4 Task 7：P1-4 Smoke, Docs, and Phase Handoff。理由：LLM mode 成功路径已能写入 provider/model/prompt_version/retry_count，LLM 输出持续非法时也能记录 failed AgentRun 和 failed PipelineRun；下一步可以补 fake LLM smoke、全量 pytest、真实 provider smoke 记录和阶段性交接文档。

## 2026-06-14 P1-4 Task 7 Smoke, Docs, and Phase Handoff 测试记录

### 当前执行阶段

- 阶段：P1-4 真实 LLM Agent 节点替换。
- 当前完成 task：Task 7 P1-4 Smoke, Docs, and Phase Handoff。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 本次新增和修改的模块

- 新增 `apps/worker/scripts/smoke_llm_event_pipeline.py`：
  - `--fixture-mode`：使用 fake LLM client，不访问真实 provider，创建 smoke SQLite 并运行 `agent_mode=llm`。
  - `--call-real-provider`：使用 `.env` / 环境变量中的 provider 配置真实调用；失败时输出真实错误 JSON。
  - stdout 输出 JSON 摘要，包含 `status`、`agent_mode`、`mode`、`database_url`、`published_count`、`agent_runs_count`、`failed_agent_runs_count` 和 `agent_names`。
- 新增 `apps/worker/tests/test_smoke_llm_event_pipeline_script.py`：
  - `test_smoke_llm_event_pipeline_fixture_mode`：subprocess 运行 smoke 脚本 fixture 模式，验证首跑生成 `PublishedEvent`。
- 更新 `docs/05-实现计划/P1-4 真实LLM Agent节点替换计划.md`：记录 Task 7 RED/GREEN、全量测试、fixture smoke、真实 provider smoke 和交接状态。
- 更新 `docs/05-实现计划/P1-4 真实LLM Agent节点替换计划.html`：同步 P1-4 已完成状态和验收结果。
- 更新 `docs/05-实现计划/README.md`：把 P1-4 状态改为已完成并标明真实 provider 凭据风险。
- 更新 `docs/00-项目总览/项目状态.md`：更新当前阶段、P1-4 完成度、核心能力、剩余风险和下一步建议。
- 更新 `docs/00-项目总览/文档索引.md`：把 P1-4 文档说明改为完成记录。
- 更新 `docs/README.md`：把当前阶段与下一步建议改为 P1-4 已完成后的交接口径。
- 更新 `docs/07-验收与运行/后端P1测试记录.md`：追加本 Task 7 测试记录。

### 测试数据

- Smoke source：`source_key=p1_4_smoke`、`source_item_id=p1-4-smoke-1`、`original_title=OpenAI releases a new coding agent`、`original_url=https://example.com/openai-coding-agent`、`heat_metrics={"points": 120, "comments": 45}`。
- Fake LLM response：两份 `EventCandidateDraft` JSON、一份 `EventDossierDraft` JSON、一份 `ReviewResultDraft` JSON。两份候选 JSON 对应现有 workflow 中 editor trace 和 candidate 创建两次调用。
- fixture smoke 数据库：脚本默认写入 `runtime/p1-4-smoke/llm-event-pipeline-*.sqlite`；pytest 测试使用 `tmp_path` 下的临时 SQLite。
- real provider smoke（历史初次执行）：当时默认 `LLM_PROVIDER=deepseek` / `LLM_MODEL=deepseek-chat` 口径误指向 DeepSeek；该口径已被下方 “2026-06-14 P1-4 OpenAI Provider 配置修正测试记录” 替代。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_smoke_llm_event_pipeline_script.py -v` | smoke 脚本测试，脚本实现前运行 | `1 failed in 0.35s`；失败原因为 `can't open file ... scripts\smoke_llm_event_pipeline.py: [Errno 2] No such file or directory` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_smoke_llm_event_pipeline_script.py -v` | smoke 脚本实现后运行 | `collected 1 item`；测试 `PASSED`；最终 `1 passed in 2.66s` | GREEN 通过 |
| 3 | `.\.venv\Scripts\python.exe -m pytest -v` | worker 全量测试 | `collected 66 items`；66 个测试均 `PASSED`；最终 `66 passed in 16.65s` | 通过 |
| 4 | `.\.venv\Scripts\python.exe scripts\smoke_llm_event_pipeline.py --fixture-mode` | fake LLM smoke 命令实跑 | stdout JSON：`status=succeeded`、`agent_mode=llm`、`mode=fixture`、`published_count=1`、`agent_runs_count=3`、`failed_agent_runs_count=0`；数据库 URL 为 `sqlite+pysqlite:///C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation\runtime\p1-4-smoke\llm-event-pipeline-20260614122849.sqlite` | 通过 |
| 5 | `.\.venv\Scripts\python.exe scripts\smoke_llm_event_pipeline.py --call-real-provider` | 真实 provider smoke 历史初次执行 | 退出码 1；stdout JSON：`status=failed`、`agent_mode=llm`、`mode=real_provider`、`error=Missing API key for provider 'deepseek'. Set DEEPSEEK_API_KEY in local environment.` | 已记录真实失败；后续已按 OpenAI 口径修正，见下一节 |
| 6 | `git diff --check` | 文档与代码空白 / 冲突标记检查 | 退出码 0；PowerShell 输出为 Windows 工作区 LF/CRLF 提示，没有 trailing whitespace 或冲突标记错误 | 通过 |
| 7 | `rg "待[补]充|占[位]|预[计]通过|应[该]通过" docs/07-验收与运行/后端P1测试记录.md docs/05-实现计划/P1-4 真实LLM Agent节点替换计划.md` | 临时标记和非真实结果口径检查 | 退出码 1；无匹配 | 通过 |

### 失败与修复记录

- RED 失败原因：`scripts/smoke_llm_event_pipeline.py` 尚不存在。
- 修复方式：新增 smoke 脚本，fixture 模式注入 fake LLM client，real provider 模式不注入 fake client，统一输出 JSON 摘要。
- 真实 provider smoke 历史初次失败原因：当时默认 provider 仍误指向 DeepSeek，且本机环境没有配置 `DEEPSEEK_API_KEY`。
- 处理方式：记录原始错误，不把真实 provider smoke 失败写成通过；随后按用户要求改为本机 OpenAI / OpenAI-compatible 配置，并在下一节补充真实修正记录。

### 未覆盖范围

- 本历史段落当时未在本机真实调用 provider 成功跑通；该未覆盖项已在下一节补充为：OpenAI SDK 已真实调用成功，完整 pipeline 因示例来源进入 `manual_review`。高热度来源 publish smoke 和表达边界验收已在 2026-06-22 记录中补齐。
- 未验证真实 provider 输出质量、成本、延迟和 token 消耗。
- 未实现自由上网、RAG、向量数据库、Redis、队列、对象存储、后台 UI 或完整事实核验。
- 未优化 workflow 中 editor 被调用两次的结构；当前已在测试数据和 smoke 数据中显式提供两份候选 JSON。
- 未覆盖 `revise/manual_review/reject` 的完整多轮人工交互体验。

### 是否可以进入 P1-5

可以进入 P1-5 的工程准备。理由：P1-4 的 fake LLM 自动化回归、全量 worker pytest、LLM mode 首跑发布、agent_runs metadata 和失败记录均已通过。该段真实 provider 结论已被下一节 OpenAI 配置修正记录更新：本机 OpenAI / OpenAI-compatible SDK 可用，后续需补高热度来源 publish smoke 和表达边界验收。

## 2026-06-14 P1-4 OpenAI Provider 配置修正测试记录

### 当前执行阶段

- 阶段：P1-4 真实 LLM Agent 节点替换后的 provider 配置修正。
- 当前完成内容：把真实 provider 默认口径从 DeepSeek 修正为本机 OpenAI / OpenAI-compatible 配置，并验证可复用主目录 `.env`。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 本次新增和修改的模块

- 新增 `apps/worker/tests/test_config.py`：
  - `test_load_settings_defaults_to_openai_provider_when_env_missing`：验证无 `.env`、无 `LLM_PROVIDER`、无 `LLM_MODEL` 时默认 provider/model 为 `openai/gpt-4o-mini`。
- 修改 `apps/worker/tests/test_llm_client.py`：
  - 新增 `test_openai_provider_config_reads_optional_user_agent`，验证 `OPENAI_USER_AGENT` 会进入 provider config。
  - 新增 `test_create_openai_client_passes_user_agent_as_default_header`，验证 OpenAI SDK 初始化会传入 `default_headers={"User-Agent": "python-httpx/0.28.1"}`。
- 修改 `apps/worker/worker/config.py`：
  - `load_settings()` 默认 `LLM_PROVIDER` 改为 `openai`。
  - 新增 `_default_llm_model(provider)`，在未配置 `LLM_MODEL` 时按 provider 返回默认模型；OpenAI 默认 `gpt-4o-mini`，DeepSeek 显式 provider 仍可默认 `deepseek-chat`。
- 修改 `apps/worker/worker/llm_client.py`：
  - `LLMProviderConfig` 新增 `user_agent`。
  - `resolve_provider_config("openai")` 读取 `OPENAI_USER_AGENT`。
  - `_create_openai_client()` 在配置 user_agent 时传给 OpenAI SDK 的 `default_headers`。

### 测试数据

- 配置默认值测试：临时项目根目录 `tmp_path`，没有 `.env`；清空 `LLM_PROVIDER`、`LLM_MODEL`、`AGENT_MODE`。
- User-Agent 测试：`OPENAI_API_KEY=test-key`、`OPENAI_BASE_URL=https://api.codexapi.space/v1`、`OPENAI_USER_AGENT=python-httpx/0.28.1`。
- 真实 OpenAI SDK smoke：运行时只把 `D:\AI World Radar\.env` 注入当前 PowerShell 进程环境，不复制 `.env` 到 worktree，不输出 API key；该 `.env` 中确认存在 `OPENAI_API_KEY`，provider/model/base_url/user-agent 口径为 `openai`、`gpt-5.5`、`https://api.codexapi.space/v1`、`python-httpx/0.28.1`。
- 真实 pipeline smoke：仍使用脚本内置 smoke source，`original_url=https://example.com/openai-coding-agent`。该 URL 是示例来源，因此真实 reviewer 判断为 `manual_review`。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_config.py tests/test_llm_client.py -v` | 新增测试后、实现前运行 | `collected 6 items`；`3 failed, 3 passed in 0.32s`；失败点分别为默认 provider 仍是 `deepseek`、`LLMProviderConfig` 没有 `user_agent`、构造 config 不接受 `user_agent` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_config.py tests/test_llm_client.py -v` | 修正 config / LLMClient 后运行 | `collected 6 items`；6 个测试均 `PASSED`；最终 `6 passed in 0.11s` | GREEN 通过 |
| 3 | `.\.venv\Scripts\python.exe -m pytest -v` | worker 全量回归 | `collected 69 items`；69 个测试均 `PASSED`；最终 `69 passed in 16.63s` | 通过 |
| 4 | 先从 `D:\AI World Radar\.env` 注入当前进程环境，再运行 `.\.venv\Scripts\python.exe scripts\smoke_llm_client.py --call` | 验证本分支可复用主目录 OpenAI/OpenAI-compatible 配置 | stdout：`provider=openai`、`model=gpt-5.5`、`base_url=https://api.codexapi.space/v1`、`api_key_present=True`、`pong` | 真实 OpenAI SDK 调用通过 |
| 5 | 先从 `D:\AI World Radar\.env` 注入当前进程环境，再运行 `.\.venv\Scripts\python.exe scripts\smoke_llm_event_pipeline.py --call-real-provider` | 验证真实 provider 下三类 LLM Agent pipeline | 退出码 1；stdout JSON：`status=partial_failed`、`agent_mode=llm`、`mode=real_provider`、`signals_count=1`、`candidates_count=1`、`dossiers_count=1`、`agent_runs_count=3`、`failed_agent_runs_count=0`、`published_count=0` | 真实 provider 已可调用；未发布是审稿安全决策 |
| 6 | 查询 smoke SQLite：`runtime/p1-4-smoke/llm-event-pipeline-20260614134624.sqlite` | 确认 partial_failed 根因 | `agent_runs` 三条均 `succeeded`，`model_provider=openai`、`model_name=gpt-5.5`；`review_results.decision=manual_review`、`risk_level=medium`；`published_events=0` | 根因是示例来源支撑不足，不是 SDK / gateway / API key 失败 |

### 失败与修复记录

- RED 失败 1：`load_settings()` 默认 provider/model 仍是 `deepseek/deepseek-chat`。
- 修复方式：默认 provider 改为 `openai`，默认 model 改为按 provider 解析；OpenAI 默认 `gpt-4o-mini`。
- RED 失败 2：本分支没有读取 `OPENAI_USER_AGENT`，OpenAI SDK 会使用默认 `OpenAI/Python ...` User-Agent，不符合本机 codexapi 网关注意事项。
- 修复方式：`LLMProviderConfig.user_agent` 贯通到 `_create_openai_client(default_headers=...)`。
- 完整真实 pipeline smoke 未发布：reviewer 对 `example.com` 示例来源给出 `manual_review`，当时理由包括来源支撑不足。
- 处理方式：不把该次 `manual_review` 改成通过；记录为真实 provider 可用但内置示例测试数据不适合作为高热度发布验收。稳定自动化验收仍以 fake LLM smoke 和全量 pytest 为准。

### 未覆盖范围

- 未把 `D:\AI World Radar\.env` 复制到 worktree；真实调用通过进程环境临时注入，避免把密钥写入本分支。
- 未用真实高热度来源重新跑一次 `published_count=1` 的 real provider publish smoke。
- 未统计真实 provider token 消耗、延迟和成本。
- 未改变 P1-4 的 `stub` 默认 Agent 模式；真实 LLM 仍需显式 `--agent-mode llm` 或 `--call-real-provider`。

### 是否可以进入下一阶段

可以继续进入 P1-5 工程准备，但进入发布质量验收前建议补一条“高热度来源 real provider publish smoke”：使用 HN 等真实热度源，证明 reviewer 可以在来源表达边界匹配时给出 `publish` 并生成热议型 `PublishedEvent`，且不会把社区讨论写成官方事实。当前本机 OpenAI/OpenAI-compatible 配置已经验证可在该分支使用。

## 2026-06-16 本机 PostgreSQL `ai_world_radar` 重建记录

### 当前执行阶段

- 阶段：P1-4 完成后的本机数据库环境校正。
- 当前完成内容：按用户要求删除 DataGrip 中可见的旧版 PostgreSQL 数据库 `ai_world_radar`，并用新版 P1-1 migration 重建同名数据库。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 操作范围

- 删除并重建本机 PostgreSQL 数据库：`ai_world_radar`。
- 未修改代码结构。
- 未修改 migration 文件。
- 未写入或提交任何数据库密码、API key 或私密连接串。

### 测试数据

- 目标数据库：本机 PostgreSQL `ai_world_radar`。
- Alembic revision：`202606120001`。
- stub pipeline demo signal：由 `scripts/run_event_pipeline.py --seed-demo-signal` 自动创建。
- Agent 模式：`stub`，不调用真实 LLM provider。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | Python `psycopg` 管理脚本连接 `postgres` 管理库，执行 terminate connections、`DROP DATABASE IF EXISTS ai_world_radar`、`CREATE DATABASE ai_world_radar` | 删除旧库并重建同名空库 | `target_db=ai_world_radar`、`existed_before=True`、`terminated_connections=4`、`recreated=true` | 通过 |
| 2 | `DATABASE_URL=<本机 ai_world_radar 连接串>; .\.venv\Scripts\python.exe -m alembic upgrade head` | 在重建后的 PostgreSQL 库上执行新版 migration | `Context impl PostgresqlImpl`、`Running upgrade  -> 202606120001, create event dossier core tables` | 通过 |
| 3 | Python `psycopg` 查询 `alembic_version` 与 `information_schema.tables` | 验证 DataGrip 应看到的真实表结构 | `alembic_version=202606120001`；表名为 `admin_actions, agent_runs, alembic_version, event_candidate_signals, event_candidates, event_dossiers, pipeline_runs, published_events, review_results, source_signals, sources` | 通过 |
| 4 | `.\.venv\Scripts\python.exe scripts\run_event_pipeline.py --database-url <本机 ai_world_radar 连接串> --seed-demo-signal --run-key manual-postgres-recreate-20260616 --agent-mode stub` | 验证新库不仅建表成功，也能跑通新版事件生产链路 | stdout JSON：`status=succeeded`、`signals_count=1`、`candidates_count=1`、`dossiers_count=1`、`published_count=1`、`agent_runs_count` 由后续计数验证为 `3` | 通过 |
| 5 | Python `psycopg` 查询核心表行数 | 验证 DataGrip 刷新后可看到新版表和 demo 数据 | `sources=1`、`source_signals=1`、`pipeline_runs=1`、`agent_runs=3`、`event_candidates=1`、`event_candidate_signals=1`、`event_dossiers=1`、`review_results=1`、`published_events=1`、`admin_actions=0` | 通过 |

### 失败与修复记录

- 用户在 DataGrip 中看到 `event_clusters`、`evidence_cards`、`briefs`、`brief_items` 等旧表。
- 根因：代码分支中的新版 migration 已存在，但 DataGrip 当前连接的本机 PostgreSQL 物理库 `ai_world_radar` 仍是旧版库；之前 P1-2 到 P1-4 的主要 smoke 多使用临时 SQLite 或独立 smoke 数据库，没有把新版结构固定落到该 DataGrip 可见库。
- 修复方式：按用户明确要求删除旧库，重建同名 `ai_world_radar`，执行新版 Alembic migration，并用 stub pipeline 写入一条 demo 数据验证链路。

### 未覆盖范围

- 本次只验证本机 PostgreSQL `stub` 模式链路，未调用真实 OpenAI / OpenAI-compatible provider。
- 当时尚未完成高热度来源 real provider `publish` smoke；该项已在 2026-06-22 记录中补齐。
- 未统计真实 provider token、延迟和成本。

### 当前结论

本机 PostgreSQL `ai_world_radar` 已经从旧表结构重建为新版 P1 数据底座。DataGrip 刷新该库后，应看到 `source_signals`、`event_candidates`、`event_dossiers`、`review_results`、`published_events`、`agent_runs` 等新版表，而不应再看到旧链路的 `event_clusters`、`evidence_cards`、`briefs`、`brief_items`。

## 2026-06-16 P0 文档收口与全量复验记录

### 当前执行阶段

- 阶段：P1-4 完成后的 P0 收口。
- 当前完成内容：同步本机 PostgreSQL 重建后的项目状态，补充最新 worker 全量测试复验，并准备推送远程。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 本次修改的文档

- 修改 `docs/00-项目总览/项目状态.md`：
  - 更新时间改为 `2026-06-16 18:09:21 +08:00`。
  - 记录本机 PostgreSQL `ai_world_radar` 已按新版 P1 migration 重建。
  - 记录本机 PostgreSQL `stub` pipeline smoke 已生成 `PublishedEvent`。
  - 将“真实 PostgreSQL 新版 migration smoke 未覆盖”的旧风险改为“真实 provider 自动发布 smoke 未覆盖”。该口径已在 2026-06-17 校正为“高热度来源自动发布 smoke 未覆盖”。
- 修改 `docs/07-验收与运行/后端P1测试记录.md`：
  - 追加本段 P0 文档收口与全量复验记录。

### 测试数据

- 测试环境：`apps/worker/.venv`。
- 测试命令运行目录：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation\apps\worker`。
- 测试范围：worker 全量 pytest，共 69 个测试用例。
- 数据库：各测试沿用现有 fixture、内存 SQLite、pytest 临时 SQLite 和 fake LLM client；本次全量测试不调用真实 provider。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `git status --short --branch; git branch --show-current; git log --oneline -5` | 确认当前分支和未推送状态 | `## codex/p1-data-foundation...origin/codex/p1-data-foundation [ahead 1]`；当前分支 `codex/p1-data-foundation`；最新提交 `6664517 docs: record local postgres rebuild` | 通过 |
| 2 | `.\.venv\Scripts\python.exe -m pytest -v` | P0 收口前最新 worker 全量复验 | `collected 69 items`；69 个测试均 `PASSED`；最终 `69 passed in 12.53s` | 通过 |

### 失败与修复记录

- 本次全量 pytest 未出现失败。
- 本次文档收口发现的偏差：`docs/00-项目总览/项目状态.md` 仍保留“真实 PostgreSQL 新版 migration smoke 未覆盖”的旧口径；但 2026-06-16 已完成本机 PostgreSQL `ai_world_radar` 重建、Alembic migration 和 `stub` pipeline smoke。
- 修复方式：更新项目状态文档，把 PostgreSQL 本机 smoke 改为已完成，并保留真实 provider 自动发布 smoke 作为后续未覆盖范围。该未覆盖范围已在 2026-06-17 校正为高热度来源 publish smoke 和表达边界验收。

### 未覆盖范围

- 本次只复验 worker 自动化测试和文档状态，不新增业务代码。
- 本次全量 pytest 不调用真实 OpenAI / OpenAI-compatible provider。
- 当时仍尚未完成高热度来源 real provider `publish` smoke；该项已在 2026-06-22 记录中补齐。
- 仍未统计真实 provider token、延迟和成本。

### 当前结论

P0 文档收口已同步到项目状态和测试记录。当前后端 P1 的剩余闭环重点不再是本机 PostgreSQL 建库，而是高热度来源 real provider 自动发布 smoke、review 分支覆盖、以及真实 provider 成本与延迟审计。

## 2026-06-17 P1 热度优先验收口径校正记录

### 当前执行阶段

- 阶段：P1-4 完成后的 P1-5 前置口径校正。
- 当前完成内容：把后续真实 provider 发布验收从“必须补官方/一手来源”校正为“高热度来源 + 表达边界”，与产品宪章中的“让国内用户理解外网 AI 圈重要风向”一致。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 本次修改的文档

- 修改 `docs/02-信息源策略/信息源策略.md` 与 HTML 阅读版：
  - 明确 P1 是热度优先、轻核验、重表达边界。
  - 明确热度源可支撑“讨论正在发生”，但不能支撑“讨论中的说法已经被证实”。
  - 明确审稿节点应判断“表达是否和来源强度匹配”。
- 修改 `docs/README.md`、`docs/00-项目总览/项目状态.md`、`docs/00-项目总览/文档索引.md`：
  - 将下一步建议改为高热度来源 real provider publish smoke。
  - 明确验收重点是 HN 等热度源可生成热议型 `PublishedEvent`，且不把社区讨论写成官方事实。
- 修改 `docs/05-实现计划/README.md`、`docs/05-实现计划/P1-4 真实LLM Agent节点替换计划.md` 与 HTML 阅读版：
  - 将 P1-4 交接建议同步为高热度来源 publish smoke。
- 修改本测试记录：
  - 保留 `example.com` 示例来源触发 `manual_review` 的历史事实。
  - 将后续未覆盖范围统一改为高热度来源 publish smoke 和表达边界验收。

### 测试数据

- 本次为文档口径校正，不修改业务代码、不迁移数据库、不调用真实 LLM provider。
- 校验数据为当前 docs 目录中与 P1 状态、信息源策略、P1-4 交接和验收记录相关的 Markdown / HTML 文件。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `$oldPattern = ('可' + '信来源 real provider|可' + '信来源自动发布|可' + '信来源 publish|真实可' + '信来源|使用可' + '信来源的 real provider|可' + '信来源'); rg $oldPattern docs/README.md docs/00-项目总览 docs/02-信息源策略 docs/05-实现计划 docs/07-验收与运行 -n -S` | 检查主验收文档范围内是否仍残留旧验收口径 | 退出码 1；无匹配输出 | 通过 |
| 2 | `git diff --check` | 检查文档 diff 是否有空白错误 | 退出码 0；仅输出 `LF will be replaced by CRLF` 的 Windows 换行提示 | 通过 |
| 3 | `git status --short --branch` | 确认本次变更范围 | 显示当前分支 `codex/p1-data-foundation`，9 个 docs 文件被修改 | 通过 |

### 失败与修复记录

- 初次关键词扫描仍发现 2 处旧口径残留，均位于测试记录历史段落。
- 修复方式：把历史段落中容易误导后续代理的旧验收词改为“来源支撑不足”和“真实 provider 自动发布 smoke 未覆盖”，并在当前记录中说明新的验收口径。

### 未覆盖范围

- 本次没有修改 reviewer prompt、smoke 脚本或 pipeline 代码。
- 本次没有重新运行 worker 全量 pytest；原因是变更范围仅为 docs，未触碰 `apps/worker` 代码。
- 当时尚未执行真实高热度来源 real provider publish smoke；该项已在 2026-06-22 记录中补齐。
- 本次没有评估真实 provider token、延迟和成本。

### 当前结论

文档交付界面已与 P1 热度优先策略对齐。该段之后的下一步曾是补高热度来源 real provider publish smoke，并配套调整 reviewer 表达边界测试；该项已在 2026-06-22 记录中完成。

## 2026-06-22 P1-5 高热度来源 real provider publish smoke 记录

### 当前执行阶段

- 阶段：P1-5 发布质量与工程准备的前置闭环。
- 当前完成内容：把“热度优先、表达边界优先”从文档口径落到 writer/reviewer prompt、fake LLM smoke fixture 和真实 provider smoke。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 本次修改的代码模块

- 修改 `apps/worker/worker/agents/llm_event_pipeline_agents.py`：
  - `ResearchWriterLLMAgent` prompt 增加热度源表达边界：热度源只支撑“讨论正在发生”，优先使用“外网热议 / 社区正在讨论 / 传闻正在发酵 / 尚未看到官方确认”，不要写成官方已确认事实。
  - `ReviewPublisherLLMAgent` prompt 增加审稿边界：热度源可以支撑讨论正在发生，不要求每条热议型事件都有官方事实源；审稿重点是表达是否和来源强度匹配。
- 修改 `apps/worker/scripts/smoke_llm_event_pipeline.py`：
  - 默认 smoke 信号从 `example.com` 示例源改为高热度 HN 社区源。
  - smoke source 改为 `source_key=hn_algolia`、`source_type=community`。
  - smoke signal 改为 `HN discussion: developers debate OpenAI coding agents`，热度指标为 `points=512`、`comments=186`、`hn_heat_score=88`。
  - fake LLM fixture 文案改为“HN 热议 / 开发者正在讨论”口径。
- 修改 `apps/worker/tests/test_llm_writer_agent.py`：
  - 新增 `test_research_writer_prompt_bounds_community_heat_language`。
- 修改 `apps/worker/tests/test_llm_reviewer_agent.py`：
  - 新增 `test_review_publisher_prompt_allows_bounded_heat_discussion_publish`。
- 修改 `apps/worker/tests/test_smoke_llm_event_pipeline_script.py`：
  - 增加 smoke 入库数据断言，确保 source / signal 是 HN 高热社区源。

### 测试数据

- fake LLM fixture 数据：
  - source：`hn_algolia`
  - source type：`community`
  - original URL：`https://news.ycombinator.com/item?id=40617088`
  - heat metrics：`points=512`、`comments=186`、`hn_heat_score=88`
  - 文案口径：只表达“HN / 开发者社区正在讨论”，不表达“OpenAI 官方已发布或确认”。
- 真实 provider smoke：
  - 从 `D:\AI World Radar\.env` 临时注入当前 PowerShell 进程环境。
  - 不复制 `.env` 到 worktree。
  - 不输出 API key。
  - 真实 provider 仍显式使用 `--call-real-provider`，默认 `AGENT_MODE=stub` 未改变。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_llm_writer_agent.py tests/test_llm_reviewer_agent.py tests/test_smoke_llm_event_pipeline_script.py -v` | 新增测试后、实现前运行 | `collected 7 items`；`3 failed, 4 passed in 2.85s`；失败点分别为 writer prompt 缺少热度源受限表达、reviewer prompt 缺少热议发布边界、smoke source 仍是 `p1_4_smoke` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_llm_writer_agent.py tests/test_llm_reviewer_agent.py tests/test_smoke_llm_event_pipeline_script.py -v` | 修改 prompt 和 smoke fixture 后运行 | `collected 7 items`；7 个测试均 `PASSED`；最终 `7 passed in 2.32s` | GREEN 通过 |
| 3 | `.\.venv\Scripts\python.exe -m pytest -v` | worker 全量回归 | `collected 71 items`；71 个测试均 `PASSED`；最终 `71 passed in 12.88s` | 通过 |
| 4 | 先从 `D:\AI World Radar\.env` 注入当前进程环境，再运行 `.\.venv\Scripts\python.exe scripts\smoke_llm_event_pipeline.py --call-real-provider` | 真实 OpenAI / OpenAI-compatible provider 高热 HN publish smoke | stdout JSON：`status=succeeded`、`mode=real_provider`、`agent_mode=llm`、`signals_count=1`、`candidates_count=1`、`dossiers_count=1`、`agent_runs_count=3`、`failed_agent_runs_count=0`、`published_count=1`、`run_id=run_60a78e4528084e22b46a5590bee508c8` | 通过 |
| 5 | Python `sqlite3` 查询 `runtime/p1-4-smoke/llm-event-pipeline-20260622051548.sqlite` 的 source、review 和 published_event | 验证真实 provider 不是只通过计数，而是表达边界正确 | `source_key=hn_algolia`；`original_url=https://news.ycombinator.com/item?id=40617088`；`decision=publish`；`risk_level=low`；`checked_items.source_supported.note` 说明 HN 只支撑“社区正在热议”；`checked_items.not_overstated.note` 说明正文未写成 OpenAI 官方发布 | 通过 |

### 失败与修复记录

- RED 失败 1：writer prompt 没有明确“热度源只支撑讨论正在发生”，真实 LLM 可能把 HN 标题写成官方已确认事实。
- 修复方式：在 writer system/user prompt 中加入热度源表达边界。
- RED 失败 2：reviewer prompt 没有明确“热度源可以支撑讨论本身，不要求官方事实源”，真实 LLM 容易把所有社区讨论压到 `manual_review`。
- 修复方式：在 reviewer system prompt 中加入“表达是否和来源强度匹配”的门禁规则。
- RED 失败 3：P1-4 smoke 数据仍是 `p1_4_smoke` / `example.com`，不适合作为高热度来源 publish 验收。
- 修复方式：把 smoke seed 改为 `hn_algolia` 高热社区源，并同步更新 fake LLM fixture 文案。
- 查询 smoke 数据库时曾按 `source_signals.source_key` 查询，实际表结构是 `source_signals.source_id -> sources.source_key`；已改为 join `sources` 后重新查询通过。该问题只影响人工核验查询，不影响业务代码。

### 未覆盖范围

- 本次真实 provider smoke 使用独立 SQLite smoke 库，未写入本机 PostgreSQL `ai_world_radar`。
- 本次没有统计真实 provider token、延迟和成本。
- 本次没有接入 live HN API；smoke 使用脚本内置高热 HN 风格信号。
- 本次没有补 `revise/manual_review/reject` 独立分支、slug 冲突分支和 LangGraph 修订循环深度分支。
- 本次没有新增 FastAPI、Next.js 前端、后台管理 UI、Redis、队列、对象存储或完整事实核验。

### 当前结论

高热度来源 real provider publish smoke 已补齐。当前真实 provider 可以把 HN 热度源生成热议型 `PublishedEvent`，并通过 reviewer 明确保持表达边界：可写“社区正在热议”，不能写成“官方已确认事实”。后续 P1-5 应继续补发布质量分支覆盖、成本延迟审计，并按需要在 PostgreSQL `ai_world_radar` 上复跑真实 provider smoke。

## 2026-06-22 P1-5 详情正文信息密度修复记录

### 当前执行阶段

- 阶段：P1-5 发布质量与工程准备前置修复。
- 用户反馈：真实 provider 已能生成 `PublishedEvent`，但详情正文信息量偏少，不足以作为用户点进事件后的正文。
- 当前完成内容：把 `EventDossierDraft.detail_body` 的最低信息密度固化为后端 schema 契约，并同步更新 writer prompt、stub、fake LLM fixture 和 smoke fixture。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 本次修改的代码模块

- 修改 `apps/worker/worker/schemas/event.py`：
  - `EventDossierDraft.detail_body` 从非空字符串提升为至少 500 字符。
  - 新增 `validate_detail_body_depth`，要求正文至少 5 段，避免三段短摘要进入发布链路。
- 修改 `apps/worker/worker/agents/llm_event_pipeline_agents.py`：
  - `ResearchWriterLLMAgent` system/user prompt 增加详情正文质量要求：至少 5 段、不少于 500 个中文字符。
  - 当时 prompt 曾要求覆盖背景、热度、讨论焦点、影响和来源边界；该口径已在后续“详情正文读者口吻修复记录”中修正为：热度和来源边界只用于内部排序 / 审稿，不进入 `detail_body`。
- 修改 `apps/worker/worker/agents/event_pipeline_agents.py`：
  - 新增 `_rich_stub_detail_body`，让 stub 模式也生成满足新质量线的正文。
- 修改测试与 smoke fixture：
  - `tests/test_schema_contracts.py`
  - `tests/test_llm_writer_agent.py`
  - `tests/test_llm_reviewer_agent.py`
  - `tests/test_event_service.py`
  - `tests/test_event_pipeline_agent_stubs.py`
  - `tests/test_event_pipeline_llm_mode.py`
  - `tests/test_smoke_llm_event_pipeline_script.py`
  - `scripts/smoke_llm_event_pipeline.py`

### 测试数据

- schema RED 数据：三段短正文，内容为“发生了什么 / 为什么重要 / 后续看什么”，用于证明低信息量正文会被拒绝。
- rich fixture 数据：HN 高热编码 Agent 讨论，保持 `source_key=hn_algolia`、`points=512`、`comments=186`、`hn_heat_score=88` 口径。
- 真实 provider smoke 数据：
  - 来源：脚本内置 HN 高热社区信号。
  - 运行模式：`--call-real-provider`。
  - 环境变量来源：只从 `D:\AI World Radar\.env` 注入当前 PowerShell 进程，不复制 `.env`，不写入密钥。
  - 数据库：独立 SQLite smoke 库 `runtime\p1-4-smoke\llm-event-pipeline-20260622062701.sqlite`。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_schema_contracts.py tests/test_llm_writer_agent.py tests/test_llm_reviewer_agent.py tests/test_event_service.py tests/test_event_pipeline_agent_stubs.py tests/test_event_pipeline_llm_mode.py tests/test_smoke_llm_event_pipeline_script.py -v` | 新增质量测试后确认当前失败 | `collected 24 items`；`9 failed, 15 passed in 4.52s`；失败集中在 reviewer/service/workflow/stub 的正文少于 500 字符，LLM workflow 因 writer 响应校验失败进入 repair 后响应队列错位 | RED 成立 |
| 2 | Python 长度检查脚本 | 检查 rich fixture 和 stub 正文长度 | 初次结果：`reviewer 483/5`、`service 453/5`、`workflow 448/5`、`writer 548/5`、`schema 613/5`、`stub 484/5`；补充后结果：`reviewer 536/5`、`service 506/5`、`workflow 515/5`、`writer 548/5`、`schema 613/5`、`stub 554/5` | 通过 |
| 3 | `.\.venv\Scripts\python.exe -m pytest tests/test_schema_contracts.py tests/test_llm_writer_agent.py tests/test_llm_reviewer_agent.py tests/test_event_service.py tests/test_event_pipeline_agent_stubs.py tests/test_event_pipeline_llm_mode.py tests/test_smoke_llm_event_pipeline_script.py -v` | 修复 schema、prompt、stub 和 fixture 后运行 targeted 回归 | `collected 24 items`；24 个测试均 `PASSED`；最终 `24 passed in 4.08s` | GREEN 通过 |
| 4 | `.\.venv\Scripts\python.exe -m pytest -v` | worker 全量回归 | `collected 73 items`；73 个测试均 `PASSED`；最终 `73 passed in 13.41s` | 通过 |
| 5 | 先从 `D:\AI World Radar\.env` 注入当前进程环境，再运行 `.\.venv\Scripts\python.exe scripts\smoke_llm_event_pipeline.py --call-real-provider` | 真实 provider 高热 HN publish smoke，验证新正文质量线不会阻断发布 | stdout JSON：`status=succeeded`、`mode=real_provider`、`agent_mode=llm`、`signals_count=1`、`candidates_count=1`、`dossiers_count=1`、`agent_runs_count=3`、`failed_agent_runs_count=0`、`published_count=1`、`run_id=run_0fc3950e18bd451aa23279468a976592` | 通过 |
| 6 | Python SQLAlchemy 查询 smoke SQLite 中的 `event_dossiers` 和 `published_events` | 验证真实 provider 生成的发布正文信息量 | `EventDossier.detail_body` 长度 `1350`、段落数 `6`；`PublishedEvent.published_detail_body` 同步保存同一正文；标题为“开发者社区正在重新评估编码 Agent：从‘补全工具’到‘工作流参与者’还有多远？” | 通过 |
| 7 | `.\.venv\Scripts\python.exe -m pytest -v` | 提交前 worker 全量复验 | `collected 73 items`；73 个测试均 `PASSED`；最终 `73 passed in 12.42s` | 通过 |

### 失败与修复记录

- 失败 1：新增 `test_event_dossier_rejects_thin_detail_body` 后，旧 schema 不会拒绝三段短正文。
- 修复方式：在 `EventDossierDraft.detail_body` 上增加 `min_length=500` 和 5 段 validator。
- 失败 2：新增 `test_research_writer_prompt_requires_information_dense_detail_body` 后，writer prompt 没有明确正文信息密度要求。
- 修复方式：在 writer system/user prompt 中写入 5 段、500 字符、覆盖信息维度的要求。
- 失败 3：schema 提升后，多处 fake LLM / stub 正文未达到 500 字符，导致原本应测试 reviewer 或 workflow 的用例提前在 writer/dossier 校验处失败。
- 修复方式：补齐 rich fixture 和 `_rich_stub_detail_body` 的实际信息量，而不是放宽 schema。

### 未覆盖范围

- 本次只设置最低长度和段落数，不做自动事实核验，也不引入复杂内容评分器。
- 本次真实 provider smoke 仍使用脚本内置 HN 风格信号，未接入 live HN API。
- 本次真实 provider smoke 使用独立 SQLite smoke 库，未写入本机 PostgreSQL `ai_world_radar`。
- 本次没有统计 token、延迟和成本。
- 本次没有开发前端详情页，因此只验证后端发布正文，不验证浏览器渲染。

### 当前结论

详情正文质量缺口已修复。后端现在会拒绝低信息量三段短正文，真实 provider smoke 首跑即可生成 6 段、1350 字符的发布详情正文，并保持“HN 热度源只支撑社区热议，不写成官方确认事实”的表达边界。当前可以继续进入 P1-5 的分支覆盖、成本延迟审计和 PostgreSQL 真实 provider smoke 复验。

## 2026-06-22 P1-5 详情正文读者口吻修复记录

### 当前执行阶段

- 阶段：P1-5 发布质量与工程准备前置修复。
- 用户反馈：详情正文不应该复述 `points/comments/hn_heat_score`，不应该出现“此次候选事件、输入信号、来源信号、来源边界、只有一条信号”等开发过程语言；正文应该像用户点开详情页后阅读的事件正文，聚焦事情本身，而不是我们的评论或内部审稿说明。
- 当前完成内容：把“热度和来源边界用于内部排序 / 审稿，不进入用户正文”固化到 writer prompt、schema validator、stub 和 smoke fixture。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 本次修改的代码模块

- 修改 `apps/worker/worker/agents/llm_event_pipeline_agents.py`：
  - `ResearchWriterLLMAgent` prompt 仍保留“热度源只支撑讨论正在发生”的内部边界。
  - 但明确该边界只用于控制措辞和审稿判断，不要写入 `detail_body`。
  - `detail_body` 要求改为“只叙述事件本身”，覆盖背景、发生了什么、关键变化或讨论焦点、不同观点或争议、与中文用户相关的现实场景和后续进展。
  - 明确禁止在 `detail_body` 中复述 `points/comments/hn_heat_score`，也禁止出现“候选事件、输入信号、来源信号、来源边界、只有一条信号”等后台或审稿语言。
- 修改 `apps/worker/worker/schemas/event.py`：
  - 新增 `DETAIL_BODY_INTERNAL_TERMS`。
  - `validate_detail_body_depth` 除了检查 500 字符和 5 段外，还会拒绝明显后台流程词和热度指标词。
- 修改 `apps/worker/worker/agents/event_pipeline_agents.py`：
  - `_rich_stub_detail_body` 改成读者口吻正文，不再输出“来源信号显示 / P1 阶段 / 来源边界”等内容。
- 修改测试与 smoke fixture：
  - `tests/test_llm_writer_agent.py`
  - `tests/test_schema_contracts.py`
  - `tests/test_event_pipeline_agent_stubs.py`
  - `tests/test_event_service.py`
  - `tests/test_llm_reviewer_agent.py`
  - `tests/test_event_pipeline_llm_mode.py`
  - `scripts/smoke_llm_event_pipeline.py`

### 测试数据

- RED 测试数据：
  - prompt 侧：HN 来源信号，断言 writer prompt 不再要求正文覆盖“热度数据或传播情况 / 来源边界和未确认内容”。
  - schema 侧：构造长度和段落数达标、但包含“候选事件 / 来源信号 / points / comments”的正文，验证会被拒绝。
  - stub 侧：默认 stub 生成正文，验证不出现“候选事件 / 输入信号 / 来源信号 / 来源边界 / P1 阶段 / points / comments / hn_heat_score”。
- 真实 provider smoke 数据：
  - 来源仍是脚本内置 HN 高热社区信号，`source_signals.heat_metrics` 中保留 `points=512`、`comments=186`、`hn_heat_score=88`，用于排序和内部审稿。
  - 发布正文中不允许出现这些指标和后台词。
  - 数据库：独立 SQLite smoke 库 `runtime\p1-4-smoke\llm-event-pipeline-20260622083105.sqlite`。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_llm_writer_agent.py tests/test_schema_contracts.py tests/test_event_pipeline_agent_stubs.py -v` | 新增读者口吻测试后确认当前失败 | `collected 19 items`；`3 failed, 16 passed in 0.45s`；失败点为 writer prompt 没有“detail_body 只叙述事件本身”、schema 未拒绝后台词、stub 正文仍出现“来源信号” | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_llm_writer_agent.py tests/test_schema_contracts.py tests/test_event_pipeline_agent_stubs.py tests/test_event_service.py tests/test_llm_reviewer_agent.py tests/test_event_pipeline_llm_mode.py tests/test_smoke_llm_event_pipeline_script.py -v` | 修改 prompt、schema、stub 和正文 fixture 后的第一次 targeted 回归 | `collected 27 items`；`7 failed, 20 passed in 4.06s`；失败原因是部分新读者口吻正文少于 500 字符，导致 fake LLM repair 队列错位 | RED 后修复中 |
| 3 | Python 长度检查脚本 | 检查所有 rich fixture、stub 和 smoke fixture 正文长度 | 修复后结果：`schema 521/5`、`service 513/5`、`workflow 502/5`、`writer 542/5`、`reviewer 547/5`、`stub 522/5`、`smoke 519/5` | 通过 |
| 4 | `.\.venv\Scripts\python.exe -m pytest tests/test_llm_writer_agent.py tests/test_schema_contracts.py tests/test_event_pipeline_agent_stubs.py tests/test_event_service.py tests/test_llm_reviewer_agent.py tests/test_event_pipeline_llm_mode.py tests/test_smoke_llm_event_pipeline_script.py -v` | 修正正文长度后 targeted 回归 | `collected 27 items`；27 个测试均 `PASSED`；最终 `27 passed in 3.54s` | GREEN 通过 |
| 5 | `.\.venv\Scripts\python.exe -m pytest -v` | worker 全量回归 | `collected 76 items`；76 个测试均 `PASSED`；最终 `76 passed in 11.70s` | 通过 |
| 6 | 先从 `D:\AI World Radar\.env` 注入当前进程环境，再运行 `.\.venv\Scripts\python.exe scripts\smoke_llm_event_pipeline.py --call-real-provider` | 真实 provider smoke 第一次尝试 | 244 秒超时；没有 stdout JSON；残留 Python 进程被确认仍在执行同一 smoke 命令 | 未通过，记录为 provider 超时 |
| 7 | `Stop-Process -Id 22028,27504 -Force` 后读取 `runtime\p1-4-smoke\llm-event-pipeline-20260622082541.sqlite` | 清理超时残留进程，并确认是否写入了部分业务数据 | 残留进程结束；超时库计数为 `source_signals=0`、`pipeline_runs=0`、`agent_runs=0`、`event_candidates=0`、`event_dossiers=0`、`review_results=0`、`published_events=0` | 已清理，无业务脏数据 |
| 8 | 先从 `D:\AI World Radar\.env` 注入当前进程环境，再运行 `.\.venv\Scripts\python.exe scripts\smoke_llm_event_pipeline.py --call-real-provider`，超时上限 600 秒 | 真实 provider smoke 第二次尝试 | stdout JSON：`status=succeeded`、`mode=real_provider`、`agent_mode=llm`、`signals_count=1`、`candidates_count=1`、`dossiers_count=1`、`agent_runs_count=3`、`failed_agent_runs_count=0`、`published_count=1`、`run_id=run_44bbb2019f2d4add9ac3913c72d1ae0e` | 通过 |
| 9 | Python SQLAlchemy 查询 `runtime\p1-4-smoke\llm-event-pipeline-20260622083105.sqlite` | 验证发布正文是否仍有后台词或热度指标 | `PublishedEvent.published_detail_body` 长度 `1125`、段落数 `6`、`banned_terms_found=[]`；标题为“OpenAI 式编程 Agent 引发开发者对软件开发工作流的再讨论”；`decision=publish`、`risk_level=low` | 通过 |
| 10 | `.\.venv\Scripts\python.exe -m pytest -v` | 提交前 worker 全量复验 | `collected 76 items`；76 个测试均 `PASSED`；最终 `76 passed in 12.47s` | 通过 |

### 失败与修复记录

- 失败 1：上一版 prompt 把“热度数据或传播情况、来源边界和未确认内容”列入 `detail_body` 必须覆盖项，导致真实正文像内部审稿说明。
- 修复方式：把热度源边界保留为内部措辞约束，同时明确禁止写进 `detail_body`。
- 失败 2：schema 只检查长度和段落数，不能拦截“候选事件 / 来源信号 / points / comments”等用户不该看到的词。
- 修复方式：增加 `DETAIL_BODY_INTERNAL_TERMS` 和 validator 拦截。
- 失败 3：stub 正文仍输出“来源信号显示 / P1 阶段 / 来源边界”。
- 修复方式：重写 `_rich_stub_detail_body` 为用户正文口吻。
- 失败 4：真实 provider 第一次 smoke 超时，没有返回结果。
- 修复方式：清理残留进程，确认超时库没有业务脏数据后，用更长超时重跑，第二次成功。

### 未覆盖范围

- 本次没有引入更复杂的自动内容评分器，只拦截明确的后台词和热度指标词。
- 本次真实 provider smoke 仍使用脚本内置 HN 风格信号，未接入 live HN API。
- 本次真实 provider smoke 使用独立 SQLite smoke 库，未写入本机 PostgreSQL `ai_world_radar`。
- 本次没有统计真实 provider token、延迟和成本。
- 本次没有开发前端详情页，因此只验证后端发布正文，不验证浏览器渲染。

### 当前结论

详情正文读者口吻问题已修复。热度指标仍可保留在 `source_signals.heat_metrics` 中用于排序和内部审稿，但不会被要求写入 `detail_body`；真实 provider 生成的发布正文为 6 段、1125 字符，未出现 `points/comments/hn_heat_score`，也未出现“候选事件 / 输入信号 / 来源信号 / 来源边界 / 只有一条”等后台语言。当前可以继续推进 P1-5 的分支覆盖、成本延迟审计和 PostgreSQL 真实 provider smoke 复验。

## 2026-06-22 P1-5 发布质量分支覆盖与审计记录

### 当前执行阶段

- 阶段：P1-5 发布质量与工程准备。
- 本次目标：补齐 `revise/manual_review/reject` 分支覆盖、slug 冲突分支、LangGraph 修订循环深度分支、真实 LLM agent run 耗时与 token usage 审计，并在本机 PostgreSQL `ai_world_radar` 上复跑一次高热度 HN real provider smoke。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。
- 主链路确认：仍沿用新版 `SourceSignal -> EventCandidate -> EventDossier -> ReviewResult -> PublishedEvent`，未恢复旧 `EvidenceCard / EventCluster / ContentArtifact / QualityGateResult / Brief / BriefItem` 主链路。

### 本次修改的代码模块

- 修改 `apps/worker/worker/services/event_service.py`：
  - `save_review_result` 现在会根据 `ReviewResult.decision` 同步更新 `EventCandidate.status` 和 `EventDossier.status`。
  - `publish` 进入 `ready_to_publish`，`revise` 回到 `drafting/needs_revision`，`manual_review` 进入人工审核，`reject` 进入拒绝态。
- 修改 `apps/worker/worker/workflows/event_pipeline.py`：
  - reviewer 的 `revision_instructions` 写入 `agent_trace`，writer 修订时可读取上一轮修订意见。
  - `revise` 分支最多修订 2 次；超过最大修订次数后转入 `manual_review`，避免 LangGraph 无限循环。
  - `manual_review` 和 `reject` 分支不会写入 `published_events`。
- 修改 `apps/worker/worker/llm_client.py`：
  - 从 OpenAI-compatible response 的 `usage` 字段提取 `prompt_tokens/completion_tokens/total_tokens`，写入 `LLMClient.last_usage`。
- 修改 `apps/worker/worker/agents/llm_json_agent.py`：
  - `LLMJsonResult` 增加 `duration_ms` 和 `token_usage`。
  - repair retry 多次调用时会累加 token usage；失败时也保留最后一次耗时和 usage。
- 修改 `apps/worker/worker/tools/event_pipeline_tools.py`：
  - `record_agent_result` 和 `record_agent_failure` 写入 `agent_runs.duration_ms`。
  - `trace_json.token_usage` 在拿不到 usage 时显式记录为 `null`。
- 修改 `apps/worker/scripts/smoke_llm_event_pipeline.py`：
  - smoke JSON 摘要不再输出 `database_url`，避免泄露本地连接串。
  - `agent_runs_count` 只统计当前 `run_id`。
  - `published_count` 使用当前 `PipelineRun.published_count`，避免被同库历史数据污染。
- 新增或修改测试：
  - `apps/worker/tests/test_event_service.py`
  - `apps/worker/tests/test_event_pipeline_workflow.py`
  - `apps/worker/tests/test_llm_client.py`
  - `apps/worker/tests/test_llm_json_agent.py`
  - `apps/worker/tests/test_event_pipeline_llm_mode.py`
  - `apps/worker/tests/test_smoke_llm_event_pipeline_script.py`

### 测试数据

- `EventService` 分支测试：
  - 使用内存 SQLite。
  - 构造同一个 `EventCandidate` / `EventDossier`，分别保存 `decision=revise/manual_review/reject` 的 `ReviewResultDraft`。
  - 断言 candidate/dossier 状态流转正确，且 `publish_dossier` 不会在非 `publish` 决策下误写 `PublishedEvent`。
- slug 冲突测试：
  - 两个不同 candidate 使用 `candidate_key=openai-agent` 和 `candidate_key=openai agent`，二者基础 slug 都会变成 `openai-agent`。
  - 断言第二个不同 candidate 自动追加候选 id 稳定后缀；重复发布同一个 candidate 仍保持幂等。
- LangGraph workflow 分支测试：
  - `FixedDecisionReviewer` 依次返回 `publish/manual_review/reject`。
  - `AlwaysReviseReviewer` 始终返回 `revise`，用于验证最大修订次数兜底。
  - `RecordingWriter` 记录 writer 是否收到 reviewer 的 `revision_instructions`。
- LLM 审计测试：
  - fake OpenAI response 带 `usage` 字段，验证 `LLMClient.last_usage`。
  - fake LLM client 不带 usage，验证 `trace_json.token_usage=null`。
  - fake LLM 失败路径验证 failed AgentRun 也写入 `duration_ms`。
- PostgreSQL real provider smoke：
  - 数据库：本机 PostgreSQL `ai_world_radar`。
  - 连接串在命令和文档中只使用脱敏形式：`postgresql+psycopg://postgres:<password>@localhost:5432/ai_world_radar`。
  - 环境变量从 `D:\AI World Radar\.env` 注入当前 PowerShell 进程；未复制 `.env`，未写入 API key。
  - 来源数据仍是脚本内置 HN 高热社区信号，不是 live HN API。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest -v` | P1-5 修改前 worker 全量基线 | `76 passed in 12.38s` | 基线通过 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_event_service.py -v` | `revise/manual_review/reject` 状态流转与 slug 冲突覆盖 | 新增测试实现前曾失败：candidate 仍停留在 `reviewing`；修复后 `5 passed` | RED/GREEN 完成 |
| 3 | `.\.venv\Scripts\python.exe -m pytest tests/test_event_pipeline_workflow.py -v` | LangGraph 修订循环、修订意见传递、manual/reject 分支覆盖 | 新增测试实现前曾失败：writer 未收到修订意见；always revise 触发 `GraphRecursionError`；修复后 `6 passed` | RED/GREEN 完成 |
| 4 | `.\.venv\Scripts\python.exe -m pytest tests/test_event_service.py tests/test_event_pipeline_workflow.py tests/test_event_pipeline_tools.py tests/test_event_pipeline_llm_mode.py -v` | service/workflow/tool/llm mode 相关回归 | `15 passed in 2.15s` | 通过 |
| 5 | `.\.venv\Scripts\python.exe -m pytest tests/test_llm_client.py tests/test_llm_json_agent.py tests/test_event_pipeline_llm_mode.py -v` | LLM token usage、duration_ms、失败路径审计 | `13 passed in 1.43s` | 通过 |
| 6 | `.\.venv\Scripts\python.exe -m pytest tests/test_event_service.py tests/test_event_pipeline_workflow.py tests/test_event_pipeline_tools.py tests/test_event_pipeline_llm_mode.py tests/test_llm_client.py tests/test_llm_json_agent.py tests/test_run_log_service.py -v` | P1-5 相关服务层、workflow、审计记录联合回归 | `26 passed in 2.40s` | 通过 |
| 7 | `.\.venv\Scripts\python.exe -m pytest tests/test_smoke_llm_event_pipeline_script.py -v` | smoke summary 只统计当前 run，并确认摘要不输出数据库连接串 | 新增测试实现前暴露问题：summary 统计整库历史数据，且失败 JSON 含 `database_url`；修复后 `3 passed in 16.60s` | RED/GREEN 完成 |
| 8 | `.\.venv\Scripts\python.exe -m pytest -v` | 本次代码修改后的 worker 全量回归 | `89 passed in 19.66s` | 通过 |
| 9 | `.\.venv\Scripts\python.exe scripts\smoke_llm_event_pipeline.py --call-real-provider --database-url postgresql+psycopg://postgres:<password>@localhost:5432/ai_world_radar` | 本机 PostgreSQL `ai_world_radar` 高热度 HN real provider smoke | stdout JSON：`status=succeeded`、`mode=real_provider`、`agent_mode=llm`、`signals_count=1`、`candidates_count=1`、`dossiers_count=3`、`agent_runs_count=7`、`failed_agent_runs_count=0`、`published_count=1`、`run_id=run_9edd05cbf4aa464593172c01911fa068` | 通过 |
| 10 | SQLAlchemy 查询 PostgreSQL `ai_world_radar` 核心表计数 | 确认真实 provider smoke 数据已入库 | `sources=2`、`source_signals=3`、`pipeline_runs=3`、`agent_runs=15`、`event_candidates=3`、`event_dossiers=6`、`review_results=6`、`published_events=3` | 通过；该库包含历史 smoke 数据 |
| 11 | SQLAlchemy 按 `run_id=run_9edd05cbf4aa464593172c01911fa068` 查询 `pipeline_runs` 和 `agent_runs` | 验证 per-run 计数、耗时和 token usage | `PipelineRun.status=succeeded`、`signals_count=1`、`candidates_count=1`、`dossiers_count=3`、`published_count=1`、`failed_count=0`；该 run 下 `agent_runs=7`、`failed=0`、provider/model 为 `openai/gpt-5.5`，每条 agent run 均有 `duration_ms`，且 token usage 可取到并已记录 | 通过 |
| 12 | SQLAlchemy 查询该 run 产出的发布事件 | 验证 PostgreSQL 中真实发布结果 | `id=pub_eb31152fd88c423f8a351ffb46d31f32`、`slug=hn-developers-debate-openai-coding-agents-2026-06-22`、标题为“开发者社区讨论 OpenAI 式编码 Agent 对日常软件开发流程的影响”、`body_length=1359` | 通过 |

### PostgreSQL real provider smoke 摘要

- `run_id`：`run_9edd05cbf4aa464593172c01911fa068`
- `status`：`succeeded`
- `published_count`：`1`
- `agent_runs_count`：`7`
- `failed_agent_runs_count`：`0`
- `dossiers_count`：`3`
- 该 run 出现了 reviewer `revise` 后再次写作并最终发布的修订循环，因此 agent runs 多于 3 条。
- 当前 provider 返回 token usage，已记录到 `agent_runs.trace_json.token_usage`；如果后续 provider 不返回 usage，系统会按本次实现显式记录为 `null`。
- 本机 PostgreSQL `ai_world_radar` 不是全新空库，已经包含此前 smoke 数据；本次验收以 `run_id` 过滤的 per-run 结果为准。

### 失败与修复记录

- 失败 1：`ReviewResult.decision=revise/manual_review/reject` 保存后，candidate 仍停留在 `reviewing`，导致状态流转不准确。
  - 修复方式：`EventService.save_review_result` 根据 review decision 同步更新 candidate 和 dossier 状态。
- 失败 2：workflow 中 reviewer 返回 `revise` 后，下一轮 writer 拿不到 `revision_instructions`。
  - 修复方式：reviewer 节点把 `revision_instructions` 写入 `agent_trace`，writer 修订节点从上一条 review trace 读取。
- 失败 3：reviewer 持续返回 `revise` 会触发 LangGraph `GraphRecursionError`。
  - 修复方式：增加最大修订次数兜底，超过后转入 `manual_review`，不再继续循环，也不写入 `published_events`。
- 失败 4：LLM client 只返回文本，没有暴露 token usage；AgentRun 也没有系统记录耗时。
  - 修复方式：`LLMClient.last_usage` 提取 usage，`LLMJsonAgent` 计算 `duration_ms` 并累加 retry usage，tool 层写入 `agent_runs.duration_ms` 和 `trace_json.token_usage`。
- 失败 5：smoke summary 的 `agent_runs_count` 和 `published_count` 使用整库计数，在 PostgreSQL 有历史数据时会误导验收。
  - 修复方式：`agent_runs_count` 按 `pipeline_run_id` 过滤；`published_count` 改用当前 `PipelineRun.published_count`。
- 失败 6：smoke 失败 JSON 暴露 `database_url`。
  - 修复方式：成功和失败摘要均移除 `database_url` 字段；文档只保留脱敏连接串形态。

### 未覆盖范围

- 本次 PostgreSQL smoke 使用脚本内置 HN 高热社区信号，未接入 live HN API。
- 本次未补 HN / GitHub live API 的限流、网络异常、分页边界和 provider 不可用分支。
- 本次未开发 FastAPI、Next.js 前端、后台管理 UI、Redis、队列、对象存储或部署脚本。
- 本次未引入正式内容质量评分器，只完成发布分支、修订深度、审计字段和 smoke 计数可信度兜底。
- 本次未实现受控 tool-calling；后续如拆出 `tool_calls` 表，需要重新补审计测试。

### 当前结论

P1-5 第一轮发布质量分支覆盖与审计闭环已完成。当前 worker 已覆盖 `revise/manual_review/reject` 非发布分支、slug 冲突和同 candidate 幂等发布、LangGraph 修订循环深度、真实 LLM 耗时与 token usage 审计，并已在本机 PostgreSQL `ai_world_radar` 上完成高热度 HN real provider smoke。当前可以进入 P1 后续产品 API / 前端页面 / 后台管理，或先做 P1 发布前总体验收清单。

## 2026-06-22 P1-6 产品接口层方案记录

### 当前执行阶段

- 阶段：P1-6 产品接口层方案确认与 FastAPI 口径同步。
- 本次目标：根据用户确认，将 P1-6 方案从“FastAPI 待确认”调整为“直接采用 FastAPI 薄适配层”；本次仍只改文档，不写接口代码。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。
- 主链路确认：仍沿用新版 `SourceSignal -> EventCandidate -> EventDossier -> ReviewResult -> PublishedEvent`，不恢复旧 `EvidenceCard / EventCluster / ContentArtifact / QualityGateResult / Brief / BriefItem` 主链路。

### 本次修改范围

- 新增 `docs/05-实现计划/P1-6 产品接口层与最小API计划.md`。
- 新增 `docs/05-实现计划/P1-6 产品接口层与最小API计划.html`。
- 更新 `docs/05-实现计划/README.md`。
- 更新 `docs/README.md`。
- 更新 `docs/00-项目总览/文档索引.md`。
- 更新 `docs/00-项目总览/项目状态.md`。
- 更新本测试记录。

### 方案结论

- 用户已确认采用两步方案：先实现 `ProductQueryService` 和 Pydantic 响应 schema，再引入 FastAPI 薄适配层。
- P1-6 产品接口层只读已有数据，不触发采集、不触发 Agent、不修改核心生产表。
- 第一批接口草案包括 `GET /events`、`GET /events/{slug}`、pipeline run / agent run 审计查询和 `manual_review` 队列查询。
- FastAPI / Uvicorn 依赖尚未加入，后续 Task 2 实现时加入。
- 前台事件列表不返回 `ranking_score`、`heat_score`、`importance_score` 等复杂分数；这些分数只用于内部排序和后台审计。
- 事件详情必须通过 `published_events.dossier_id` 联查发布时对应的 `event_dossiers` 版本。
- `manual_review` 队列同一 candidate 只返回最新 dossier 和最新 review。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `Test-Path "docs\05-实现计划\P1-6 产品接口层与最小API计划.md"` | 确认 P1-6 方案文档是否已存在 | 修改前输出 `False` | 确认需新增 |
| 2 | `Test-Path "docs\05-实现计划\P1-6 产品接口层与最小API计划.md"; Test-Path "docs\05-实现计划\P1-6 产品接口层与最小API计划.html"` | 确认 Markdown 和 HTML 阅读版已创建 | 输出 `True`、`True` | 通过 |
| 3 | `git diff --check` | 检查文档 diff 是否有 trailing whitespace 或冲突标记 | 退出码 0；仅有 Windows 工作区 LF/CRLF 提示 | 通过 |
| 4 | `rg "T[O]DO|TBD|待补充|占位符|预[期]通过|应[该]通过|执行后写入真实输出" "docs\05-实现计划\P1-6 产品接口层与最小API计划.md" "docs\05-实现计划\P1-6 产品接口层与最小API计划.html"` | 检查新增方案文档是否存在占位或伪验收描述 | 退出码 1，无匹配 | 通过 |
| 5 | `rg "postgresql\+psycopg://postgres:[^<]|postgres:c[j]y|s[k]-[A-Za-z0-9_-]{20,}|OPENAI_API_KEY=s[k]|DEEPSEEK_API_KEY=s[k]" docs` | 检查文档是否泄露数据库密码或 API key，命令写法避免自匹配 | 退出码 1，无匹配 | 通过 |

### 未覆盖范围

- 本次没有修改 `apps/worker` 代码。
- 本次没有新增 `ProductQueryService`。
- 本次没有引入 FastAPI / Uvicorn，依赖将在 P1-6 Task 2 实现时加入。
- 本次没有运行 worker pytest，因为没有产品接口实现代码可测；后续 Task 1 开始必须测试先行。
- 本次没有运行 PostgreSQL 产品查询 smoke，需等 query service 或 API 实现后执行。

### 当前结论

P1-6 产品接口层方案已按用户决策更新。当前已确认采用 `ProductQueryService + FastAPI` 薄适配层方案；下一步可以从 Task 1 开始测试先行实现。

## 2026-06-22 P1-6 Task 1 ProductQueryService 测试记录

### 当前执行阶段

- 阶段：P1-6 产品接口层 Task 1。
- 本次目标：先实现只读 `ProductQueryService` 和 Pydantic 响应 schema，不引入 FastAPI。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。
- 执行时间：2026-06-22 23:04:47 +08:00。

### 本次修改的代码模块

- 新增 `apps/worker/worker/schemas/product.py`：
  - `ProductEventListItem`
  - `ProductEventDetail`
  - `ProductPipelineRunItem`
  - `ProductAgentRunItem`
  - `ManualReviewQueueItem`
- 新增 `apps/worker/worker/services/product_query_service.py`：
  - `list_published_events`
  - `get_event_by_slug`
  - `list_pipeline_runs`
  - `get_pipeline_run`
  - `list_agent_runs`
  - `list_manual_review_items`
- 修改 `apps/worker/worker/services/__init__.py`：导出 `ProductQueryService`。
- 新增 `apps/worker/tests/test_product_query_service.py`：覆盖产品查询服务核心行为。

### 测试数据

- 使用内存 SQLite，`sessionmaker(autoflush=False, expire_on_commit=False)`，和生产 session 的 autoflush 口径一致。
- published 列表测试：
  - 两条 `published_events.status="published"` 事件。
  - 一条 `published_events.status="hidden"` 事件。
  - 设置 `homepage_rank=1/2` 验证首页排序。
- 详情测试：
  - 同一个 candidate 先发布 dossier v1，再新增未发布 dossier v2。
  - 验证详情接口通过 `published_events.dossier_id` 读取发布时版本，不误读最新草稿。
- Agent run 审计测试：
  - 写入一条 `trace_json.llm_raw_text` 和 `trace_json.token_usage` 同时存在的 AgentRun。
  - 验证响应只暴露 `token_usage`，不暴露完整 `llm_raw_text` 或 `trace_json`。
- manual_review 队列测试：
  - 同一个 candidate 写入两版 manual_review dossier/review。
  - 另写入一个 reject candidate。
  - 验证队列只返回 manual_review candidate，且使用最新 dossier version。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_product_query_service.py -v` | Task 1 RED：产品查询服务尚未实现 | `ModuleNotFoundError: No module named 'worker.services.product_query_service'`，`1 error during collection` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_product_query_service.py -v` | Task 1 GREEN：ProductQueryService targeted 测试 | 首次 GREEN 前测试数据正文低于既有 500 字符门槛，修正 fixture 后结果为 `4 passed in 0.70s` | 通过 |
| 3 | `.\.venv\Scripts\python.exe -m pytest tests/test_product_query_service.py tests/test_event_service.py tests/test_run_log_service.py -v` | 产品查询服务与事件服务、运行记录服务相关回归 | `10 passed in 0.93s` | 通过 |

### 失败与修复记录

- RED 失败符合预期：`ProductQueryService` 尚不存在。
- 实现后首次 targeted 测试失败原因是测试 fixture 的 `detail_body` 低于既有 `EventDossierDraft.detail_body` 500 字符门槛。
- 修复方式：补足测试正文信息量，不放宽生产 schema 校验。

### 未覆盖范围

- 本次未引入 FastAPI / Uvicorn。
- 本次未开发 HTTP endpoint。
- 本次未运行 PostgreSQL 只读 smoke。
- 本次未开发前端页面、后台 UI、鉴权或部署能力。

### 当前结论

P1-6 Task 1 已完成。当前已经有可复用的只读 `ProductQueryService` 和产品响应 schema，可以进入 Task 2：FastAPI HTTP 薄适配层。

## 2026-06-22 P1-6 Task 2 FastAPI 薄适配层测试记录

### 当前执行阶段

- 阶段：P1-6 产品接口层 Task 2。
- 本次目标：在 Task 1 `ProductQueryService` 之上新增 FastAPI 只读 HTTP 适配层。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 本次修改的代码模块

- 修改 `apps/worker/pyproject.toml`：新增 `fastapi==0.138.0`、`uvicorn==0.49.0`。
- 新增 `apps/worker/worker/api/__init__.py`：导出 `create_app`。
- 新增 `apps/worker/worker/api/dependencies.py`：提供 Session 工厂和 `ProductQueryService` dependency。
- 新增 `apps/worker/worker/api/app.py`：
  - `create_app`
  - `GET /health`
  - `GET /events`
  - `GET /events/{slug}`
  - `GET /admin/pipeline-runs`
  - `GET /admin/pipeline-runs/{run_id}`
  - `GET /admin/pipeline-runs/{run_id}/agent-runs`
  - `GET /admin/review-queue`
- 新增 `apps/worker/tests/test_product_api.py`：覆盖前台和后台只读 API。

### 测试数据

- 使用 `StaticPool` 内存 SQLite，保证 TestClient 多请求复用同一测试数据库。
- 前台 API 测试：
  - 创建一条已发布事件。
  - 验证 `/events` 不返回 `ranking_score`。
  - 验证 `/events/{slug}` 返回详情和 `why_it_matters`。
  - 验证缺失 slug 返回 404。
- 后台 API 测试：
  - 创建一条 `pipeline_runs`。
  - 创建一条带 `trace_json.llm_raw_text` 和 `trace_json.token_usage` 的 `agent_runs`。
  - 创建一条 `manual_review` 事件。
  - 验证 agent run API 只返回 `token_usage`，不返回完整 `trace_json` 或 `llm_raw_text`。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_product_api.py -v` | Task 2 RED：FastAPI 依赖尚未安装 | `ModuleNotFoundError: No module named 'fastapi'`，`1 error during collection` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pip index versions fastapi` | 查询 FastAPI 可用版本 | 当前可用最新显示为 `fastapi (0.138.0)` | 记录版本 |
| 3 | `.\.venv\Scripts\python.exe -m pip index versions uvicorn` | 查询 Uvicorn 可用版本 | 当前可用最新显示为 `uvicorn (0.49.0)` | 记录版本 |
| 4 | `.\.venv\Scripts\python.exe -m pip install -e ".[dev]"` | 安装新增依赖 | 输出包含 `Successfully installed ... fastapi-0.138.0 ... uvicorn-0.49.0` | 通过 |
| 5 | `.\.venv\Scripts\python.exe -m pytest tests/test_product_api.py -v` | Task 2 第二轮 RED：API 模块尚未实现 | `ModuleNotFoundError: No module named 'worker.api'`，`1 error during collection` | RED 成立 |
| 6 | `.\.venv\Scripts\python.exe -m pytest tests/test_product_api.py -v` | FastAPI targeted 测试 | 实现后曾出现 `/events` 422，原因为当前 FastAPI 未识别 `Annotated[..., Depends(...)]` 依赖写法；改成 `service: ProductQueryService = Depends(...)` 后结果为 `2 passed in 1.05s` | 通过 |
| 7 | `.\.venv\Scripts\python.exe -m pytest tests/test_product_query_service.py tests/test_product_api.py -v` | 产品查询服务和 HTTP 适配层联合回归 | `6 passed in 1.13s` | 通过 |

### 失败与修复记录

- 失败 1：测试首次运行缺少 `fastapi`。
  - 修复方式：在 `pyproject.toml` 写入 `fastapi==0.138.0`、`uvicorn==0.49.0` 并安装。
- 失败 2：依赖安装后测试缺少 `worker.api`。
  - 修复方式：新增 `worker/api` 包和 `create_app`。
- 失败 3：实现后 `/events` 返回 422，FastAPI 将 `service` 识别成 query 参数。
  - 修复方式：将 dependency 参数改为 `service: ProductQueryService = Depends(query_dependency)`。
- 失败 4：API 测试 fixture 的 `detail_body` 低于既有 500 字符门槛。
  - 修复方式：补足测试正文信息量，不放宽生产 schema 校验。
- 处理 1：`fastapi.testclient` 在当前版本会触发 Starlette deprecation warning。
  - 处理方式：仅在测试文件导入 TestClient 时过滤这条第三方已知 warning，业务测试输出保持干净。

### 未覆盖范围

- 本次尚未运行 worker 全量 pytest。
- 本次尚未运行本机 PostgreSQL `ai_world_radar` 只读 smoke。
- 本次未开发鉴权；管理端接口当前只适合本地或内网开发联调。
- 本次未开发任何写接口，也未触发采集、Agent、发布或重跑。

### 当前结论

P1-6 Task 2 已完成。当前已有 FastAPI 只读产品接口层，可以进入 Task 3：全量测试、PostgreSQL 只读 smoke 和最终文档验收。

## 2026-06-22 P1-6 Task 3 PostgreSQL 只读 smoke 与最终验收记录

### 当前执行阶段

- 阶段：P1-6 产品接口层 Task 3。
- 本次目标：补齐产品查询 smoke 脚本，运行 P1-6 相关回归、worker 全量测试，并在本机 PostgreSQL `ai_world_radar` 上验证只读产品查询可读取真实入库数据。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 本次修改的代码模块

- 新增 `apps/worker/scripts/smoke_product_queries.py`：
  - `parse_args`：解析 `--database-url`，输出中不回显连接串。
  - `main`：创建 `autoflush=False` session，执行只读 smoke，并打印 JSON 摘要。
  - `build_smoke_summary`：调用 `ProductQueryService` 读取事件列表、首条事件详情、pipeline run、agent run 和 `manual_review` 队列。
- 新增 `apps/worker/tests/test_smoke_product_queries_script.py`：
  - 用临时 SQLite 种入产品查询 smoke 所需最小数据。
  - 验证脚本返回 0、stdout JSON 计数正确、且不包含 `database_url`。

### 测试数据

- 单测数据：临时 SQLite 文件库。
- 单测种子数据：
  - 1 条 `published_events`，来源于 `Product Query Smoke 事件`。
  - 1 条 `pipeline_runs`，run_key 为 `product-query-smoke-run`。
  - 1 条 `agent_runs`，`trace_json.token_usage={"prompt_tokens":10,"completion_tokens":5,"total_tokens":15}`。
  - 1 条 `manual_review` 事件，用于验证人工审核队列读取。
- PostgreSQL smoke 数据：本机 PostgreSQL `ai_world_radar`，连接串在文档中脱敏为 `postgresql+psycopg://postgres:<password>@localhost:5432/ai_world_radar`。

### 命令与真实结果

| 顺序 | 命令 | 测试内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `.\.venv\Scripts\python.exe -m pytest tests/test_smoke_product_queries_script.py -v` | Task 3 RED：smoke 脚本尚未实现 | `can't open file ... scripts\smoke_product_queries.py: [Errno 2] No such file or directory`，`1 failed in 0.93s` | RED 成立 |
| 2 | `.\.venv\Scripts\python.exe -m pytest tests/test_smoke_product_queries_script.py -v` | Task 3 GREEN：smoke 脚本读取已有库并输出摘要 | `1 passed in 1.91s` | 通过 |
| 3 | `.\.venv\Scripts\python.exe -m pytest tests/test_product_query_service.py tests/test_product_api.py tests/test_smoke_product_queries_script.py -v` | P1-6 查询服务、FastAPI 和 smoke 脚本联合回归 | `7 passed in 2.88s` | 通过 |
| 4 | `.\.venv\Scripts\python.exe -m pytest -v` | worker 全量回归 | 首次全量结果为 `96 passed in 26.23s`；文档更新后复跑最新结果为 `96 passed in 23.40s` | 通过 |
| 5 | `.\.venv\Scripts\python.exe scripts\smoke_product_queries.py --database-url postgresql+psycopg://postgres:<password>@localhost:5432/ai_world_radar` | 本机 PostgreSQL 只读产品查询 smoke | `status=succeeded`、`events_count=3`、`detail_found=true`、`pipeline_runs_count=3`、`agent_runs_count=7`、`review_queue_count=0`、`first_event_slug=demo-openai-releases-a-new-developer-tool`、`first_pipeline_run_id=run_9edd05cbf4aa464593172c01911fa068` | 通过 |
| 6 | SQLAlchemy 只读计数查询 PostgreSQL `ai_world_radar` 核心表 | 确认真实 provider / smoke 数据已入库且产品查询有数据可读 | `sources_count=2`、`source_signals_count=3`、`source_signal_counts_by_source={"demo":1,"hn_algolia":2}`、`event_candidates_count=3`、`event_candidate_signals_count=3`、`event_dossiers_count=6`、`review_results_count=6`、`published_events_count=3`、`pipeline_runs_count=3`、`agent_runs_count=15` | 通过 |

### 失败与修复记录

- 失败 1：Task 3 RED 时 `scripts/smoke_product_queries.py` 不存在。
  - 修复方式：新增只读 smoke 脚本，复用 `ProductQueryService`，不新增任何写库逻辑。
- 处理 1：本机 worktree 没有 `.env`，当前进程环境变量也没有 `DATABASE_URL`。
  - 处理方式：仅在本机 PowerShell 进程中临时传入脱敏文档外的实际连接串；文档、代码和 git 中均不写入密码。

### 未覆盖范围

- 本次没有开发鉴权；`/admin/*` 当前只适合本地或内网开发联调，公开部署前必须补权限保护。
- 本次没有开发写接口、人工修正动作、发布/隐藏/驳回/重跑接口。
- 本次没有开发前端页面、后台管理 UI 或线上部署脚本。
- 本次 PostgreSQL 产品查询 smoke 是只读查询，不触发 live HN API、真实 LLM Agent、采集或发布。
- 本次没有覆盖大规模分页性能、索引优化和缓存策略。

### 当前结论

P1-6 后端产品接口层已完成最小闭环。当前已有 `ProductQueryService`、Pydantic 产品响应 schema、FastAPI 只读 HTTP endpoint、脚本化 PostgreSQL 只读 smoke 和人工可读验收记录；worker 全量测试最新结果为 `96 passed in 23.40s`。当前可以进入产品页面联调或前端页面开发，但公开部署前必须补 API 鉴权和管理员权限。

## 2026-06-23 P1-6 FastAPI 真实 HTTP 运行验收记录

### 当前执行阶段

- 阶段：P1-6 产品接口层运行验收。
- 本次目标：在本机启动 FastAPI 服务，连接 PostgreSQL `ai_world_radar`，通过真实 HTTP 请求验证 public/admin 只读接口可被前端或后台页面消费。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 验收环境

- 服务端口：`http://127.0.0.1:8016`。
- 启动方式：`.\.venv\Scripts\python.exe -m uvicorn worker.api.app:create_app --factory --host 127.0.0.1 --port 8016`。
- 数据库：本机 PostgreSQL `ai_world_radar`。
- 数据库连接串记录方式：文档中仅保留脱敏形式 `postgresql+psycopg://postgres:<password>@localhost:5432/ai_world_radar`。
- 日志目录：`runtime\api-smoke`。
- 服务状态：验收结束后已停止 uvicorn；端口 `8016` 仅剩 `TIME_WAIT`，无监听进程。

### 命令与真实结果

| 顺序 | 命令 / 请求 | 验收内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `git status --short --branch` | 确认 push 后分支和工作区状态 | `## codex/p1-data-foundation...origin/codex/p1-data-foundation` | 通过 |
| 2 | `Start-Process ... uvicorn worker.api.app:create_app --factory --host 127.0.0.1 --port 8016` | 启动 FastAPI 只读接口服务 | uvicorn 日志显示 `Application startup complete`、`Uvicorn running on http://127.0.0.1:8016` | 通过 |
| 3 | `GET /health` | 健康检查 | `status_code=200`、`body_status=ok`、`elapsed_ms=34` | 通过 |
| 4 | `GET /events?limit=10` | 前台事件列表 | `status_code=200`、`count=3`、`first_slug=demo-openai-releases-a-new-developer-tool`、`exposed_internal_scores=false` | 通过 |
| 5 | `GET /events/{first_slug}` | 前台事件详情 | `status_code=200`、`slug=demo-openai-releases-a-new-developer-tool`、`body_length=128`、`source_refs_count=1` | 通过 |
| 6 | `GET /events/not-existing-product-api-smoke-slug` | 缺失事件详情 | `status_code=404`、`detail=Event not found` | 通过 |
| 7 | `GET /admin/pipeline-runs?limit=10` | 后台 pipeline run 列表 | `status_code=200`、`count=3`、`first_run_id=run_9edd05cbf4aa464593172c01911fa068` | 通过 |
| 8 | `GET /admin/pipeline-runs/run_9edd05cbf4aa464593172c01911fa068` | 单次 pipeline run 详情 | `status_code=200`、`published_count=1` | 通过 |
| 9 | `GET /admin/pipeline-runs/run_9edd05cbf4aa464593172c01911fa068/agent-runs` | Agent run 审计列表 | `status_code=200`、`count=7`、`raw_trace_exposed=false` | 通过 |
| 10 | `GET /admin/review-queue?limit=10` | 人工审核队列 | `status_code=200`、`count=0` | 通过 |
| 11 | `GET /events/hn-developers-debate-openai-coding-agents-2026-06-22` | PostgreSQL 中真实 HN 发布事件详情 | `status_code=200`、标题为“开发者社区讨论 OpenAI 式编码 Agent 对日常软件开发流程的影响”、`body_length=1359`、`source_refs_count=1`、`follow_up_points_count=4` | 通过 |
| 12 | `Stop-Process` + `Get-NetTCPConnection -LocalPort 8016` | 停止验收服务 | 仅剩 `TIME_WAIT`，无 `Listen` 进程 | 通过 |

### 失败与修复记录

- 处理 1：第一次启动命令把日志目录误指向 `apps\worker\runtime`，该目录不存在，命令在启动服务前失败。
  - 修复方式：将日志目录修正为项目根目录下的 `runtime\api-smoke`，随后 uvicorn 正常启动。
- 处理 2：PowerShell `Invoke-WebRequest` 展示中文 JSON 时出现客户端乱码。
  - 修复方式：改用 Python `urllib` 按 UTF-8 解码复验，确认真实 HN 事件标题和正文均正常。

### 未覆盖范围

- 本次只做本机 HTTP 验收，没有做线上部署、反向代理、CORS、TLS 或域名配置。
- 本次没有新增 API 鉴权；`/admin/*` 仍只适合本机或内网开发联调。
- 本次没有做浏览器端前端页面联调；下一阶段需要由前端消费这些接口。
- 本次没有触发采集、Agent、workflow、发布或任何写接口。

### 当前结论

P1-6 FastAPI 真实 HTTP 运行验收通过。服务连接本机 PostgreSQL `ai_world_radar` 后，public 事件列表、事件详情、缺失详情 404、pipeline run、agent run 审计和 `manual_review` 队列均可通过真实 HTTP 请求读取；agent run 响应未暴露 `trace_json` 或 `llm_raw_text`。当前可以进入产品页面联调或前端页面开发。

## 2026-06-23 P1 全链路真实验收记录

### 当前执行阶段

- 阶段：P1 全链路真实验收。
- 本次目标：按用户要求跑一遍 `live HN API -> 真实 LLM Agent -> PostgreSQL 写入 -> FastAPI HTTP 读取` 的端到端链路。
- 当前分支：`codex/p1-data-foundation`。
- 当前 worktree：`C:\Users\admin\.config\superpowers\worktrees\AI World Radar\p1-data-foundation`。

### 验收环境

- live 来源：HN Algolia API，脚本参数 `--source hn --hn-days 7 --hn-limit 1`。
- 真实 Agent：`--agent-mode llm`，使用本机 `.env` 中 OpenAI / OpenAI-compatible 配置；文档不记录 API key。
- 数据库：本机 PostgreSQL `ai_world_radar`。
- 数据库连接串记录方式：文档中仅保留脱敏形式 `postgresql+psycopg://postgres:<password>@localhost:5432/ai_world_radar`。
- API 服务：临时启动 `uvicorn worker.api.app:create_app --factory --host 127.0.0.1 --port 8017`；验收后已停止。

### 命令与真实结果

| 顺序 | 命令 / 请求 | 验收内容 | 真实输出摘要 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | `git status --short --branch` | 确认验收前分支和工作区状态 | `## codex/p1-data-foundation...origin/codex/p1-data-foundation` | 通过 |
| 2 | 检查 `D:\AI World Radar\.env` | 确认真实 LLM 所需环境变量存在 | `.env` 存在；`OPENAI_API_KEY`、`OPENAI_BASE_URL`、`LLM_PROVIDER`、`LLM_MODEL` 存在；未打印 secret | 通过 |
| 3 | `.\.venv\Scripts\python.exe scripts\collect_source_signals.py --database-url postgresql+psycopg://postgres:<password>@localhost:5432/ai_world_radar --source hn --hn-days 7 --hn-limit 1` | live HN API 采集写入 PostgreSQL `source_signals` | stdout JSON：`status=succeeded`、`sources_count=1`、`signals_count=3`、`source_keys=["hn_algolia"]` | 通过 |
| 4 | SQLAlchemy 查询最新 HN signal | 确认本次 live HN 采集到的真实来源 | `signal_id=sig_d4823864c09a4fc981daec6d6cc3f41d`、`source_item_id=48631169`、标题为 `Show HN: PMB – local-first memory for AI coding agents over MCP`、`points=7`、`comments=6`、`hn_heat_score=19` | 通过 |
| 5 | `.\.venv\Scripts\python.exe scripts\run_event_pipeline.py --database-url postgresql+psycopg://postgres:<password>@localhost:5432/ai_world_radar --run-key p1-full-real-20260623-1021 --source-key hn_algolia --limit 1 --agent-mode llm` | 消费 live HN signal，真实 LLM Agent 生成候选、档案、审稿并写入 PostgreSQL | stdout JSON：`status=succeeded`、`agent_mode=llm`、`run_id=run_f2cabe57ea404c5fbb23244d9a428f22`、`signals_count=1`、`candidates_count=1`、`dossiers_count=1`、`published_count=1`、`published_event_id=pub_bc6ee13a869347c4b3abd083e2e73e42` | 通过 |
| 6 | SQLAlchemy 查询 `run_id=run_f2cabe57ea404c5fbb23244d9a428f22` | 核验 PostgreSQL 中本次 run 产物 | run `status=succeeded`；candidate `cand_08792e0c5e0249adbfc56eddcd387cbf`；dossier `dos_0cdb0a2e38a8458e9954696d70d98174`；review `decision=publish`、`risk_level=low`；published slug `pmb-local-first-memory-mcp-ai-coding-agents`；正文长度 `1226`、段落数 `7` | 通过 |
| 7 | SQLAlchemy 查询本次 `agent_runs` | 核验真实 LLM 审计字段 | 3 条 agent run 均 `status=succeeded`；provider=`openai`；model=`gpt-5.5`；editor `duration_ms=24550`、`total_tokens=1245`；writer `duration_ms=63189`、`retry_count=1`、`total_tokens=7493`；reviewer `duration_ms=44084`、`retry_count=1`、`total_tokens=5671` | 通过 |
| 8 | 启动 `uvicorn worker.api.app:create_app --factory --host 127.0.0.1 --port 8017` | 启动 FastAPI 读取同一个 PostgreSQL 库 | uvicorn 日志显示 `Application startup complete`、`Uvicorn running on http://127.0.0.1:8017` | 通过 |
| 9 | `GET /events?limit=20` | 通过真实 HTTP 验证前台列表包含本次发布事件 | `status_code=200`、`count=4`、`contains_new_slug=true`、`new_slug=pmb-local-first-memory-mcp-ai-coding-agents`、`exposed_internal_scores=false` | 通过 |
| 10 | `GET /events/pmb-local-first-memory-mcp-ai-coding-agents` | 通过真实 HTTP 验证本次事件详情 | `status_code=200`、标题为“PMB 发布本地优先记忆层，试图让 AI 编程 Agent 在项目中持续记住上下文”、`body_length=1226`、`source_refs_count=1`、`follow_up_points_count=5` | 通过 |
| 11 | `GET /admin/pipeline-runs/run_f2cabe57ea404c5fbb23244d9a428f22` | 通过真实 HTTP 验证后台 run 详情 | `status_code=200`、`status=succeeded`、`published_count=1` | 通过 |
| 12 | `GET /admin/pipeline-runs/run_f2cabe57ea404c5fbb23244d9a428f22/agent-runs` | 通过真实 HTTP 验证后台 Agent run 审计 | `status_code=200`、`count=3`、`raw_trace_exposed=false` | 通过 |
| 13 | `Stop-Process` + `Get-NetTCPConnection -LocalPort 8017` | 停止验收服务 | `PORT_8017_NO_LISTEN` | 通过 |

### 本次真实产物

- live HN signal：`sig_d4823864c09a4fc981daec6d6cc3f41d`。
- HN item：`48631169`。
- 来源标题：`Show HN: PMB – local-first memory for AI coding agents over MCP`。
- pipeline run：`run_f2cabe57ea404c5fbb23244d9a428f22`。
- candidate：`cand_08792e0c5e0249adbfc56eddcd387cbf`。
- dossier：`dos_0cdb0a2e38a8458e9954696d70d98174`。
- review：`rev_fe461cf7a2504f5f8c0fd4a763768165`，decision 为 `publish`。
- published event：`pub_bc6ee13a869347c4b3abd083e2e73e42`。
- slug：`pmb-local-first-memory-mcp-ai-coding-agents`。

### 失败与修复记录

- 本轮全链路没有出现业务失败。
- 注意点 1：采集脚本 summary 的 `signals_count=3` 是 `hn_algolia` 来源当前总 signal 数，不是本轮新增数；因此后续用 SQLAlchemy 查询最新 signal 和 pipeline `run_id` 做本轮归因。
- 注意点 2：本轮 live HN 信号热度较低，`hn_heat_score=19`，但链路验收目标是验证真实端到端能力，不是验证高热排序策略。

### 未覆盖范围

- 本次没有跑 GitHub live source；只选择 HN live source 做端到端闭环。
- 本次没有做并发、多页采集或长周期定时采集。
- 本次没有开发或验证 API 鉴权；`/admin/*` 仍只适合本机或内网开发联调。
- 本次没有启动 Next.js 前端页面；前端完成后还需要做页面级联调验收。

### 当前结论

P1 全链路真实验收通过。系统已完成一次真实 HN API 采集、真实 OpenAI / OpenAI-compatible LLM Agent 生产、PostgreSQL 全链路写入、FastAPI HTTP 读取的端到端验证。本轮生成并发布了 `pmb-local-first-memory-mcp-ai-coding-agents` 事件，前台列表、详情页接口和后台审计接口均能读取本轮真实产物。
